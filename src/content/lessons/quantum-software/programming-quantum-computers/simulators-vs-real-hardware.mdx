import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemMetaForLesson } from "@/lib/problems/metaRegistry";
import { stateVectorAmplitudeCount, stateVectorMemoryBytes } from "@/lib/quantum/simulationCost";
import { PredictBeforeReveal } from "@/components/mdx/PredictBeforeReveal";

export const formatMemoryBytes = (bytes) => (
  bytes < 1e6 ? `${(bytes / 1e3).toFixed(1)} KB`
  : bytes < 1e9 ? `${(bytes / 1e6).toFixed(1)} MB`
  : bytes < 1e12 ? `${(bytes / 1e9).toFixed(1)} GB`
  : bytes < 1e15 ? `${(bytes / 1e12).toFixed(1)} TB`
  : `${(bytes / 1e15).toFixed(1)} PB`
);

export const stateVectorFrameQubitCounts = [10, 15, 20, 25, 30, 35, 40, 45, 50];

export const linearScaleMaxValue = stateVectorMemoryBytes(30);

export const practiceProblems = getProblemMetaForLesson(
  "quantum-software/programming-quantum-computers/simulators-vs-real-hardware"
);

export const lessonMeta = {
  title: "Simulators vs. Real Hardware",
  description:
    "Writing Your First Circuit isolated pure shot noise; real hardware adds actual physical error (Noise, Decoherence & Scaling) on top, plus queue time and a hard qubit-count ceiling a simulator doesn't have, while an ideal noiseless simulator hits an entirely different wall: exponential memory cost in qubit count.",
  course: "programming-quantum-computers",
  module: "simulators-vs-real-hardware",
  order: 1,
  difficulty: "foundational",
  estimatedMinutes: 20,
  prerequisites: ["quantum-software/programming-quantum-computers/writing-your-first-circuit"],
  objectives: [
    "List the practical differences between running a circuit on a simulator vs. real hardware",
    "Explain why an ideal noiseless simulator's cost grows exponentially with qubit count",
    "Decide, for a given development stage, whether a simulator or real hardware is the more appropriate target",
  ],
};

<LessonHook eyebrow="Two different walls">
A noiseless simulator strips away real hardware's physical error and access constraints — but it hits a very different wall of its own: exponential memory cost in qubit count.
</LessonHook>

## Motivation

Writing Your First Circuit showed shot noise alone, using a perfect,
noiseless simulator. This closing lesson adds back everything a
simulator conveniently strips away, real hardware's actual physical
error, practical access constraints, while also being honest about
where simulators themselves hit a very different wall.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/thumb/9/91/IBM_Quantum_System_One.jpg/1280px-IBM_Quantum_System_One.jpg"
  alt="An IBM Quantum System One, a full commercial quantum computing system in its housing"
  caption="An IBM Quantum System One. Everything this lesson calls 'real hardware' — queue time, a fixed physical qubit count, real physical error — comes from a device like this, not a simulator."
  credit="Photo by OJB Quantum / Wikimedia Commons"
  creditUrl="https://commons.wikimedia.org/wiki/File:IBM_Quantum_System_One.jpg"
  license="CC BY 4.0"
/>

## Engineering Development

### What real hardware adds on top of shot noise

Every physical error source Noise, Decoherence & Scaling covered
(<Term id="t1-t2-coherence-times">T1/T2 decay</Term>, <Term id="crosstalk">crosstalk</Term>, readout error, compounding multi-gate error)
sits on TOP of the pure statistical shot noise Writing Your First
Circuit already demonstrated. A real device's reported counts deviate
from the ideal probabilities by more than shot noise alone predicts; the
excess deviation is a measurable signature of physical error, not
something a noiseless simulator will ever show you.

### Practical constraints unique to real hardware

- **Queue time**: real quantum hardware is a shared, scarce resource;
  jobs often wait in a queue before running, unlike a simulator running
  immediately on local (or cloud classical) compute.
- **Qubit count ceiling**: a specific device has a fixed number of
  physical qubits (Physical Qubit Platforms); a simulator's qubit count
  is limited differently (next section).
- **Connectivity constraints**: real hardware often only supports gates
  between specific qubit pairs (Physical Qubit Platforms' connectivity
  discussion), requiring extra <Term id="qubit-routing-swap-overhead">SWAP gates</Term> (Compilation & Hybrid
  Algorithms) a simulator doesn't need.

### Where simulators hit their own wall

An ideal, noiseless simulator represents a state as a list of $2^n$
complex <Term id="amplitude">amplitudes</Term> (this platform's own `StateVector`), doubling with
every additional qubit. At $n=30$ qubits, that's over a billion
amplitudes; well before $n=50$, the memory requirement exceeds any
existing classical computer's capacity. This is the exact reason exact
state-vector simulation cannot substitute for real hardware at large
qubit counts — a completely different kind of limitation from anything
on the "real hardware" side of this comparison, developed fully in
Simulating Quantum Systems' Computational Cost & Scaling lesson.

<Callout type="note">
**A preview of the exact numbers.** That later lesson computes this
precisely via `stateVectorMemoryBytes`: about 16.8 MB at 20 qubits (a
laptop handles this easily), already 17.2 GB at 30 qubits, and 18.0 PB
at 50 qubits, beyond any existing classical system's storage. The wall
is an exact, computed curve, not a rough estimate.
</Callout>

<PredictBeforeReveal
  question="At n=30 qubits, roughly how much memory does exact state-vector simulation need to store every amplitude?"
  options={[
    { label: "Around 17 MB", value: "mb" },
    { label: "Around 17 GB", value: "gb" },
    { label: "Around 17 TB", value: "tb" },
    { label: "Around 17 PB", value: "pb" },
  ]}
  correctValue="gb"
  explanation="17.2 GB at 30 qubits, already beyond a typical laptop's RAM. The curve climbs from 16.8 MB at n=20 to 18.0 PB at n=50, purely from the amplitude count doubling with every added qubit (2ⁿ). Slide the Explorer below up to n=30 and check its caption against this number."
/>

<InteractiveSection title="Feel the exponential wall yourself" description="Slide n from 10 up to 50 qubits, then toggle between a log10 scale (readable at every n, but smooths the climb into a gentle slope) and a linear scale (unreadable at large n, but the one that actually looks like a wall). Captions always show the real numbers, computed live by this platform's own stateVectorAmplitudeCount and stateVectorMemoryBytes, the exact same functions behind the 16.8 MB, 17.2 GB, and 18.0 PB figures quoted above.">
  <div className="not-prose flex flex-wrap items-center gap-2">
    <input type="radio" id="svh-scale-linear" name="svh-scale" className="peer/linear sr-only" />
    <label
      htmlFor="svh-scale-linear"
      className="cursor-pointer rounded-full border border-border bg-surface px-3 py-1.5 text-xs font-medium text-muted-foreground transition-colors hover:bg-surface-muted peer-checked/linear:border-transparent peer-checked/linear:bg-brand peer-checked/linear:text-brand-foreground peer-focus-visible/linear:outline-none peer-focus-visible/linear:ring-2 peer-focus-visible/linear:ring-brand peer-focus-visible/linear:ring-offset-2 peer-focus-visible/linear:ring-offset-background"
    >
      Linear scale
    </label>
    <input type="radio" id="svh-scale-log" name="svh-scale" defaultChecked className="peer/log sr-only" />
    <label
      htmlFor="svh-scale-log"
      className="cursor-pointer rounded-full border border-border bg-surface px-3 py-1.5 text-xs font-medium text-muted-foreground transition-colors hover:bg-surface-muted peer-checked/log:border-transparent peer-checked/log:bg-brand peer-checked/log:text-brand-foreground peer-focus-visible/log:outline-none peer-focus-visible/log:ring-2 peer-focus-visible/log:ring-brand peer-focus-visible/log:ring-offset-2 peer-focus-visible/log:ring-offset-background"
    >
      Log scale
    </label>
    <div className="hidden w-full peer-checked/linear:block">
      <div className="not-prose overflow-hidden rounded-xl">
        <BarChartExplorer
          sliderLabel="Qubit count (n)"
          ariaLabel="State-vector amplitude count and memory required, on a linear scale fixed to n=30's memory requirement, as qubit count n grows from 10 to 50; bars for n=35 and above run off the top of the chart"
          maxValue={linearScaleMaxValue}
          frames={stateVectorFrameQubitCounts.map((n) => ({
            paramLabel: `n = ${n}`,
            bars: [
              { label: "Amplitudes (2ⁿ)", value: stateVectorAmplitudeCount(n), caption: stateVectorAmplitudeCount(n).toExponential(2) },
              { label: "Memory", value: stateVectorMemoryBytes(n), caption: formatMemoryBytes(stateVectorMemoryBytes(n)), highlight: true },
            ],
          }))}
        />
      </div>
      <p className="mt-2 text-xs text-muted-foreground">
        Bars for n = 35 and above run off the top of this chart, clipped at
        the border, that is the wall, not a rendering bug. The axis is fixed
        to n = 30&apos;s memory requirement (about 17.2 GB) so nothing past
        it fits on screen; fine, fine, fine, then gone.
      </p>
    </div>
    <div className="hidden w-full peer-checked/log:block">
      <BarChartExplorer
        sliderLabel="Qubit count (n)"
        ariaLabel="State-vector amplitude count and memory required, both on a log10 scale, as qubit count n grows from 10 to 50"
        maxValue={Math.log10(stateVectorMemoryBytes(50))}
        frames={stateVectorFrameQubitCounts.map((n) => ({
          paramLabel: `n = ${n}`,
          bars: [
            { label: "Amplitudes (2ⁿ)", value: Math.log10(stateVectorAmplitudeCount(n)), caption: stateVectorAmplitudeCount(n).toExponential(2) },
            { label: "Memory", value: Math.log10(stateVectorMemoryBytes(n)), caption: formatMemoryBytes(stateVectorMemoryBytes(n)), highlight: true },
          ],
        }))}
      />
    </div>
  </div>
</InteractiveSection>

## Physical Interpretation

Simulators and real hardware fail in *opposite* ways: a simulator is
perfectly accurate (no physical error) but limited by classical memory,
scaling exponentially badly with qubit count; real hardware has no such
memory limit (a physical qubit doesn't cost exponentially more per
additional qubit, at least not for this reason) but suffers unavoidable
physical error and limited access. Neither is simply "better." The
right choice depends on the specific development stage and question
being asked.

## Worked Example

**A researcher is debugging a new 4-qubit circuit's logic. Should they
use a simulator or real hardware?**

Simulator: at only 4 qubits ($2^4=16$ amplitudes), exact simulation is
trivially cheap, gives exact (not sampled) results for easy debugging,
runs instantly with no queue, and has zero physical error to confound
whether a bug is in the circuit LOGIC or in hardware noise, exactly
what's needed at this development stage. Real hardware becomes the
right target only later, once the logic is verified and the actual
question shifts to "how does this perform under real physical
conditions."

## Common Mistakes

<Callout type="note">
**Assuming real hardware is always the "more real" or "better" choice.**
For debugging circuit logic specifically, a noiseless simulator is
strictly more useful: it isolates logic bugs from hardware noise
entirely, something real hardware cannot do.
</Callout>

<Callout type="warning">
**Assuming simulators can scale to arbitrarily many qubits just by using
a bigger classical computer.** The $2^n$ memory cost means even a
substantial hardware upgrade buys only a few more qubits of simulation
capacity. Exponential growth overwhelms any realistic linear resource
increase.
</Callout>

## Practice Questions

1. List three practical constraints real hardware has that a local
   simulator does not.
2. How many complex amplitudes does an exact state-vector simulation of
   a 20-qubit circuit require? (Express as a power of 2, and roughly in
   scientific notation.)
3. For the worked example's 4-qubit debugging scenario, explain
   specifically why hardware noise would make it HARDER, not easier, to
   isolate an actual circuit-logic bug.
4. Explain why simulators and real hardware are described as failing in
   "opposite ways," rather than one simply being more limited than the
   other overall.

<PracticeLinks problems={practiceProblems} />

## Summary

- Real hardware adds physical error (on top of pure shot noise)
  plus practical constraints (queue time, fixed qubit count,
  connectivity) that a simulator doesn't face.
- An ideal simulator instead faces exponential memory cost ($2^n$
  amplitudes), a hard wall well before large qubit counts.
- Simulators and hardware fail in opposite ways; the right choice
  depends on development stage: simulators for isolating logic bugs at
  small qubit count, hardware for evaluating real physical performance.

## Further Exploration

<NextDiscovery>
This closes **Programming Quantum Computers**. The next course,
**Simulating Quantum Systems**, develops the exponential-cost problem
this lesson just introduced in full quantitative detail, and covers the
techniques (tensor networks, noise simulation) that push past it.
</NextDiscovery>
