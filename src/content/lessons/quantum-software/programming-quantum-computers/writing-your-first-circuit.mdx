import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemsForLesson } from "@/lib/problems/registry";
import { LazyCircuitBuilder } from "@/components/simulators/circuit-builder/LazyCircuitBuilder";
import { QuantumCircuit, runCircuit, sampleMeasurements } from "@/lib/quantum/circuitBuilder";
import { CircuitDiagramExplorer } from "@/components/visualizations/CircuitDiagramExplorer";

export const practiceProblems = getProblemsForLesson(
  "quantum-software/programming-quantum-computers/writing-your-first-circuit"
).map((problem) => problem.meta);

export const ghzSampleCircuit = new QuantumCircuit(3).h(0).cnot(0, 1).cnot(1, 2);
export const ghzSampleState = runCircuit(ghzSampleCircuit);
export const ghzSampleCounts = sampleMeasurements(ghzSampleState, 5000);
export const ghzCount000 = ghzSampleCounts["000"] || 0;
export const ghzCount111 = ghzSampleCounts["111"] || 0;

export const ghzShotCounts = [100, 500, 1000, 5000, 20000, 100000];
export const ghzFrames = ghzShotCounts.map((shots) => {
  const counts = shots === 5000 ? ghzSampleCounts : sampleMeasurements(ghzSampleState, shots);
  const count000 = counts["000"] || 0;
  const count111 = counts["111"] || 0;
  return {
    paramLabel: `${shots.toLocaleString()} shots`,
    bars: [
      { label: "000", value: count000 / shots, caption: `${count000}` },
      { label: "111", value: count111 / shots, caption: `${count111}`, highlight: true },
    ],
  };
});

export const lessonMeta = {
  title: "Writing Your First Circuit",
  description:
    "A complete, hands-on walkthrough: build a 3-qubit GHZ state circuit, run it exactly, then sample it 5000 times and watch the measured 000/111 split converge toward the exact 50/50 probabilities, the full build-run-sample workflow in one worked example.",
  course: "programming-quantum-computers",
  module: "writing-your-first-circuit",
  order: 1,
  difficulty: "foundational",
  estimatedMinutes: 20,
  prerequisites: ["quantum-software/programming-quantum-computers/quantum-sdks-overview"],
  objectives: [
    "Build a multi-qubit circuit from scratch using QuantumCircuit's chained methods",
    "Distinguish exact simulation (runCircuit) from shot-based sampling (sampleMeasurements)",
    "Explain why real hardware always reports the sampled version, never the exact amplitudes directly",
  ],
};

<LessonHook eyebrow="Build it, run it, sample it">
Every previous lesson discussed the pattern conceptually. This one does it: build a real circuit, run it, and watch the measured counts converge toward the exact probabilities, end to end.
</LessonHook>

## Motivation

Both kinds of output this platform's engine can produce, exact
amplitudes and shot-based samples, appear end to end below.

## Walkthrough

### Step 1: Build the circuit

A **GHZ state** ($\frac1{\sqrt2}(|000\rangle+|111\rangle)$, a 3-qubit
generalization of the Bell state) is built with:

```
const circuit = new QuantumCircuit(3);
circuit.h(0).cnot(0, 1).cnot(1, 2);
```

Nothing has run yet. `circuit.instructions` holds three
`GateInstruction` objects (Circuit Representation in Code), waiting to
be executed. Here's that list taking shape one chained call at a time:

<CircuitDiagramExplorer
  sliderLabel="After calling"
  frames={[
    {
      paramLabel: "circuit.h(0)",
      numQubits: 3,
      instructions: [{ gate: "H", targets: [0] }],
      highlightColumn: 0,
      ariaLabel: "3-qubit circuit with one instruction: H on qubit 0",
    },
    {
      paramLabel: "circuit.h(0).cnot(0, 1)",
      numQubits: 3,
      instructions: [{ gate: "H", targets: [0] }, { gate: "CNOT", targets: [0, 1] }],
      highlightColumn: 1,
      ariaLabel: "3-qubit circuit with two instructions: H on qubit 0, then CNOT from qubit 0 to qubit 1",
    },
    {
      paramLabel: "circuit.h(0).cnot(0, 1).cnot(1, 2)",
      numQubits: 3,
      instructions: [{ gate: "H", targets: [0] }, { gate: "CNOT", targets: [0, 1] }, { gate: "CNOT", targets: [1, 2] }],
      highlightColumn: 2,
      ariaLabel: "3-qubit circuit with three instructions: H on qubit 0, CNOT from qubit 0 to qubit 1, CNOT from qubit 1 to qubit 2",
    },
  ]}
/>

Now build the same three instructions yourself, on the interactive circuit below:

<InteractiveSection title="Build the GHZ circuit yourself" description="Build this exact circuit yourself below (H on q0, then CNOT(0,1), then CNOT(1,2) on 3 qubits) and confirm you get the same two-peak distribution before reading Step 2's exact numbers.">
  <LazyCircuitBuilder />
</InteractiveSection>

### How this looks in Qiskit

As a static comparison only (not run by this platform's own simulator),
here's the exact same 3-qubit GHZ circuit built with real Qiskit syntax,
IBM's SDK, using only its long-stable circuit-construction API:

```python
# Real Qiskit syntax, shown for comparison only.
from qiskit import QuantumCircuit

qc = QuantumCircuit(3)   # same 3 qubits as new QuantumCircuit(3)
qc.h(0)                  # same as circuit.h(0)
qc.cx(0, 1)               # same as circuit.cnot(0, 1)
qc.cx(1, 2)               # same as circuit.cnot(1, 2)
qc.measure_all()          # add measurements, needed before sampling on a backend
```

Same three gates, same qubit indices, same resulting GHZ state
$\frac1{\sqrt2}(|000\rangle+|111\rangle)$; only the method names
(`.cx()` instead of `.cnot()`) and the explicit `measure_all()` call
differ from this platform's own `QuantumCircuit`.

### Step 2: Run it exactly

<PredictBeforeReveal
  question="Before running it, predict: what will state.probabilities() give for this 3-qubit GHZ circuit?"
  options={[
    { label: "0.5 spread evenly across all 8 outcomes", value: "uniform" },
    { label: "1.0 on |000⟩ only", value: "zero-only" },
    { label: "0.5 on |000⟩, 0.5 on |111⟩, 0 everywhere else", value: "ghz" },
  ]}
  correctValue="ghz"
  explanation="H puts qubit 0 into superposition, and each CNOT then copies that superposition onto the next qubit, entangling all three into 1/√2(|000⟩+|111⟩). Only the all-zeros and all-ones outcomes get any probability — 0.5 each — and every other 3-bit string stays at exactly 0."
/>

```
const state = runCircuit(circuit);
```

This platform's engine confirms `state.probabilities()` gives exactly
`[0.5, 0, 0, 0, 0, 0, 0, 0.5]`: probability $0.5$ each for
$|000\rangle$ and $|111\rangle$, zero everywhere else, matching the GHZ
state exactly.

### Step 3: Sample it like real hardware would

```
const counts = sampleMeasurements(state, 5000);
```

Real hardware (and honest simulators) don't hand back exact amplitudes.
They report a finite number of individual measurement shots
(Qubit Readout Techniques). Running this platform's `sampleMeasurements`
with 5000 shots gives counts close to (but not exactly) a 2500/2500
split between "000" and "111", e.g. one representative run gave 2521
and 2479. The deviation from a perfectly even split is ordinary
**statistical (shot) noise**, not a bug. It shrinks (relatively) as
shot count grows, following the standard $1/\sqrt{\text{shots}}$
statistical scaling any repeated-sampling estimate obeys.

Here's one real, live run of exactly that at 5000 shots: the exact
probability is $0.5$ for each outcome by construction; the sampled
fraction is this page's own actual `sampleMeasurements` call, jittering
around 0.5 by whatever this particular run of 5000 shots happened to
land on.

<BarChart
  ariaLabel="Exact GHZ probabilities vs. one real 5000-shot sampled run"
  bars={[
    { label: "000 exact", value: 0.5 },
    { label: "000 sampled", value: ghzCount000 / 5000, highlight: true },
    { label: "111 exact", value: 0.5 },
    { label: "111 sampled", value: ghzCount111 / 5000, highlight: true }
  ]}
  maxValue={0.6}
/>

Practice Question 2 asks whether statistical noise grows or shrinks
with more shots, in absolute count versus as a fraction of shots. A
single shot count can't show that; scrubbing across shot counts can.
Below are this page's own actual `sampleMeasurements` calls (same GHZ
state, same two outcomes) run at 100, 500, 1000, 5000, 20,000, and
100,000 shots. Slide across them and watch the sampled bars visibly
tighten around the exact 0.5 line as shots grow, the $1/\sqrt{\text{shots}}$
scaling made visible: the raw count captioned above each bar swings by
more in absolute terms at high shot counts, even as the fraction it
represents converges.

<BarChartExplorer
  ariaLabel="Sampled GHZ state fractions for 000 and 111 at increasing shot counts, converging toward the exact 0.5/0.5 split"
  sliderLabel="Shots"
  maxValue={0.7}
  frames={ghzFrames}
/>

## Physical Interpretation

The gap between Step 2's exact probabilities and Step 3's sampled counts
*is* the central practical fact of running anything on real quantum
hardware: you never directly observe $|\psi\rangle$'s <Term id="amplitude">amplitudes</Term>, only
statistics accumulated over many runs, always with <Term id="shot-noise-standard-error">finite-shot noise</Term> on
top of whatever hardware error (Noise, Decoherence & Scaling) is also
present.

<HistoricalMoment date="2016">
IBM offers free cloud access to real superconducting quantum processors — a circuit built the same way as this lesson's GHZ circuit can be sent to run on actual hardware, not just a simulator.
</HistoricalMoment>

This isn't a hypothetical. IBM has offered free cloud access to real
superconducting quantum processors since 2016, and cloud platforms like
Amazon Braket now offer similar (generally paid, pay-per-shot) access
to real hardware across multiple vendors: submitting a circuit built the
same way as this lesson's, choosing a shot count, and downloading a
counts histogram that looks exactly like Step 3's, just with real
hardware error mixed into the shot noise on top. The first histogram a beginner
downloads from an actual queued hardware job looks structurally
identical to the bar chart above, only noisier.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/thumb/6/60/IBM_Q_system_%28Fraunhofer_2%29.jpg/1280px-IBM_Q_system_%28Fraunhofer_2%29.jpg"
  alt="IBM Quantum System One, a real superconducting quantum processor in its cryostat housing, in Ehningen, Germany"
  caption="An IBM Quantum System One. Since 2016, IBM has offered free cloud access to real superconducting processors like this one; a circuit built the same way as this lesson's GHZ circuit can be sent to run on actual hardware like it, not just this platform's own simulator."
  credit="IBM Research / Wikimedia Commons"
  creditUrl="https://commons.wikimedia.org/wiki/File:IBM_Q_system_(Fraunhofer_2).jpg"
  license="CC BY 2.0"
/>

## Worked Example

**Using this platform's confirmed test-suite result (5000 shots on a
Bell state, `00`/`11` split), what range of `00` counts is expected 95%
of the time, treating this as a binomial sampling process?**

For $p=0.5$, $n=5000$: standard deviation
$\sigma=\sqrt{np(1-p)}=\sqrt{5000\times0.25}=\sqrt{1250}\approx35.4$.
A 95% range is roughly $\pm2\sigma\approx\pm71$ around the expected
mean of 2500, so counts between about 2429 and 2571 are expected 95% of
the time, purely from statistical shot noise, with a perfect,
noiseless simulator and an exact 50/50 underlying probability.

## Common Mistakes

<Callout type="warning">
**Expecting sampled counts to exactly match the underlying probabilities.**
Even with a perfect, noiseless <Term id="quantum-state">quantum state</Term>, finite-shot sampling
always shows statistical fluctuation. The worked example's $\pm71$-count
range at 5000 shots is real and expected, not evidence of a broken
circuit.
</Callout>

<Callout type="note">
**Confusing shot noise (statistical, present even for a perfect
simulator) with hardware error (physical, from Noise, Decoherence &
Scaling's actual error sources).** Both contribute to real hardware
results; only shot noise is present in this lesson's idealized
`sampleMeasurements` example.
</Callout>

## Practice Questions

1. Write out (conceptually) the three `GateInstruction`s the GHZ
   circuit's `.h(0).cnot(0,1).cnot(1,2)` chain produces.
2. Using the same $\sqrt{np(1-p)}$ formula, estimate the standard
   deviation for 10,000 shots instead of 5000. Does statistical noise
   grow or shrink (in absolute count) with more shots? What about as a
   FRACTION of total shots?
3. Why does real hardware always report sampled counts, never exact
   amplitudes directly?
4. If a sampled experiment gave 4000 "000" counts and 1000 "111" counts
   out of 5000 shots (badly off from the expected 50/50 split), would
   this most likely indicate shot noise alone, or something else?
   Explain your reasoning using the worked example's expected range.

<PracticeLinks problems={practiceProblems} />

## Summary

- Building a circuit (`.h(0).cnot(0,1).cnot(1,2)`) creates instructions
  as data; `runCircuit` executes them, giving exact GHZ-state
  probabilities `[0.5,...,0.5]`.
- `sampleMeasurements` illustrates what real hardware actually reports:
  shot-based counts with real statistical fluctuation around the
  exact probabilities.
- At 5000 shots on a perfect 50/50 state, roughly $\pm71$ counts of
  deviation is expected 95% of the time from shot noise alone.

## Further Exploration

<NextDiscovery>
[Simulators vs. Real Hardware](/lessons/quantum-software/programming-quantum-computers/simulators-vs-real-hardware) adds actual hardware
error on top of this lesson's pure shot noise, completing the picture of
what a real device's output actually looks like.
</NextDiscovery>
