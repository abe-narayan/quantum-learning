import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemMetaForLesson } from "@/lib/problems/metaRegistry";
import { QuantumCircuit, runCircuit } from "@/lib/quantum/circuitBuilder";
import { PAULI_X, PAULI_Z } from "@/lib/quantum/gates";
import { expectationValue } from "@/lib/quantum/observables";
import { exactGroundStateEnergy } from "@/lib/quantum/vqe";
import { CostLandscapeHeatmap } from "@/components/visualizations/CostLandscapeHeatmap";

export const practiceProblems = getProblemMetaForLesson(
  "quantum-software/compilation-and-hybrid-algorithms/variational-algorithm-implementation"
);

export const twoParamHamiltonian = PAULI_Z.scale(0.6).add(PAULI_X.scale(0.8));

export const twoParamExactEnergy = exactGroundStateEnergy(twoParamHamiltonian);

export const costLandscapeGrid = Array.from({ length: 15 }, (_, i) => {
  const theta = (i / 14) * 2 * Math.PI;
  return Array.from({ length: 15 }, (_, j) => {
    const phi = (j / 14) * 2 * Math.PI;
    const state = runCircuit(new QuantumCircuit(1).ry(0, theta).rz(0, phi));
    const value = expectationValue(state, twoParamHamiltonian).re;
    return { theta, phi, value };
  });
});

export const lessonMeta = {
  title: "Variational Algorithm Implementation",
  description:
    "Closing the loop this pillar has been building toward: a VQE ansatz built from this platform's own QuantumCircuit (not Quantum Algorithms II's separate matrix-based ansatzState) finds ⟨Z⟩'s minimum at θ=π, value -1, matching exactGroundStateEnergy(Z) to full precision, then extends to a two-parameter Hamiltonian and converges to within 0.04% of the true ground energy by grid search alone.",
  course: "compilation-and-hybrid-algorithms",
  module: "variational-algorithm-implementation",
  order: 1,
  difficulty: "advanced",
  estimatedMinutes: 20,
  prerequisites: ["quantum-software/compilation-and-hybrid-algorithms/hybrid-workflows"],
  objectives: [
    "Build a VQE ansatz using QuantumCircuit rather than Quantum Algorithms II's separate matrix-based approach",
    "Compute a Hamiltonian expectation value directly from a QuantumCircuit's output state",
    "Run a simple classical optimization loop and verify convergence against exactGroundStateEnergy",
  ],
};

<LessonHook eyebrow="Assembling the pieces">
Every piece needed for this final implementation already exists, scattered across this entire platform — Quantum Algorithms II's VQE physics, Programming Quantum Computers' QuantumCircuit, this course's compilation concepts. This closing lesson assembles them into one working, end-to-end variational algorithm.
</LessonHook>

## Motivation

Quantum Algorithms II already built <Term id="vqe">VQE</Term> once, computing its ansatz
state by direct matrix arithmetic. This closing lesson implements the
same variational loop the "software" way instead: the circuit-as-data
pattern this pillar has developed, built from this platform's own
`QuantumCircuit`. Because both code paths compute the same physics,
every result here can be cross-checked against
`exactGroundStateEnergy`, the same verified answers Quantum
Algorithms II already established.

The Hamiltonians in this lesson are deliberately small — one and two
parameters, ground energies known in closed form — because that is what
makes the implementation checkable. The reason anyone builds this
machinery is the case where no closed form exists: a molecular
Hamiltonian whose ground-state energy is the thing you wanted and cannot
get any other way.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/6/60/FeMoco_cluster.svg"
  alt="Structural diagram of the FeMo-cofactor, the iron-molybdenum-sulfur active-site cluster of the nitrogenase enzyme"
  caption="The FeMo-cofactor of nitrogenase, the most-cited target in the quantum-chemistry resource-estimation literature. A variational algorithm aimed at a molecule like this runs the same loop this lesson builds — prepare an ansatz, measure an expectation value, hand it to a classical optimizer — with a Hamiltonian of thousands of terms in place of 0.6Z + 0.8X, and no exact answer to check against."
  credit="Smokefoot / Wikimedia Commons"
  creditUrl="https://commons.wikimedia.org/wiki/File:FeMoco_cluster.svg"
  license="Public domain"
/>

## Engineering Development

### Building the ansatz with QuantumCircuit

Instead of Quantum Algorithms II's `ansatzState` (a direct matrix
computation), build the identical physical <Term id="ansatz">ansatz</Term> using
`QuantumCircuit`:

```
const circuit = new QuantumCircuit(1);
circuit.ry(0, theta);
const state = runCircuit(circuit);
```

This is the "software" version of the same physics — the
circuit-as-data pattern (Circuit Representation in Code) applied to a
variational algorithm.

### Computing the cost function

For the simplest case, $H=Z$: since $\langle Z\rangle=P(0)-P(1)$
directly from measurement probabilities, no separate matrix-expectation
machinery is even needed.
`state.probabilities()[0] - state.probabilities()[1]` computes
$\langle Z\rangle$ directly from the circuit's own output.

### The optimization loop, and verification

<PredictBeforeReveal
  question="The ansatz is Ry(θ)|0⟩ and the cost function is ⟨Z⟩. Where in θ ∈ [0, 2π] should a grid search find the minimum ⟨Z⟩?"
  options={[
    { label: "θ = 0", value: "0" },
    { label: "θ = π/2", value: "half-pi" },
    { label: "θ = π", value: "pi" },
    { label: "θ = 3π/2", value: "three-half-pi" },
  ]}
  correctValue="pi"
  explanation="Ry(π)|0⟩ = |1⟩ up to global phase, and ⟨Z⟩ on |1⟩ is exactly -1, the smallest value Z's expectation can take on any state. The grid search below confirms this directly, landing exactly on θ=π with ⟨Z⟩=-1, matching exactGroundStateEnergy(PAULI_Z) exactly."
/>

A simple grid search over $\theta\in[0,2\pi]$ (the same
"smallest correct optimizer" choice Quantum Algorithms II's `runVqe`
already used) finds the minimum at $\theta=\pi$, with
$\langle Z\rangle=-1$, matching `exactGroundStateEnergy(PAULI_Z)=-1` to
full precision — the `QuantumCircuit` implementation reproduces Quantum
Algorithms II's matrix-based result through an entirely separate
(software-first) code path. This page's own grid search, run live over
all 41 points, traces out the cost landscape the optimizer is searching:

<ParametricCurve
  ariaLabel="Cost landscape: expectation value of Z vs theta, grid search over one full period"
  frames={[
    {
      paramLabel: "grid search over θ ∈ [0, 2π]",
      series: [
        {
          label: "⟨Z⟩(θ)",
          color: "brand",
          points: Array.from({ length: 41 }, (_, i) => ({
            x: (i / 40) * 2 * Math.PI,
            y: runCircuit(new QuantumCircuit(1).ry(0, (i / 40) * 2 * Math.PI)).probabilities()[0] - runCircuit(new QuantumCircuit(1).ry(0, (i / 40) * 2 * Math.PI)).probabilities()[1]
          }))
        }
      ]
    }
  ]}
  referenceLines={[{ y: -1, label: "exact ground energy" }]}
/>

Watch the curve dip to $-1$ at $\theta=\pi$, touching the dashed
reference line there and nowhere else in the full period. That is what
"grid search finds the exact minimum" looks like as a picture rather
than a single reported number.

### Extending to a nontrivial Hamiltonian

For $H=0.6Z+0.8X$ (exact ground energy $-1$, since
$\sqrt{0.6^2+0.8^2}=1$), a two-parameter ansatz
(`circuit.ry(0,theta).rz(0,phi)`) with $\langle H\rangle$ computed via
direct matrix application to the circuit's output amplitudes,
grid-searched over both $\theta$ and $\phi$, converges to
$\langle H\rangle\approx-0.9996$, within $0.04\%$ of the true $-1$
ground energy, limited only by grid resolution — a finer grid closes the
gap further, as the Variational Method course established for a
different, continuous-wavefunction trial family.

Unlike the single-parameter case, this cost landscape is
two-dimensional: $\langle H\rangle$ is now a function of a $(\theta,\phi)$
plane, not a single curve. This page's own $15\times15$ grid search,
run live over both parameters, traces out that entire plane as a
color-mapped heatmap, with the grid's own minimum marked directly:

<CostLandscapeHeatmap
  ariaLabel="Cost landscape heatmap: expectation value of H=0.6Z+0.8X over a 15 by 15 grid search in theta and phi, with the grid minimum marked"
  grid={costLandscapeGrid}
  exactMin={twoParamExactEnergy}
/>

The darkest-green cell is the grid search's winner: the lowest
$\langle H\rangle$ this particular $15\times15$ grid found, sitting
close to (but, unlike the $H=Z$ case, not exactly on) the true
continuous minimum. That offset is the residual gap described above,
seen as a picture of the whole landscape rather than a single reported
number.

## Physical Interpretation

This is the Quantum Software pillar's thesis made concrete: one physical
algorithm (VQE, already built in Quantum Algorithms II) admits several
software realizations — direct matrix computation, circuit-as-data — and
when both are correct they agree to the last digit. Software structure
is a real engineering choice, and it changes nothing about the physics
underneath.

## Worked Example

**Confirm the QuantumCircuit-based single-parameter result matches
Quantum Algorithms II's exactGroundStateEnergy exactly, and explain why
exact (not approximate) agreement is expected here specifically.**

Grid search over `circuit.ry(0,theta)`'s $\langle Z\rangle$ finds the
minimum at $\theta=\pi$ (a grid point landed exactly on it), giving
$-1$, matching `exactGroundStateEnergy(PAULI_Z)=-1` to the full
precision tested. Exact agreement (not just "close") is expected here
because $H=Z$'s ground state is $|1\rangle$ on the nose, and the
$R_y(\theta)$ ansatz family reaches it at $\theta=\pi$, unlike
the two-parameter $0.6Z+0.8X$ case, where the grid search's finite
resolution leaves a small (but shrinkable) residual gap.

## Common Mistakes

<Callout type="note">
**Assuming a QuantumCircuit-based and a matrix-based VQE implementation
would give different physics.** They compute the identical underlying
quantities. This lesson's exact agreement with `exactGroundStateEnergy`
confirms this directly, not just plausibly.
</Callout>

<Callout type="note">
**Expecting every optimization to converge exactly, the way the
single-parameter H=Z case did.** The two-parameter case's small residual
gap (grid resolution, not a real physics discrepancy) is the more
typical situation. Exact convergence at $\theta=\pi$ for $H=Z$ is a
special, checkable coincidence of that specific problem's structure.
</Callout>

## Practice Questions

1. Write out (conceptually) the QuantumCircuit-based steps to compute
   $\langle Z\rangle$ for a given $\theta$.
2. Why does $H=Z$'s ground state land EXACTLY on a grid point
   ($\theta=\pi$) while $H=0.6Z+0.8X$'s optimal parameters don't land
   as precisely on this lesson's grid?
3. Explain why building the SAME VQE physics through two different code
   paths (direct matrix vs. circuit-as-data) serves as a genuine
   correctness check on both implementations.
4. What would you expect to happen to the two-parameter case's residual
   gap (currently $\approx0.04\%$) if the grid resolution were doubled?

<PracticeLinks problems={practiceProblems} />

## Summary

- A VQE ansatz built with `QuantumCircuit` (`circuit.ry(0,theta)`)
  reproduces Quantum Algorithms II's matrix-based `ansatzState` physics
  exactly, computed through a different software path.
- Grid-search optimization finds $H=Z$'s ground energy EXACTLY
  ($\theta=\pi$, $\langle Z\rangle=-1$), matching
  `exactGroundStateEnergy` precisely.
- A two-parameter extension ($H=0.6Z+0.8X$) converges to within
  $0.04\%$ of the true ground energy, limited only by grid resolution,
  the same variational-method pattern established all the way back in
  Approximation Methods, now implemented end to end in software.

## Further Exploration

<NextDiscovery>
This closes the algorithmic core of **Compilation & Hybrid Algorithms**:
only **Quantum Error Mitigation** remains, closing out this course and,
with it, the entire Quantum Software pillar. Every course from
Mathematical Foundations through this lesson now has lessons, problems,
and, wherever useful, verified engine code connecting them, with the
four graduate-level Quantum Mastery courses still ahead across this
platform's full, 26-course, five-pillar curriculum.
</NextDiscovery>
