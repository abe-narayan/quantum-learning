import { PredictBeforeReveal } from "@/components/mdx/PredictBeforeReveal";
import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemMetaForLesson } from "@/lib/problems/metaRegistry";
import { LazyTwoQubitExplorer } from "@/components/simulators/two-qubit-explorer/LazyTwoQubitExplorer";
import { VectorDiagramExplorer } from "@/components/visualizations/VectorDiagramExplorer";
import { angularMomentumX, angularMomentumY, angularMomentumZ } from "@/lib/quantum/angularMomentum";
import { IDENTITY } from "@/lib/quantum/gates";

export const practiceProblems = getProblemMetaForLesson(
  "quantum-mechanics/angular-momentum-and-spin/addition-of-angular-momentum"
);

export const totalComponent = (single) => single.tensor(IDENTITY).add(IDENTITY.tensor(single));

export const totalJx = totalComponent(angularMomentumX(0.5));

export const totalJy = totalComponent(angularMomentumY(0.5));

export const totalJz = totalComponent(angularMomentumZ(0.5));

export const totalJSquared = totalJx
  .mul(totalJx)
  .add(totalJy.mul(totalJy))
  .add(totalJz.mul(totalJz));

export const additionSpinLen = Math.sqrt(3) / 2;
export const additionTripletLen = Math.sqrt(2);
export const additionHalfAngle = Math.acos(1 / 3) / 2;
export const additionTripletFrame = (m) => {
  const alpha = Math.asin(m / additionTripletLen);
  const j = { x: additionTripletLen * Math.cos(alpha), y: additionTripletLen * Math.sin(alpha) };
  const s1Angle = alpha + additionHalfAngle;
  const s1 = { x: additionSpinLen * Math.cos(s1Angle), y: additionSpinLen * Math.sin(s1Angle) };
  return {
    paramLabel: `Triplet |1,${m}⟩ (m = ${m})`,
    vectors: [
      { label: "S₁", x: s1.x, y: s1.y, color: "brand" },
      { label: "S₂", x: j.x, y: j.y, from: s1, color: "accent" },
      { label: "J = S₁+S₂", x: j.x, y: j.y, color: "foreground", dashed: true }
    ]
  };
};
export const additionSingletFrame = (() => {
  const s1Angle = (95 * Math.PI) / 180;
  const s2Angle = (-80 * Math.PI) / 180;
  const s1 = { x: additionSpinLen * Math.cos(s1Angle), y: additionSpinLen * Math.sin(s1Angle) };
  const s2Delta = { x: additionSpinLen * Math.cos(s2Angle), y: additionSpinLen * Math.sin(s2Angle) };
  const resultant = { x: s1.x + s2Delta.x, y: s1.y + s2Delta.y };
  return {
    paramLabel: "Singlet |0,0⟩ (J = 0)",
    vectors: [
      { label: "S₁", x: s1.x, y: s1.y, color: "brand" },
      { label: "S₂", x: resultant.x, y: resultant.y, from: s1, color: "accent" },
      { label: "J = S₁+S₂", x: resultant.x, y: resultant.y, color: "foreground", dashed: true }
    ]
  };
})();
export const additionFrames = [
  additionTripletFrame(1),
  additionTripletFrame(0),
  additionTripletFrame(-1),
  additionSingletFrame
];

export const lessonMeta = {
  title: "Addition of Angular Momentum",
  description:
    "Two spin-1/2 particles combine into a spin-0 singlet and a spin-1 triplet, and the singlet turns out to be exactly the Bell state |Ψ⁻⟩, verified directly as a J²=0 eigenstate using this platform's own total-angular-momentum operator.",
  course: "angular-momentum-and-spin",
  module: "addition-of-angular-momentum",
  order: 1,
  difficulty: "advanced",
  estimatedMinutes: 35,
  prerequisites: ["quantum-mechanics/angular-momentum-and-spin/the-stern-gerlach-experiment"],
  objectives: [
    "Construct the total angular momentum operator for two spin-1/2 particles from the individual spins",
    "Verify the singlet and triplet states as exact J² and Jz eigenstates",
    "Identify the singlet exactly as the Bell state |Ψ⁻⟩ from Quantum Gates & Circuits",
  ],
  related: [
    {
      slug: "quantum-computing/quantum-gates-and-circuits/bell-states-and-entanglement",
      note: "Builds |Ψ⁻⟩ from a Hadamard and a CNOT. That construction and this lesson's spin-0 singlet produce the same vector, which this lesson verifies by checking it is a J²=0 eigenstate.",
    },
  ],
};

<LessonHook eyebrow="Why this matters">
  Combine two spin-1/2 particles and one particular combination turns out
  to be a state this platform already built for an entirely different
  reason: a Bell state.
</LessonHook>

## Motivation

Two spin-$\tfrac12$ particles do not combine into one well-defined total
spin. Depending on which combined state they occupy, the pair behaves as
spin $1$ or as spin $0$, and "spin $0$" for a pair of spinning things is
strange enough on its own to be worth the derivation.

This lesson works out that decomposition, then finds that one of the four
resulting states was already built on this platform, in a different
course, for reasons that had nothing to do with atoms.

Before deriving anything, guess: does combining two spin-$\tfrac12$
particles always give total spin $1$ (the "obvious" $\tfrac12+\tfrac12$
answer), or can it come out differently depending on how the two spins
are combined?

<PredictBeforeReveal
  question="Two spin-1/2 particles are combined. Is the total spin always exactly 1?"
  options={[
    { label: "Yes, spins just add: 1/2 + 1/2 = 1, always", value: "always-1" },
    { label: "No, it depends on the specific combined state: total spin can be 0 or 1", value: "depends" },
    { label: "No, the total spin is always 0", value: "always-0" },
  ]}
  correctValue="depends"
  explanation="Spin does not add like ordinary numbers. The combined space is four-dimensional, and it splits into three states of total spin 1 (the triplet) plus one of total spin 0 (the singlet). Which you have depends on which of the four states the pair occupies, and the count 3+1=4 is the first hint that the answer had to be more than a single number."
/>

## Mathematical Development

### The total angular momentum operator

For two spin-$\tfrac12$ particles, the total spin operators act on the
combined $4$-dimensional space via
$J_x=S_x^{(1)}\otimes I+I\otimes S_x^{(2)}$ (and similarly for $J_y,J_z$),
each particle's own spin operator, tensored with the identity on the
other particle, then summed. This is the standard way a single-particle
operator extends to a multi-particle system, already used throughout
Quantum Gates & Circuits.

Here is why a change of basis is unavoidable. Below is $\vec J^{\,2}$
written in the computational basis $|{\uparrow}{\uparrow}\rangle,
|{\uparrow}{\downarrow}\rangle,|{\downarrow}{\uparrow}\rangle,
|{\downarrow}{\downarrow}\rangle$, built from the operators just defined
with $\hbar=1$:

<MatrixGrid
  matrix={totalJSquared}
  label="J² in the computational basis (ħ = 1)"
  highlightDiagonal
  ariaLabel="A four by four matrix. The first and last diagonal entries are 2, the middle two diagonal entries are 1, and there are two off-diagonal entries of 1 connecting the second and third basis states. All other entries are zero."
/>

The outer states $|{\uparrow}{\uparrow}\rangle$ and
$|{\downarrow}{\downarrow}\rangle$ sit on their own with eigenvalue $2$.
The middle two do not: the off-diagonal $1$s couple
$|{\uparrow}{\downarrow}\rangle$ to $|{\downarrow}{\uparrow}\rangle$, so
neither has a definite total spin on its own. That $2\times2$ block
$\begin{pmatrix}1&1\\1&1\end{pmatrix}$ has eigenvalues $2$ and $0$, and
diagonalizing it means taking the sum and the difference. The four states
listed next are what comes out.

### Four candidate combined states

Using the <Term id="computational-basis">computational basis</Term> $|{\uparrow}{\uparrow}\rangle,
|{\uparrow}{\downarrow}\rangle,|{\downarrow}{\uparrow}\rangle,
|{\downarrow}{\downarrow}\rangle$ (this platform's usual 2-qubit
ordering), consider:

$$
|1,1\rangle=|{\uparrow}{\uparrow}\rangle,\quad
|1,0\rangle=\frac{|{\uparrow}{\downarrow}\rangle+|{\downarrow}{\uparrow}\rangle}{\sqrt2},\quad
|1,-1\rangle=|{\downarrow}{\downarrow}\rangle,\quad
|0,0\rangle=\frac{|{\uparrow}{\downarrow}\rangle-|{\downarrow}{\uparrow}\rangle}{\sqrt2}
$$

### Verified as exact eigenstates

Applying $\vec J^{\,2}$ and $J_z$, built as above from this platform's
$S_x,S_y,S_z$ at $j=\tfrac12$, to each of the four states gives, to
floating-point precision:

| State | $\vec J^{\,2}$ eigenvalue | $J_z$ eigenvalue | Label |
|---|---|---|---|
| $|1,1\rangle$ | $2\hbar^2$ | $\hbar$ | Triplet, $m=1$ |
| $|1,0\rangle$ | $2\hbar^2$ | $0$ | Triplet, $m=0$ |
| $|1,-1\rangle$ | $2\hbar^2$ | $-\hbar$ | Triplet, $m=-1$ |
| $|0,0\rangle$ | $0$ | $0$ | Singlet |

$2\hbar^2=j(j+1)\hbar^2$ at $j=1$, and $0=j(j+1)\hbar^2$ at $j=0$: both
agree with Lesson 2's formula, and both were computed rather than
inferred from symmetry.

Count dimensions as a check. The combined space is $2\times2=4$
dimensional, and the triplet ($2j+1=3$ states) plus the singlet ($1$
state) accounts for all four. No state is left over and none is
counted twice.

### The classical vector-model picture

Textbooks almost always accompany that table with a picture: draw the
two spin-$\tfrac12$ vectors, each of fixed length $\sqrt3/2\,\hbar$
($\sqrt{s(s+1)}\hbar$ at $s=\tfrac12$), tip to tail, and look at the
resultant $\vec J=\vec S_1+\vec S_2$. For the triplet, $\vec J$ keeps a
fixed length $\sqrt2\,\hbar$ ($\sqrt{j(j+1)}\hbar$ at $j=1$) while its
vertical component runs through $+\hbar,0,-\hbar$ across the three
$m$ values, matching the $J_z$ column above exactly. For the singlet
the two vectors point opposite ways and cancel, matching $\vec J=0$:

<InteractiveSection
  title="Scrub through the four states"
  description="Scrub through the four states. Each spin has fixed length √3/2 ħ, drawn tip to tail (S₁ solid brand, S₂ solid accent); the dashed resultant J = S₁+S₂ is the classical cartoon of total spin. Across the three triplet states J keeps a fixed length √2 ħ while its vertical (z) component runs through +1, 0, -1 in units of ħ, matching the Jz column of the table above. For the singlet the two vectors are drawn nearly antiparallel (offset by a few degrees so both arrows stay visible) and cancel to a point at the origin, matching J=0 exactly. Caveat: this vector-cone picture is the standard textbook cartoon, not literal physics. S1 and S2 don't have simultaneously definite components along every axis as quantum operators, so no real spin vector actually points in one fixed direction like this.">
  <VectorDiagramExplorer
    frames={additionFrames}
    sliderLabel="State"
    ariaLabel="Classical vector-model cartoon of two spin-1/2 vectors of length root-3-over-2 combining tip to tail into a resultant J: across the three triplet states J has fixed length root-2 with vertical component +1, 0, or -1, while for the singlet the two vectors are drawn nearly antiparallel and cancel to a point at the origin"
  />
</InteractiveSection>

The picture earns its place for intuition and should not be trusted
further than that. $\vec S_1$ and $\vec S_2$ are operators, not arrows
with definite directions, and no measurement finds a spin pointing along
the drawn line. What the diagram does encode correctly is each vector's
length and $z$-projection, and those were verified from the operators in
the table above.

### The singlet is a Bell state

Compare $|0,0\rangle=\frac{1}{\sqrt2}(|{\uparrow}{\downarrow}\rangle-|{\downarrow}{\uparrow}\rangle)$
directly to Quantum Gates & Circuits' Bell state
$|\Psi^-\rangle=\frac{1}{\sqrt2}(|01\rangle-|10\rangle)$. Under
$|{\uparrow}\rangle\leftrightarrow|0\rangle$ and
$|{\downarrow}\rangle\leftrightarrow|1\rangle$ they agree term for term.

These are the same vector. The maximally entangled Bell state was reached
by asking what a Hadamard and a CNOT do to $|00\rangle$; the singlet was
reached by asking which two-spin states have zero total angular momentum.
The two questions have no obvious relationship, and they have one answer.
(The triplet's $m=0$ state, $|1,0\rangle$, is $|\Psi^+\rangle$ in the same
way.)

Checking this yourself in the same explorer used to build Bell states in
the first place makes the identity concrete rather than just asserted.
The explorer's one-click "Bell" preset lands on $|\Phi^+\rangle$, not
$|\Psi^-\rangle$, so reaching $|\Psi^-\rangle$ takes one extra step:
after the Bell preset, target qubit q1 and apply X, then Z. That turns
$(|00\rangle+|11\rangle)/\sqrt2$ into $-(|01\rangle-|10\rangle)/\sqrt2$,
the same $|\Psi^-\rangle$ up to an unobservable <Term id="global-relative-phase">global phase</Term>:

<InteractiveSection
  title="Prepare |Ψ⁻⟩ and find the singlet hiding inside"
  description="Click the Bell preset, then target q1 and apply X followed by Z. Read the correlation table: the same amplitudes as the spin-0 singlet above, and the same perfect anti-correlation whichever basis you look in.">
  <LazyTwoQubitExplorer />
</InteractiveSection>

## Physical Interpretation

The agreement is not a coincidence of notation, and there is a physical
reason for it. A system with total angular momentum zero has no preferred
direction available to it: rotate the whole state however you like and it
comes back unchanged, since there is no vector in it to rotate.

A state with no preferred direction cannot favour any outcome for a
single-particle measurement along any axis, so each particle alone reads
out at $50/50$. But the total must stay zero, so whatever one particle
gives, the other must cancel. Perfectly random individually, perfectly
anti-correlated jointly, in every basis.

That is precisely the property Bell States and Entanglement established
for $|\Phi^+\rangle$ by pushing Hadamards through a circuit. Here it
follows from a symmetry statement instead: zero angular momentum means no
directional information, and no directional information means no basis
can be the special one.

## Worked Example

**Verify $|1,0\rangle$'s $J_z$ eigenvalue is exactly $0$, directly from
the individual-particle operators.**

$J_z|1,0\rangle = \left(S_z^{(1)}\otimes I+I\otimes S_z^{(2)}\right)\frac{|{\uparrow}{\downarrow}\rangle+|{\downarrow}{\uparrow}\rangle}{\sqrt2}$.
On $|{\uparrow}{\downarrow}\rangle$: $S_z^{(1)}$ gives $+\tfrac\hbar2$,
$S_z^{(2)}$ gives $-\tfrac\hbar2$, summing to $0$. On
$|{\downarrow}{\uparrow}\rangle$: $-\tfrac\hbar2+\tfrac\hbar2=0$ as well.
Both terms are annihilated, so the superposition is too, and the table's
$J_z=0$ entry is confirmed.

Note that this calculation cannot distinguish $|1,0\rangle$ from
$|0,0\rangle$: both have $J_z=0$, and the same argument applies to the
minus combination verbatim. Separating them requires $\vec J^{\,2}$,
which is precisely the operator whose off-diagonal block forced the two
states to mix in the first place.

## Common Mistakes

<Callout type="warning">
**Assuming any two-spin-$\tfrac12$ state has a well-defined total spin.**
Only the four states above, and their linear combinations *within* one
$j$, do. The plainest counterexample is a bare product state:
$|{\uparrow}{\downarrow}\rangle$ has a perfectly definite $J_z$ but is
*not* a $\vec J^{\,2}$ eigenstate at all: the off-diagonal $1$s in the
matrix above send it to
$|{\uparrow}{\downarrow}\rangle+|{\downarrow}{\uparrow}\rangle$, which is
not a multiple of what went in. Contrast
$|{\uparrow}{\uparrow}\rangle+|{\downarrow}{\downarrow}\rangle$, which
*is* one: it combines $|1,1\rangle$ and $|1,-1\rangle$, two members of the
same $j=1$ multiplet, so it inherits their shared eigenvalue $2\hbar^2$
while having no definite $m$.
</Callout>

<Callout type="note">
**Treating the Bell-state/singlet connection as a resemblance between two
states.** Under the basis correspondence they are one vector with two
names, and the $\vec J^{\,2}=0$ check above is performed on that vector.
</Callout>

## Practice Questions

1. Verify $|1,1\rangle=|{\uparrow}{\uparrow}\rangle$'s $J_z$ eigenvalue
   is exactly $\hbar$, directly from the individual operators (no
   superposition to track, unlike the worked example).
2. Confirm $|1,-1\rangle$ and $|0,0\rangle$ from this lesson correspond
   exactly to which two of the four Bell states from Quantum Gates &
   Circuits (Hint: $|1,-1\rangle=|{\downarrow}{\downarrow}\rangle$ is a
   product state, not entangled at all).
3. Using the antisymmetry of $|0,0\rangle=\frac1{\sqrt2}(|{\uparrow}{\downarrow}\rangle-|{\downarrow}{\uparrow}\rangle)$
   under swapping the two particles, explain in one sentence why swapping
   which particle is "1" and which is "2" leaves the *physical* state
   unchanged (up to an overall sign, which doesn't affect any
   measurement).
4. Why does a spin-zero (singlet) two-particle system necessarily give
   perfectly random single-particle measurement outcomes along *any*
   axis, not just $z$? Connect this to $\vec J^{\,2}=0$ meaning zero
   total angular momentum in every direction simultaneously.

<PracticeLinks problems={practiceProblems} />

## Summary

- Two spin-$\tfrac12$ particles combine into a spin-$1$ triplet (3 states)
  and a spin-$0$ singlet (1 state), verified as exact
  $\vec J^{\,2}$/$J_z$ eigenstates, not assumed by symmetry.
- The singlet $|0,0\rangle$ *is* the Bell state $|\Psi^-\rangle$, and the
  triplet's $m=0$ state is $|\Psi^+\rangle$: the same vectors, reached
  from atomic physics and from circuit construction independently.
- Total spin zero forces perfectly random single-particle outcomes along
  any measurement axis, with perfect correlation between the two
  particles, the physical content behind Bell-state entanglement,
  arrived at from a completely different starting point.

## Further Exploration

<ChallengePrompt prompt="Physical Interpretation states, in passing, that the triplet's m=0 state |1,0⟩ is identical to the Bell state |Ψ+⟩ from Quantum Gates & Circuits. Verify this directly: write out |Ψ+⟩'s definition and compare coefficients against |1,0⟩ term by term.">
  Same coefficient-matching move as the singlet/|Ψ⁻⟩ identification
  above, applied to the one correspondence this lesson only asserts.
</ChallengePrompt>

<NextDiscovery>
  Next, the capstone: [From Abstract Algebra to the Hydrogen Atom](/lessons/quantum-mechanics/angular-momentum-and-spin/capstone-from-abstract-algebra-to-the-hydrogen-atom),
  which previews how this course's $\vec L^{\,2}$, $L_z$, and spherical
  harmonics become the angular part of a real, solvable three-dimensional
  atom.

  This lesson's two-spin-$\tfrac12$ case is the smallest possible example of
  a much more general tool: Quantum Mastery's
  [Clebsch-Gordan Coefficients and the Wigner-Eckart Theorem](/lessons/quantum-mastery/symmetry-scattering-and-semiclassical-methods/clebsch-gordan-coefficients-and-the-wigner-eckart-theorem)
  derives the coupling coefficients for arbitrary $j_1\otimes j_2$, then
  uses them to read off atomic selection rules from symmetry alone.
</NextDiscovery>
