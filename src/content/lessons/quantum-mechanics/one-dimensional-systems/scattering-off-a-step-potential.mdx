import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemsForLesson } from "@/lib/problems/registry";

export const practiceProblems = getProblemsForLesson(
  "quantum-mechanics/one-dimensional-systems/scattering-off-a-step-potential"
).map((problem) => problem.meta);

export const stepHeight = 4;
export const stepK1 = (ratio) => Math.sqrt(2 * ratio * stepHeight);
export const stepK2 = (ratio) => Math.sqrt(2 * (ratio * stepHeight - stepHeight));
export const stepReflection = (ratio) => ((stepK1(ratio) - stepK2(ratio)) / (stepK1(ratio) + stepK2(ratio))) ** 2;

export const stepR = (ratio) => (stepK1(ratio) - stepK2(ratio)) / (stepK1(ratio) + stepK2(ratio));
export const stepT = (ratio) => (2 * stepK1(ratio)) / (stepK1(ratio) + stepK2(ratio));
export const stepPotentialAt = (x) => (x < 0 ? 0 : stepHeight);
export const stepDensityAt = (x, ratio) =>
  x < 0 ? 1 + stepR(ratio) ** 2 + 2 * stepR(ratio) * Math.cos(2 * stepK1(ratio) * x) : stepT(ratio) ** 2;

export const stepWaveXValues = Array.from({ length: 221 }, (_, i) => -4 + (i / 220) * 5.5);
export const stepWaveRatios = [1.08, 1.2, 1.5, 2, 3, 4.5, 6];
export const stepWaveFrames = stepWaveRatios.map((ratio) => ({
  paramLabel: `E/V0 = ${ratio.toFixed(2)}`,
  xValues: stepWaveXValues,
  potential: stepWaveXValues.map(stepPotentialAt),
  density: stepWaveXValues.map((x) => stepDensityAt(x, ratio)),
  markers: [{ x: 0, label: "x = 0" }],
}));

export const lessonMeta = {
  title: "Scattering Off a Step Potential",
  description:
    "A particle with more than enough energy to classically cross a step still partially reflects, quantum mechanically -- derived in closed form from plane-wave boundary matching, with probability conservation checked algebraically.",
  course: "one-dimensional-systems",
  module: "scattering-off-a-step-potential",
  order: 1,
  difficulty: "intermediate",
  estimatedMinutes: 22,
  prerequisites: ["quantum-mechanics/wave-mechanics/free-particle-wave-packets"],
  objectives: [
    "Set up and solve the boundary-matching equations for a particle scattering off a potential step with E > step height",
    "Derive closed-form reflection and transmission probabilities and verify R + T = 1 algebraically",
    "Explain why quantum scattering off a step differs qualitatively from the classical prediction",
  ],
};

## Motivation

A ball rolling toward a downhill (or gentle uphill, as long as it has
enough energy) step always makes it across, classically, with
certainty. A quantum particle, remarkably, does not: some of its
probability always reflects, no matter how much energy it has above the
step. This lesson derives exactly how much, from nothing but plane waves
and boundary matching. No numerics are needed this time; it's a fully
closed-form result.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/5/5a/First_STM.jpg"
  alt="The first scanning tunneling microscope, built at IBM Zurich in 1981, an instrument whose atomic-resolution imaging depends on exactly this kind of wavefunction behavior at a potential discontinuity"
  caption="The scanning tunneling microscope (IBM Zurich, 1981; Nobel Prize 1986) resolves individual atoms by exploiting how sharply a matter wave's behavior changes across a potential discontinuity -- the same boundary physics this lesson derives for a simple step."
  credit="J Brew / Wikimedia Commons"
  creditUrl="https://commons.wikimedia.org/wiki/File:First_STM.jpg"
  license="CC BY-SA 2.0"
/>

## Intuition

The Free-Particle Wave Packets lesson built normalizable wave packets
from superpositions of plane waves; scattering problems instead work
directly with individual plane waves (idealized, not normalizable on
their own, but the standard and cleanest tool for describing a steady
beam of particles). A step in $V(x)$ changes the particle's wavenumber
abruptly, and just like light partially reflecting at any interface
where its speed changes (a glass-air boundary, for instance), a matter
wave partially reflects wherever its wavenumber changes discontinuously.

## Mathematical Development

### Setting up the two regions

Let $V(x)=0$ for $x<0$ and $V(x)=V_0>0$ for $x\ge0$, with a particle of
energy $E>V_0$ incident from the left (over-the-step scattering: the
particle has enough energy to classically continue past $x=0$).
Define $k_1=\sqrt{2E}$ (region 1) and $k_2=\sqrt{2(E-V_0)}$ (region 2, a
real, smaller wavenumber since $E>V_0$). The general stationary-state
solutions:

$$
\psi_1(x) = e^{ik_1x} + r\,e^{-ik_1x} \quad(x<0), \qquad \psi_2(x) = t\,e^{ik_2x} \quad(x\ge0)
$$

Here $e^{ik_1x}$ is an incident wave (amplitude 1, normalized this way by
convention), $r\,e^{-ik_1x}$ a reflected wave (amplitude $r$), and
$t\,e^{ik_2x}$ a transmitted wave (amplitude $t$). There's no left-moving
wave in region 2, since nothing is incident from the right.

### Matching at $x=0$

Continuity of $\psi$ and $\psi'$ at $x=0$:

$$
1+r = t \qquad\text{(continuity of $\psi$)}
$$

$$
ik_1(1-r) = ik_2t \;\Longrightarrow\; k_1(1-r)=k_2t \qquad\text{(continuity of $\psi'$)}
$$

Substituting $t=1+r$ into the second equation:

$$
k_1-k_1r = k_2(1+r) = k_2+k_2r \;\Longrightarrow\; k_1-k_2 = r(k_1+k_2)
$$

$$
\boxed{\;r = \frac{k_1-k_2}{k_1+k_2}, \qquad t = \frac{2k_1}{k_1+k_2}\;}
$$

### Reflection and transmission probabilities

The reflection probability is simply $R=|r|^2$. The transmission
probability needs an extra factor of $k_2/k_1$, accounting for the
different *probability flux*: a wave with smaller wavenumber carries
probability more slowly, so equal amplitude doesn't mean equal flux.

$$
R = \left(\frac{k_1-k_2}{k_1+k_2}\right)^{\!2}, \qquad T = \frac{k_2}{k_1}|t|^2 = \frac{4k_1k_2}{(k_1+k_2)^2}
$$

**Verify $R+T=1$ directly** (probability conservation, the same
requirement checked throughout Wave Mechanics):

$$
R+T = \frac{(k_1-k_2)^2+4k_1k_2}{(k_1+k_2)^2} = \frac{k_1^2+2k_1k_2+k_2^2}{(k_1+k_2)^2} = \frac{(k_1+k_2)^2}{(k_1+k_2)^2} = 1 \;\checkmark
$$

using $(k_1-k_2)^2+4k_1k_2=k_1^2-2k_1k_2+k_2^2+4k_1k_2=k_1^2+2k_1k_2+k_2^2=(k_1+k_2)^2$,
a genuine algebraic identity, not an assumption.

Plotted against the energy ratio $E/V_0$, the shape of this falloff is
worth seeing directly:

<ParametricCurve
  frames={[
    {
      paramLabel: `V0 = ${stepHeight}`,
      series: [
        {
          label: "R = r²",
          color: "brand",
          points: Array.from({ length: 100 }, (_, i) => ({
            x: 1.05 + (i / 99) * (6 - 1.05),
            y: stepReflection(1.05 + (i / 99) * (6 - 1.05))
          }))
        }
      ]
    }
  ]}
  sliderLabel="Step height"
  ariaLabel="Reflection probability R versus energy ratio E over V0 for a fixed step height, showing R falling toward but never reaching zero"
/>

$R$ drops fast as $E/V_0$ grows past 1, but the curve only approaches
the axis asymptotically. It never actually touches $R=0$ at any finite
energy, the visual form of the $R>0$ claim the next section makes
explicit.

That curve is the *aggregate* reflection probability; the actual
mechanism producing it is a standing-wave ripple in $|\psi(x)|^2$ for
$x<0$, from $\psi_1=e^{ik_1x}+r\,e^{-ik_1x}$ interfering with itself,
meeting a flat, uniform $|t|^2$ for $x\ge0$ where only the single
transmitted wave $t\,e^{ik_2x}$ survives:

<ScatteringStandingWave
  frames={stepWaveFrames}
  sliderLabel="Energy ratio"
  ariaLabel="Probability density |psi(x)|^2 overlaid on the step potential: a rippled standing-wave interference pattern for x less than 0 from the incident and reflected waves, meeting a flat uniform density for x greater than 0 from the transmitted wave, as the energy ratio E over V0 is scanned"
/>

Near $E/V_0=1$ the ripple amplitude is largest (large $r$, strong
interference); as $E/V_0$ grows the ripples shrink toward the flat
$|t|^2\to1$ line, the spatial picture of $R\to0$ only asymptotically.

## Physical Interpretation

$R=0$ only in the trivial case $k_1=k_2$ (no step at all, $V_0=0$).
Otherwise $R>0$ always: however small the step, however large the
particle's energy above it, *some* reflection survives. This is exactly
analogous to a light wave partially reflecting at any interface between
media with different refractive indices, however similar those indices
are. It's the same physics, a wave equation with a discontinuous
coefficient, producing the same qualitative behavior.

## Worked Example

**For $E=10$, $V_0=4$ (natural units), find $R$ and $T$.**

$$
k_1=\sqrt{20}\approx4.4721, \qquad k_2=\sqrt{12}\approx3.4641
$$

$$
R = \left(\frac{4.4721-3.4641}{4.4721+3.4641}\right)^{\!2} \approx (0.1270)^2 \approx 0.01613
$$

$$
T = 1-R \approx 0.98387
$$

So even with the particle's energy $2.5\times$ the step height, about
$1.6\%$ of an incident beam still reflects: small, but never zero, and
exactly what this platform's `stepPotentialScattering` function computes
and its test suite confirms sums to exactly 1 with $R$.

## Common Mistakes

<Callout type="mistake">
**Forgetting the $k_2/k_1$ factor in $T$.** Probability flux, not
amplitude-squared alone, is what must be conserved. $|t|^2$ alone
doesn't equal the transmission probability unless $k_1=k_2$.
</Callout>

<Callout type="mistake">
**Expecting classical behavior to reappear for large $E$.** $R\to0$ only
in the limit $E\to\infty$ (where $k_2\to k_1$), never exactly at any
finite energy. The reflection is genuinely persistent, not a
small-energy artifact.
</Callout>

## Practice Questions

1. For $E=8$, $V_0=2$, compute $k_1,k_2,R,T$ and confirm $R+T=1$.
2. Show that $R\to0$ as $V_0\to0$ directly from the boxed formula (not by
   re-deriving from scratch).
3. Explain briefly why $t=2k_1/(k_1+k_2)$ can be *greater* than 1
   (amplitude, not probability) without violating anything: what
   physical quantity is actually guaranteed to be $\le1$?

<PracticeLinks problems={practiceProblems} />

## Summary

- Matching plane waves at a step potential gives closed-form
  $r=(k_1-k_2)/(k_1+k_2)$, $t=2k_1/(k_1+k_2)$.
- $R=r^2$ and $T=(k_2/k_1)t^2$ satisfy $R+T=1$ exactly, an algebraic
  identity.
- Quantum particles partially reflect off *any* potential step, however
  much energy they have above it: a genuine, persistent departure from
  classical mechanics.

## Further Exploration

Next: [Resonant Transmission Through a Barrier](/lessons/quantum-mechanics/one-dimensional-systems/resonant-transmission-through-a-barrier), extending this
two-region result to a full barrier, with a striking consequence:
special energies where a barrier becomes perfectly, exactly transparent.
