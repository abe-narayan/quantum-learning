import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemMetaForLesson } from "@/lib/problems/metaRegistry";
import { Complex } from "@/lib/quantum/complex";
import { symmetrize, antisymmetrize, exchangeParticles } from "@/lib/quantum/identicalParticles";
import { vectorToExchangeGrid } from "@/components/visualizations/ExchangeDiagram";
import { ExchangeDiagramExplorer } from "@/components/visualizations/ExchangeDiagramExplorer";

export const practiceProblems = getProblemMetaForLesson(
  "quantum-mechanics/identical-particles/bosons-and-fermions"
);

export const basisDim = 3;
export const e0 = [new Complex(1), new Complex(0), new Complex(0)];
export const e1 = [new Complex(0), new Complex(1), new Complex(0)];
export const psiS = symmetrize(e0, e1);
export const psiA = antisymmetrize(e0, e1);
export const eigenstatePresets = [
  {
    label: "Symmetric (bosons)",
    before: vectorToExchangeGrid(psiS, basisDim),
    after: vectorToExchangeGrid(exchangeParticles(psiS, basisDim, basisDim), basisDim),
    beforeLabel: "ψ_S = symmetrize(e0, e1)",
    afterLabel: "P₁₂ψ_S",
    basisLabels: ["0", "1", "2"],
    verdict: "symmetric"
  },
  {
    label: "Antisymmetric (fermions)",
    before: vectorToExchangeGrid(psiA, basisDim),
    after: vectorToExchangeGrid(exchangeParticles(psiA, basisDim, basisDim), basisDim),
    beforeLabel: "ψ_A = antisymmetrize(e0, e1)",
    afterLabel: "P₁₂ψ_A",
    basisLabels: ["0", "1", "2"],
    verdict: "antisymmetric"
  }
];

export const lessonMeta = {
  title: "Bosons & Fermions",
  description:
    "symmetrize(a,b) and antisymmetrize(a,b) build the actual +1 and -1 exchange eigenstates from any two single-particle states, checked here against exchangeParticles rather than taken on faith.",
  course: "identical-particles",
  module: "bosons-and-fermions",
  order: 1,
  difficulty: "advanced",
  estimatedMinutes: 25,
  prerequisites: ["quantum-mechanics/identical-particles/indistinguishability"],
  objectives: [
    "Construct the normalized symmetric and antisymmetric combinations of two single-particle states",
    "Verify numerically that these combinations are genuine +1 and -1 eigenstates of the exchange operator",
    "State the spin-statistics connection (integer spin = boson, half-integer spin = fermion) as an experimental fact this course doesn't derive",
  ],
};

<LessonHook eyebrow="Why this matters">
Every particle in the universe belongs to one of exactly two families — and which family decides whether it can pile into a laser beam or a superfluid with a billion identical companions, or whether it insists on its own private quantum state the way electrons do.
</LessonHook>

## Motivation

The
previous lesson showed a bare product state isn't a valid
two-identical-particle state at all. Here we build the two combinations
that are: the symmetric sum (bosons) and the antisymmetric difference
(fermions), and check both directly against the exchange operator
rather than trusting the algebra that built them.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/5/5a/Liquid_helium_lambda_point_transition.jpg"
  alt="Photograph of liquid helium violently boiling, then suddenly falling still as it crosses the lambda point into its superfluid phase"
  caption="Liquid helium-4, cooled through the 2.2 K lambda point: the violent boiling abruptly stops as the liquid transitions to a superfluid. This is a macroscopic, directly photographed consequence of helium-4 atoms being bosons: below the transition, a large fraction condense into the very same symmetric quantum state this lesson constructs algebraically as |ψ_S⟩."
  credit="Alfred Leitner"
  creditUrl="https://commons.wikimedia.org/wiki/File:Liquid_helium_lambda_point_transition.jpg"
  license="Public domain"
/>

## Mathematical Development

### Symmetric combination: bosons

$$
|\psi_S\rangle = \mathcal N\big(|a\rangle\otimes|b\rangle + |b\rangle\otimes|a\rangle\big)
$$

with $\mathcal N$ a <Term id="normalization">normalization</Term> constant. Applying $P_{12}$ swaps the
two terms, which simply reproduces the same sum: $P_{12}|\psi_S\rangle=+|\psi_S\rangle$,
a genuine $+1$ <Term id="eigenvalue-eigenvector">eigenstate</Term>, for *any* choice of $a,b$, including $a=b$.

### Antisymmetric combination: fermions

$$
|\psi_A\rangle = \mathcal N\big(|a\rangle\otimes|b\rangle - |b\rangle\otimes|a\rangle\big)
$$

Applying $P_{12}$ swaps the two terms and flips their relative sign back:
$P_{12}|\psi_A\rangle=-|\psi_A\rangle$, a genuine $-1$ eigenstate,
*except* when $a=b$: then the two terms are identical and cancel
exactly, giving the zero vector, not a valid (normalizable) state at
all. This is the <Term id="pauli-exclusion-principle">Pauli exclusion principle</Term>, previewed here and made the
whole subject of the next lesson.

### Checking the construction against the operator

This platform's `symmetrize`/`antisymmetrize` functions build exactly
these combinations (with `normalizeVector` handling the division), and
`exchangeParticles` independently swaps the combined vector's two
particle-labels. Feeding `symmetrize(a,b)` through `exchangeParticles`
and comparing the result to `symmetrize(a,b)` itself (expecting exact
equality), and doing the same for `antisymmetrize(a,b)` against its own
*negative*, both hold to floating-point precision. That's a numerical
check of the eigenvalue claim using a function that doesn't know how
`symmetrize`/`antisymmetrize` were built, rather than just re-tracing
the algebra that built them.

## Physical Interpretation

**The spin-statistics theorem** (a deep result from relativistic quantum
field theory, well beyond this course's scope, stated here as an
experimentally-confirmed fact) says: particles with integer spin
($0,1,2,\ldots$, photons, the recently-covered spin-1 case from
Angular Momentum & Spin) are bosons; particles with half-integer spin
($\frac12,\frac32,\ldots$, electrons, protons, neutrons) are fermions.
This course takes the connection as given and explores its
consequences, rather than deriving it.

<PredictBeforeReveal
  question="ψ_A = antisymmetrize(e0, e1). What do you expect exchangeParticles(ψ_A) to return?"
  options={[
    { label: "ψ_A itself, unchanged", value: "same" },
    { label: "−ψ_A, every entry sign-flipped", value: "negative" },
    { label: "The zero vector", value: "zero" },
  ]}
  correctValue="negative"
  explanation="Every entry of ψ_A comes back with its sign flipped: exchangeParticles(ψ_A) = −ψ_A exactly, to floating-point precision. That's the −1 eigenvalue that defines a fermion — not asserted, but read straight off the numbers."
/>

## Worked Example

**Using a 3-dimensional single-particle basis $\{|0\rangle,|1\rangle,|2\rangle\}$,
confirm `antisymmetrize(e0,e1)` is a genuine $-1$ eigenstate.**

`antisymmetrize(e0,e1)` gives the normalized vector proportional to
$|0\rangle\otimes|1\rangle-|1\rangle\otimes|0\rangle$. Applying
`exchangeParticles` to this result gives exactly the *negative* of the
input, matching to floating-point precision (`maxDiff` between
`antisymmetrize(e0,e1)` and $-$`exchangeParticles(antisymmetrize(e0,e1))`
is exactly 0 in this platform's test suite), a direct numerical
confirmation of the $-1$ eigenvalue.

<InteractiveSection title="Compare the two constructions directly" description="Switch between ψ_S and ψ_A, both built from the same e0, e1. Exchange leaves ψ_S's grid completely unchanged (+1); for ψ_A, every entry flips sign (−1) — read the numbers off the grid yourself rather than trusting the algebra alone.">
  <ExchangeDiagramExplorer
    presets={eigenstatePresets}
    ariaLabel="Choose between the symmetric and antisymmetric combinations of e0 and e1 to see each one's coefficient grid before and after exchange"
  />
</InteractiveSection>

## Common Mistakes

<Callout type="warning">
**Forgetting the normalization step matters.** $|a\rangle\otimes|b\rangle\pm|b\rangle\otimes|a\rangle$
is *not* automatically normalized even when $|a\rangle,|b\rangle$
individually are (its norm depends on whether $a,b$ are orthogonal, and
whether $a=b$). `normalizeVector` handles this correctly in both cases,
rather than assuming a fixed $1/\sqrt2$ factor.
</Callout>

<Callout type="note">
**Thinking symmetrization/antisymmetrization is a choice experimenters
make.** It's dictated entirely by the particle species (boson vs.
fermion) via the spin-statistics connection, not a modeling choice
applied differently depending on the situation.
</Callout>

## Practice Questions

1. Show that $|\psi_S\rangle$ is a valid (normalizable) state for
   *every* choice of $a,b$, including $a=b$, while $|\psi_A\rangle$ is
   not.
2. If $a$ and $b$ are orthogonal and each individually normalized, what
   is $\||a\rangle\otimes|b\rangle+|b\rangle\otimes|a\rangle\|^2$, before
   normalizing? (This is exactly what `normalizeVector` divides out.)
3. Photons have spin 1; electrons have spin 1/2. Which symmetrization
   rule applies to each, per the spin-statistics connection?
4. Why is checking `exchangeParticles(antisymmetrize(a,b))` against
   $-$`antisymmetrize(a,b)` numerically a stronger confirmation than just
   re-deriving the algebra that built `antisymmetrize` in the first
   place?

<PracticeLinks problems={practiceProblems} />

## Summary

- $|\psi_S\rangle\propto|a\rangle\otimes|b\rangle+|b\rangle\otimes|a\rangle$
  is always a valid $+1$ exchange eigenstate (bosons).
- $|\psi_A\rangle\propto|a\rangle\otimes|b\rangle-|b\rangle\otimes|a\rangle$
  is a valid $-1$ exchange eigenstate only when $a\neq b$ (fermions); at
  $a=b$ it vanishes entirely.
- The spin-statistics theorem (integer spin ↔ boson, half-integer spin ↔
  fermion) is taken as an experimental fact; this platform's engine
  checks the ±1 eigenvalue claims against `exchangeParticles` directly,
  rather than resting on the construction alone.

## Further Exploration

<NextDiscovery>
[The Pauli Exclusion Principle](/lessons/quantum-mechanics/identical-particles/the-pauli-exclusion-principle) treats the $a=b$
vanishing result as real physics rather than a mathematical curiosity.
</NextDiscovery>
