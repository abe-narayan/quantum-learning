import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemsForLesson } from "@/lib/problems/registry";
import { BarChartExplorer } from "@/components/visualizations/BarChartExplorer";
import { EnergyLevelDiagram } from "@/components/visualizations/EnergyLevelDiagram";

export const practiceProblems = getProblemsForLesson(
  "quantum-mechanics/identical-particles/multi-electron-atoms-introduction"
).map((problem) => problem.meta);

export const shellCapacities = [2, 2, 6, 2, 6, 2, 10];
export const shellLabels = ["1s", "2s", "2p", "3s", "3p", "4s", "3d"];
export const shellFillCounts = (electronCount) =>
  shellCapacities.reduce(
    (acc, cap) => ({
      remaining: acc.remaining - Math.max(0, Math.min(cap, acc.remaining)),
      filled: [...acc.filled, Math.max(0, Math.min(cap, acc.remaining))]
    }),
    { remaining: electronCount, filled: [] }
  ).filled;
export const shellFillFrame = (electronCount) => ({
  paramLabel: `${electronCount} electrons`,
  bars: shellLabels.map((label, i) => ({
    label,
    value: shellFillCounts(electronCount)[i] / shellCapacities[i],
    caption: `${shellFillCounts(electronCount)[i]}/${shellCapacities[i]}`,
    highlight: shellFillCounts(electronCount)[i] > 0 && shellFillCounts(electronCount)[i] < shellCapacities[i]
  }))
});

export const hydrogenLikeLevels = [
  { label: "3s", energy: -0.111 },
  { label: "3p", energy: -0.111 },
  { label: "3d", energy: -0.111 },
  { label: "4s", energy: -0.0625 },
];

export const realAtomLevels = [
  { label: "3s", energy: -0.3 },
  { label: "3p", energy: -0.2 },
  { label: "4s", energy: -0.12, highlight: true },
  { label: "3d", energy: -0.08, highlight: true },
];

export const lessonMeta = {
  title: "Multi-Electron Atoms (Introduction)",
  description:
    "Combining hydrogen's already-derived n² orbital degeneracy with exclusion's 2-electrons-per-orbital (spin up/down) limit gives each shell a maximum capacity of 2n² electrons, explaining, without any new machinery, why the periodic table's shells fill the way they do.",
  course: "identical-particles",
  module: "multi-electron-atoms-introduction",
  order: 1,
  difficulty: "advanced",
  estimatedMinutes: 20,
  prerequisites: ["quantum-mechanics/identical-particles/the-pauli-exclusion-principle"],
  objectives: [
    "Derive the 2n² maximum shell capacity by combining hydrogen's n² orbital count with exclusion's spin-up/spin-down limit per orbital",
    "State why exact hydrogen-like orbitals are only an approximation for multi-electron atoms, and name the missing physics (electron-electron repulsion)",
    "Read off a simple electron configuration (e.g. for carbon or neon) using the shell-filling picture",
  ],
};

<LessonHook eyebrow="Why this matters">
Why does the periodic table have the shape it does — two elements in the first row, eight in the next two, then a jump to eighteen? Nothing about this lesson needs new physics to answer that.
</LessonHook>

## Motivation

Hydrogen Energy Levels
already found $n^2$ degenerate orbitals for shell $n$; the previous
lesson already found that each single-particle state, spatial orbital
*and* spin, can hold at most one electron. Multiply those two
already-established facts together and the shell structure of the whole
periodic table falls out.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/c/ce/Glowing_noble_gases.jpg"
  alt="Five glass discharge tubes glowing with the characteristic colors of ionized helium, neon, argon, krypton, and xenon"
  caption="Helium, neon, argon, krypton, and xenon (left to right), each glowing its own characteristic color when ionized. Every one of these elements has a completely filled outer shell in exactly the 2n² sense this lesson derives, which is also why they are chemically inert: neon's practice question below is the second tube from the left."
  credit="Alchemist-hp (composite); individual gas photographs by Jurii"
  creditUrl="https://commons.wikimedia.org/wiki/File:Glowing_noble_gases.jpg"
  license="CC BY 3.0"
/>

## Mathematical Development

### Shell capacity: 2n²

Shell $n$ has $n^2$ distinct spatial orbitals (Hydrogen Energy Levels'
degeneracy count: $\sum_{l=0}^{n-1}(2l+1)=n^2$). Each spatial orbital,
by exclusion, can hold at most 2 electrons, one spin-up, one spin-down
(these count as different complete single-particle states, as the
previous lesson established). So shell $n$ holds at most:

$$
2n^2 \text{ electrons}
$$

$n=1$: $2(1)^2=2$ electrons (fills 1s). $n=2$: $2(2)^2=8$ electrons
(fills 2s and 2p together). $n=3$: $2(3)^2=18$, though in real multi-electron
atoms the filling order gets more complicated starting around here (a
real complication this introduction names but doesn't resolve; see
below).

The factor of 2 is most of chemistry's answer to "why do noble gases
exist." An atom with every orbital up to some shell completely full,
helium at 2, neon at 10 ($2+8$), has no partially-occupied orbital left
to share or steal an electron through, which is exactly why those
elements sit inertly at the ends of their rows while carbon, with 2p
only two-sixths full, is eager to bond in four different directions at
once. It's also why reactivity is periodic rather than a one-off
property of each element: the same "one orbital short of closing" or
"one orbital past closing" pattern recurs every time a shell's $2n^2$
slots fill up again.

### What's exactly hydrogen-like, and what isn't

The orbital *shapes* and *degeneracy counts* used above come directly
from Hydrogen Energy Levels and Orbitals & Quantum Numbers, carried over
wholesale, not re-derived. What's *not* reused: hydrogen's exact
$l$-independence of energy (Hydrogen Energy Levels' special result for
the pure $1/r$ potential) breaks down in a multi-electron atom, because
each electron now also feels the other electrons' repulsion, not just
the nucleus's attraction. This repulsion is not spherically symmetric
in the same simple $1/r$ way, and, critically, it does depend on $l$
(a $2s$ electron, closer to the nucleus, feels the nuclear attraction
more directly than a $2p$ electron of the same $n$, which is
"screened" more by inner electrons). This is exactly why real multi-electron
filling order is 1s, 2s, 2p, 3s, 3p, 4s, 3d, ... rather than simply
increasing $n$.

## Physical Interpretation

This course does not attempt to solve the true multi-electron
Hamiltonian (which has no exact closed-form solution even for helium,
the two-electron case: a hard many-body problem in its own right, active
research territory for larger atoms). What this introduction provides is
the *qualitative* structure, shell capacities and the shape of the
filling logic, using hydrogen's exact orbitals as an approximate
single-particle basis, which is both an honest, standard approximation
and as far as this course's scope goes.

<PredictBeforeReveal
  question="Carbon has 6 electrons. Filling 1s and 2s to capacity (2 + 2 = 4) leaves 2 electrons for 2p, whose capacity is 6. Does 2p end up full, and does carbon have a closed outer shell like a noble gas?"
  options={[
    { label: "Yes — 2p fills completely, carbon is inert", value: "full" },
    { label: "No — 2p ends up only 2 of 6 full, carbon's shell stays open", value: "partial" },
  ]}
  correctValue="partial"
  explanation="Only 2 of 2p's 6 slots get filled: capacity isn't the same as occupancy. Carbon's outer shell is wide open, which is exactly why it's chemically versatile (four bonds) rather than inert like the noble gases in the figure above, whose outer shells really are full."
/>

## Worked Example

**Using the shell-filling picture, write the electron configuration for
carbon (6 electrons).**

Fill lowest shells first: 1s holds 2 (fills, using its full $2(1)^2=2$
capacity). Remaining 4 electrons go into $n=2$: 2s holds 2 more (full,
$2(0+1)^2$... more precisely 2s alone holds 2, its own sub-shell
capacity), leaving 2 electrons for 2p. Configuration:
$1s^2\,2s^2\,2p^2$, matching carbon's known configuration, built here
entirely from the $n^2$-orbital-count-times-2-for-spin logic, no
periodic table lookup required.

<BarChart
  bars={[
    { label: "1s", value: 2 / 2, caption: "2/2" },
    { label: "2s", value: 2 / 2, caption: "2/2" },
    { label: "2p", value: 2 / 6, caption: "2/6", highlight: true },
  ]}
  maxValue={1}
  ariaLabel="Carbon's electron configuration: 1s2 2s2 2p2, with 2p only partially filled"
/>

1s and 2s are both filled to capacity; 2p (capacity 6, three orbitals
times 2 for spin) holds only 2 of its 6 available slots, exactly why
carbon's outer shell isn't closed and it forms 4 bonds rather than being
chemically inert.

<InteractiveSection title="Fill the shells for any element" description="Slide through electron counts and watch the shells fill in order. Land on 10 to check this lesson's own practice question 2 (neon), and notice 3s only starts filling once 2p is completely full at 10. Keep sliding past 18 (argon) to 20 (calcium) and 21 (scandium): 4s fills completely before 3d gets a single electron, the exact anomaly named in Common Mistakes below, happening live rather than just being asserted.">
  <BarChartExplorer
    frames={Array.from({ length: 21 }, (_, i) => shellFillFrame(i + 1))}
    sliderLabel="Electron count"
    ariaLabel="Electron shell filling recomputed live as the electron count increases, showing each subshell filling to capacity before the next begins, including the 4s-before-3d crossover past argon"
    maxValue={1}
  />
</InteractiveSection>

### Why 4s slips under 3d

The bar chart above shows *that* 4s fills before 3d; this diagram shows
*why*, using the same screening argument from Mathematical Development.
In a pure hydrogen-like potential, energy depends only on $n$, so 3s,
3p, and 3d are exactly degenerate and 4s sits strictly above all three.
Toggle to "Real multi-electron atom" to see screening split that
degeneracy apart and drop 4s below 3d, letting the higher-$n$ 4s state
fill first even though $n=4>3$.

<div className="not-prose flex flex-wrap items-center gap-2 rounded-xl border border-border bg-surface-muted/40 p-4">
  <input type="radio" id="mea-n3-preset-hlike" name="mea-n3-preset" defaultChecked className="peer/hlike sr-only" />
  <label
    htmlFor="mea-n3-preset-hlike"
    className="cursor-pointer rounded-full border border-border bg-surface px-3 py-1.5 text-xs font-medium text-muted-foreground transition-colors hover:bg-surface-muted peer-checked/hlike:border-transparent peer-checked/hlike:bg-brand peer-checked/hlike:text-brand-foreground peer-focus-visible/hlike:outline-none peer-focus-visible/hlike:ring-2 peer-focus-visible/hlike:ring-brand peer-focus-visible/hlike:ring-offset-2 peer-focus-visible/hlike:ring-offset-background"
  >
    Hydrogen-like (l-degenerate)
  </label>
  <input type="radio" id="mea-n3-preset-real" name="mea-n3-preset" className="peer/real sr-only" />
  <label
    htmlFor="mea-n3-preset-real"
    className="cursor-pointer rounded-full border border-border bg-surface px-3 py-1.5 text-xs font-medium text-muted-foreground transition-colors hover:bg-surface-muted peer-checked/real:border-transparent peer-checked/real:bg-brand peer-checked/real:text-brand-foreground peer-focus-visible/real:outline-none peer-focus-visible/real:ring-2 peer-focus-visible/real:ring-brand peer-focus-visible/real:ring-offset-2 peer-focus-visible/real:ring-offset-background"
  >
    Real multi-electron atom
  </label>
  <div className="hidden w-full peer-checked/hlike:block">
    <EnergyLevelDiagram
      levels={hydrogenLikeLevels}
      unit="arb. units"
      ariaLabel="Hydrogen-like n=3 and n=4 levels: 3s, 3p, and 3d exactly degenerate, with 4s strictly above all three"
    />
  </div>
  <div className="hidden w-full peer-checked/real:block">
    <EnergyLevelDiagram
      levels={realAtomLevels}
      unit="arb. units"
      ariaLabel="Real multi-electron atom: 3s below 3p below 4s below 3d, screening having pushed 4s under 3d, both 4s and 3d highlighted"
    />
  </div>
</div>

## Common Mistakes

<Callout type="mistake">
**Assuming multi-electron atoms fill strictly by increasing n.** Real
atoms deviate starting around $n=3$ (4s fills before 3d, for instance),
a direct consequence of electron-electron screening breaking hydrogen's
exact $l$-independence, as this lesson's Mathematical Development
section explains. This course's $2n^2$ formula is exactly right for
*capacity*, but filling *order* needs the screening correction this
introduction only names, not derives.
</Callout>

<Callout type="note">
**Thinking multi-electron atoms are "solved" the same way hydrogen was.**
Hydrogen's Schrödinger equation has an exact closed-form solution;
multi-electron atoms, even helium, do not. This course uses hydrogen's
orbitals only as an approximate starting basis, an honest scope
limitation stated explicitly rather than glossed over.
</Callout>

## Practice Questions

1. Compute the shell capacity for $n=3$ using $2n^2$, and explain what
   each factor of the formula represents physically.
2. Write the electron configuration for neon (10 electrons), following
   the worked example's method.
3. Why can't the shell-filling picture developed here be used to compute
   an EXACT ground-state energy for a multi-electron atom, even though it
   correctly predicts electron configurations?
4. Name the one physical effect, absent from the pure hydrogen
   treatment, that's responsible for the 4s-before-3d filling anomaly.

<PracticeLinks problems={practiceProblems} />

## Summary

- Shell $n$ holds at most $2n^2$ electrons: $n^2$ spatial orbitals
  (Hydrogen Energy Levels) times 2 (exclusion's spin-up/spin-down limit
  per orbital).
- This reuses hydrogen's orbitals as an approximate single-particle
  basis, useful for electron configurations, but not an exact solution
  to the true multi-electron Hamiltonian.
- Electron-electron repulsion (screening) breaks hydrogen's exact
  $l$-independence of energy, which is why real filling order departs
  from strict increasing-$n$ starting around $n=3$.

## Further Exploration

<NextDiscovery>
This closes **Identical Particles & Many-Body Systems**. The next
course, **Advanced Topics in Quantum Mechanics**, builds on density
matrices and entanglement with new material, not a repeat of the
already-completed Entanglement, Mixed States & Bell Tests course.
</NextDiscovery>
