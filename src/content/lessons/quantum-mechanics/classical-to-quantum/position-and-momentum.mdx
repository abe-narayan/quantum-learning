import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemsForLesson } from "@/lib/problems/registry";
import { PhaseSpacePanel } from "@/components/visualizations/PhaseSpacePanel";

export const practiceProblems = getProblemsForLesson(
  "quantum-mechanics/classical-to-quantum/position-and-momentum"
).map((problem) => problem.meta);

export const HBAR = 1.055e-34;
export const ELECTRON_MASS = 9.1e-31;

export const positionMomentumFrame = (dxTimes1e10) => ({
  paramLabel: `Δx = ${dxTimes1e10.toFixed(1)}×10⁻¹⁰ m`,
  bars: [
    {
      label: "Δp_min (×10⁻²⁵ kg·m/s)",
      value: (HBAR / (2 * dxTimes1e10 * 1e-10)) * 1e25,
      highlight: Math.abs(dxTimes1e10 - 1) < 0.05,
    },
    {
      label: "Δv_min, electron (×10⁵ m/s)",
      value: (HBAR / (2 * dxTimes1e10 * 1e-10) / ELECTRON_MASS) * 1e-5,
    },
  ],
});

export const positionMomentumFrames = Array.from({ length: 21 }, (_, i) => positionMomentumFrame(0.5 + i * 0.1));

export const uncertaintyBoxFrames = positionMomentumFrames.map((f, i) => ({
  paramLabel: f.paramLabel,
  deltaX: 0.5 + i * 0.1,
  deltaP: f.bars[0].value,
}));

export const lessonMeta = {
  title: "Position and Momentum",
  description:
    "The canonical commutation relation [x,p]=iℏ, the Heisenberg uncertainty bound it implies via the general relation already derived, and how it connects directly to the harmonic oscillator's ladder algebra.",
  course: "classical-to-quantum",
  module: "position-and-momentum",
  order: 1,
  difficulty: "intermediate",
  estimatedMinutes: 20,
  prerequisites: ["quantum-mechanics/classical-to-quantum/the-quantum-harmonic-oscillator"],
  objectives: [
    "State the canonical commutation relation [x,p] = iℏ",
    "Derive the Heisenberg uncertainty bound ΔxΔp ≥ ℏ/2 from the general relation proved earlier in this course",
    "Verify [x,p]=iℏ directly from the harmonic oscillator's ladder-operator commutator",
  ],
};

## Motivation

Every observable in this course so far has lived on a finite-dimensional
Hilbert space, a handful of numbers was enough to specify a state
completely. A real particle's position doesn't work that way: it can, in
principle, take a continuum of values. This lesson previews what changes
and, more importantly, what doesn't, when position and momentum enter the
picture, without building the full continuous machinery, which is the
subject of the Wave Mechanics course.

## Intuition

Position and momentum are the original pair the entire theory of
quantum mechanics grew out of: Heisenberg's uncertainty principle,
stated for exactly this pair, is the single most famous consequence of
everything derived so far in this course. The postulates don't change at
all for a continuous observable: states are still vectors, position and
momentum are still Hermitian operators, measurement is still governed by
the Born rule, only the *label* on the basis states becomes a continuous
variable $x$ instead of a discrete index $i$.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/2/21/Blue_laser_single_slit_diffraction_photo.jpg"
  alt="Photograph of a single-slit diffraction pattern from a blue laser, a bright central band flanked by fainter side bands"
  caption="Confining a laser beam's position to a narrow slit spreads its momentum (direction) into this diffraction pattern — the same position-momentum trade-off that [x,p]=iℏ makes precise for any quantum system."
  credit="Xyqorophibian / Wikimedia Commons"
  creditUrl="https://commons.wikimedia.org/wiki/File:Blue_laser_single_slit_diffraction_photo.jpg"
  license="CC BY-SA 4.0"
/>

## Mathematical Development

### Position and momentum as operators

A particle's state is a vector $|\psi\rangle$ in an infinite-dimensional
Hilbert space; position eigenstates $|x\rangle$ exist for every real
number $x$ (with subtleties around normalization that a fully rigorous
treatment handles carefully, deferred to Wave Mechanics). Position and
momentum are both Hermitian operators, $\hat x$ and $\hat p$, and satisfy
the **canonical commutation relation**:

$$
[\hat x,\hat p] = i\hbar
$$

<Callout type="note">
This relation is the quantum version of a classical structure (the
Poisson bracket $\{x,p\}=1$) turned into a commutator, a standard and
motivated step, but not one this lesson derives from first principles;
doing so rigorously needs the position-space representation ($\hat p$
acting as $-i\hbar\,d/dx$ on wavefunctions) built in the Wave Mechanics
course. Here, $[\hat x,\hat p]=i\hbar$ is taken as given, and its
*consequences*, which this course already has every tool needed to
derive, are worked out below.
</Callout>

### The Heisenberg uncertainty principle, derived

The Expectation Values and Uncertainty lesson proved, for *any* two
Hermitian observables $A,B$:

$$
\Delta A\,\Delta B \ge \tfrac12|\langle[A,B]\rangle|
$$

Substitute $A=\hat x$, $B=\hat p$, and $[\hat x,\hat p]=i\hbar$ directly:

$$
\Delta x\,\Delta p \ge \tfrac12|\langle i\hbar\rangle| = \tfrac12\hbar|\langle1\rangle| = \frac{\hbar}{2}
$$

(using $\langle i\hbar\rangle = i\hbar\langle\psi|\psi\rangle = i\hbar$
for a normalized state, so $|\langle i\hbar\rangle|=\hbar$). This is the
**Heisenberg uncertainty principle**, $\Delta x\,\Delta p\ge\hbar/2$, not
a new postulate, but the general uncertainty relation from earlier in
this course, evaluated for this one specific, historically first-studied
pair of observables.

### Connecting to the harmonic oscillator

The harmonic oscillator's position and momentum operators can be written
directly in terms of the ladder operators from the last lesson:

$$
\hat x = \sqrt{\frac{\hbar}{2m\omega}}\,(a+a^\dagger), \qquad \hat p = i\sqrt{\frac{\hbar m\omega}{2}}\,(a^\dagger-a)
$$

This is worth checking rather than taking on faith: compute $[\hat
x,\hat p]$ using only $[a,a^\dagger]=1$ (the harmonic oscillator's
defining relation from the last lesson) and bilinearity of the
commutator:

$$
[\hat x,\hat p] = i\sqrt{\tfrac{\hbar}{2m\omega}}\sqrt{\tfrac{\hbar m\omega}{2}}\,[a+a^\dagger,\ a^\dagger-a] = i\cdot\frac{\hbar}{2}\left([a,a^\dagger]-[a,a]+[a^\dagger,a^\dagger]-[a^\dagger,a]\right)
$$

The middle two commutators vanish (any operator commutes with itself),
leaving $[a,a^\dagger]-[a^\dagger,a] = 1-(-1)=2$:

$$
[\hat x,\hat p] = i\cdot\frac{\hbar}{2}\cdot2 = i\hbar \checkmark
$$

The canonical commutation relation for $\hat x,\hat p$ isn't a separate
fact from the harmonic oscillator's ladder algebra. It's a direct
algebraic consequence of it, confirmed by the same commutator machinery
built in the Expectation Values lesson.

## Physical Interpretation

Momentum plays a second role beyond "an observable": it generates
translations in space, exactly the way the Hamiltonian generates
translations in time (Time Evolution lesson), a deep structural
parallel this course doesn't develop fully, but worth knowing exists.
Historically, $[\hat x,\hat p]=i\hbar$ (via Heisenberg's matrix mechanics)
and the Schrödinger equation (via wave mechanics) were discovered
independently and only later shown to be the same theory. This course,
by building the Schrödinger equation from unitarity and now connecting
$[\hat x,\hat p]$ to the harmonic oscillator's algebra, has effectively
retraced both routes from the same postulates.

## Worked Example

**Using $\Delta x\,\Delta p\ge\hbar/2$, if a particle's position is known
to $\Delta x = 10^{-10}$ m (roughly an atomic radius), what is the
minimum possible $\Delta p$?**

$$
\Delta p \ge \frac{\hbar}{2\Delta x} = \frac{1.055\times10^{-34}}{2\times10^{-10}} \approx 5.3\times10^{-25}\ \text{kg·m/s}
$$

Small in everyday units, but for an electron (mass $\approx
9.1\times10^{-31}$ kg) this corresponds to a minimum velocity uncertainty
of roughly $\Delta p/m \approx 5.8\times10^5$ m/s, not negligible at
all on an atomic scale, which is exactly why quantum effects dominate
atomic physics but are undetectable for everyday (much more massive)
objects.

<InteractiveSection title="Slide Δx, watch the trade-off" description="Δp_min = ℏ/(2Δx) is recomputed live as Δx varies around an atomic radius; Δx = 1.0×10⁻¹⁰ m is marked, reproducing the worked example's Δp_min ≈ 5.3×10⁻²⁵ kg·m/s exactly. The second bar converts that same Δp_min to a minimum velocity uncertainty for an electron, the same conversion the worked example makes.">
  <BarChartExplorer
    frames={positionMomentumFrames}
    sliderLabel="Δx (×10⁻¹⁰ m)"
    ariaLabel="Minimum momentum uncertainty and the corresponding minimum electron velocity uncertainty, recomputed live from the Heisenberg bound as position uncertainty Delta x varies around one atomic radius"
  />
</InteractiveSection>

<InteractiveSection title="The uncertainty box: squeeze one side, the other stretches" description="A box of width Δx and height Δp = ħ/(2Δx), centered at the origin of the (x, p) plane. Drag the same Δx as above and watch the box narrow horizontally while it stretches vertically — its area Δx·Δp never changes, because it's pinned at ħ/2 by construction, not by coincidence. Because Δx here is in units of ×10⁻¹⁰ m and Δp in ×10⁻²⁵ kg·m/s, the readout's raw product (≈5.3) carries an implied ×10⁻³⁵ kg·m²/s — restoring it recovers the actual ħ/2 ≈ 5.3×10⁻³⁵ kg·m²/s.">
  <PhaseSpacePanel
    mode="box"
    frames={uncertaintyBoxFrames}
    sliderLabel="Δx (×10⁻¹⁰ m)"
    ariaLabel="Rectangle in the x, p plane with width Delta x and height the minimum Delta p, shrinking in width and growing in height as Delta x decreases, with area held fixed at h-bar over 2"
    productSuffix="(pinned — in these ×10⁻¹⁰ m / ×10⁻²⁵ kg·m/s units, this constant equals ħ/2 once the implied ×10⁻³⁵ kg·m²/s is restored)"
  />
</InteractiveSection>

## Common Mistakes

<Callout type="mistake">
**Treating $\hat p$ as an ordinary number rather than an operator.** Every
manipulation above used $[\hat x,\hat p]$ as an operator commutator, the
same object type as $[X,Y]$ for the Pauli matrices, the physics is
identical in kind, just on an infinite-dimensional space.
</Callout>

<Callout type="mistake">
**Re-deriving the uncertainty bound from scratch instead of reusing the
general relation.** $\Delta x\Delta p\ge\hbar/2$ is a single substitution
into the Expectation Values lesson's already-proven inequality, there's
no need for a separate argument specific to position and momentum.
</Callout>

## Practice Questions

1. What is $[\hat p,\hat x]$ (note the reversed order), given
   $[\hat x,\hat p]=i\hbar$?
2. Using the general uncertainty relation, what is the minimum value of
   $\Delta x\,\Delta(2p)$, in terms of $\hbar$?
3. If a particle's momentum were known exactly ($\Delta p=0$), what does
   the uncertainty relation say about $\Delta x$? Is this physically
   achievable, even in principle?
4. Verify, by substituting the ladder-operator definitions of $\hat x$
   directly, that $\hat x$ is Hermitian (recall $a^\dagger$ is the
   adjoint of $a$).

<PracticeLinks problems={practiceProblems} />

## Summary

- $[\hat x,\hat p]=i\hbar$ is the canonical commutation relation; its full
  derivation needs the continuous position representation built in Wave
  Mechanics, but its *consequences* don't.
- Substituting directly into the general uncertainty relation gives
  $\Delta x\,\Delta p\ge\hbar/2$, the famous bound, as a special case of
  a theorem already proven.
- $[\hat x,\hat p]=i\hbar$ follows directly from $[a,a^\dagger]=1$ once
  $\hat x,\hat p$ are written in terms of the harmonic oscillator's ladder
  operators: one algebra, two familiar faces.

## Further Exploration

Next: [Superposition, Interference, and Phase](/lessons/quantum-mechanics/classical-to-quantum/superposition-interference-and-phase), which returns to the
amplitude-and-phase picture from earlier in this course and ties it
together with everything built since, operators, evolution, and
uncertainty, using the Complex Amplitude Explorer directly.
