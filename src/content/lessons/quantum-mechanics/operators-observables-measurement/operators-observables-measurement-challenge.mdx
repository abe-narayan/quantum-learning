import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemsForLesson } from "@/lib/problems/registry";
import { Complex } from "@/lib/quantum/complex";
import { PAULI_Z } from "@/lib/quantum/gates";
import { projectorOntoSubspace } from "@/lib/quantum/projectors";
import { matrixToCells } from "@/components/visualizations/MatrixGrid";
import { MatrixGridExplorer } from "@/components/visualizations/MatrixGridExplorer";

export const practiceProblems = getProblemsForLesson(
  "quantum-mechanics/operators-observables-measurement/operators-observables-measurement-challenge"
).map((problem) => problem.meta);

export const degenerateEigvec1 = [new Complex(1), new Complex(0), new Complex(0)];
export const degenerateEigvec2 = [new Complex(0), new Complex(1), new Complex(0)];
export const synthesisP = projectorOntoSubspace([degenerateEigvec1, degenerateEigvec2]);
export const synthesisPresets = [
  {
    label: "P (a projector): P vs P²",
    panels: [
      { label: "P", cells: matrixToCells(synthesisP), highlightDiagonal: true },
      { label: "P²", cells: matrixToCells(synthesisP.mul(synthesisP)), highlightDiagonal: true }
    ]
  },
  {
    label: "Pauli-Z (Hermitian, not a projector): Z vs Z²",
    panels: [
      { label: "Z", cells: matrixToCells(PAULI_Z), highlightDiagonal: true },
      { label: "Z²", cells: matrixToCells(PAULI_Z.mul(PAULI_Z)), highlightDiagonal: true }
    ]
  }
];

export const lessonMeta = {
  title: "Operators, Observables & Measurement Challenge",
  description:
    "A synthesis capstone connecting this course's generalized theory back to the specific postulates it generalizes, and forward to where degeneracy and compatible observables reappear in more advanced systems.",
  course: "operators-observables-measurement",
  module: "operators-observables-measurement-challenge",
  order: 1,
  difficulty: "advanced",
  estimatedMinutes: 24,
  prerequisites: ["quantum-mechanics/operators-observables-measurement/degeneracy-in-practice"],
  objectives: [
    "Map every generalized result from this course onto the specific postulate it generalizes",
    "Solve synthesis problems combining spectral decomposition, compatibility, and the measurement postulate",
    "Identify what remains genuinely open for future courses that need degenerate spectra",
  ],
};

<LessonHook eyebrow="Why this matters">
This course took four ideas the earlier quantum-mechanics courses had already introduced in a specific, sometimes special-cased form — eigenvalues as measurement outcomes, the Born rule, uncertainty relations, compatible observables — and rebuilt each one at full generality.
</LessonHook>

## Motivation

Projectors instead of bare eigenvectors, degenerate
eigenspaces instead of assuming they don't exist, a proven (not asserted)
criterion for compatibility, and an honestly-derived energy-time bound
that doesn't pretend time is an operator.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/4/41/Hydrogen_spectrum.svg"
  alt="Hydrogen emission spectrum on a logarithmic wavelength scale, showing the Lyman, Balmer, Paschen, Brackett, Pfund, and Humphreys series"
  caption="Each line here marks a transition between two energy eigenvalues of hydrogen's Hamiltonian. Every one of those levels except the ground state is degenerate over l and m -- an entire multiplet of distinct orbital states hides inside each single line, exactly the situation this course's projectors, compatible observables, and CSCOs exist to describe."
  credit="OrangeDog / Wikimedia Commons"
  creditUrl="https://commons.wikimedia.org/wiki/File:Hydrogen_spectrum.svg"
  license="CC BY-SA 3.0"
/>

## Recap: Specific Cases to General Theory

| Earlier, specific version | This course's general version |
|---|---|
| $A=\sum_i\lambda_i\lvert e_i\rangle\langle e_i\rvert$ (Math. Foundations, nondegenerate) | $A=\sum_i\lambda_iP_i$, $P_i$ possibly higher-rank (Spectral Decomposition) |
| "$[A,B]=0$ removes the uncertainty tradeoff" (From Classical to Quantum) | $[A,B]=0\iff$ shared eigenbasis, proven both directions (Simultaneous Eigenstates) |
| A single observable's eigenvalue as a state label | A complete *set* of commuting observables, needed when any one is degenerate (Complete Sets) |
| $P(a_i)=\lvert c_i\rvert^2$ (From Classical to Quantum, implicitly nondegenerate) | $P(a_i)=\langle\psi\rvert P_i\lvert\psi\rangle$, reducing exactly to $\lvert c_i\rvert^2$ (Measurement Postulate, Generalized) |
| "Incompatible observables disturb each other" (stated) | A fully numeric $Z\to X\to Z$ sequence showing exactly how (Sequential Measurements) |
| $\Delta x\Delta p\ge\hbar/2$ specifically (Wave Mechanics) | $\Delta E\,\Delta t_A\ge\hbar/2$ for *any* observable $A$, derived via Ehrenfest's theorem (Energy-Time Uncertainty) |

Nothing in the right column contradicts the left column, every general
result reduces exactly to the specific one in the nondegenerate,
single-observable case already covered.

## Worked Synthesis Examples

**1. A Hermitian operator has a triply-degenerate eigenvalue. Using this
course's tools, how many *additional*, mutually-compatible observables
would generically be needed, at minimum, to fully resolve it into three
individually-labeled states?**

At minimum one, a second observable whose eigenvalues happen to take
three *different* values across the 3-dimensional degenerate eigenspace
resolves it completely (Complete Sets of Commuting Observables). Whether
one suffices in a *specific* physical case depends on whether such an
observable exists and is physically available to measure. The
Worked System lesson's $(Z_0,Z_1)$ pair needed exactly two observables
total (one degenerate on its own, resolved by the second) because that
was the natural symmetry available; a different system's degeneracy
might need more.

**2. A state is measured with observable $A$, giving outcome $a_i$
(degenerate). Immediately after, the *same* observable $A$ is measured
again. What's the probability of getting $a_i$ again, and why, using the
Measurement Postulate, Generalized?**

Probability $1$. After the first measurement, the state is
$P_i|\psi\rangle/\|P_i|\psi\rangle\|$, already entirely within the
$a_i$-eigenspace, so $P_i$ applied to it returns it unchanged (projectors
are idempotent, Spectral Decomposition), giving
$P(a_i\text{ again})=\langle\psi'|P_i|\psi'\rangle=1$. Repeated
immediate measurement of the same observable never disturbs its own
outcome. Only an *incompatible* observable measured in between can, as
Sequential Measurements demonstrated.

<InteractiveSection title="Idempotent versus merely Hermitian" description="Compare a genuine projector (P² = P, the property this example relies on) against Pauli-Z, which is Hermitian but not idempotent (Z² = I ≠ Z). Idempotence is a real, extra condition beyond Hermiticity, not automatic.">
  <MatrixGridExplorer
    presets={synthesisPresets}
    ariaLabel="Choose between a projector and a merely-Hermitian operator to compare each one against its own square"
  />
</InteractiveSection>

**3. Two observables $A$ and $B$ satisfy $[A,B]=0$. A student claims this
means measuring $A$ then $B$ always gives the same result as measuring
$B$ then $A$. Is this exactly right?**

Not quite as stated. Compatibility guarantees the two measurements don't
*disturb* each other's probability distribution or destroy each other's
definiteness, but if the *pre-measurement* state isn't already an
eigenstate of both, the two individual outcomes are still genuinely
random (with the same joint probabilities regardless of order). What's
guaranteed is that a *second* measurement of $A$ after measuring $B$
(compatible) reproduces $A$'s *first* outcome with certainty, exactly
because collapse onto a shared eigenbasis for one operator leaves the
state an eigenstate of the other too.

## Common Mistakes, Revisited

<Callout type="note">
**Treating this course's generalizations as replacing the earlier
postulates.** They're the same postulates, made precise for a case
(degeneracy) the earlier statements quietly assumed away, exactly the
same relationship "From Classical to Quantum" had to "Mathematical
Foundations," and "Wave Mechanics" had to "From Classical to Quantum."
</Callout>

<Callout type="note">
**Assuming every quantum system has trivial (nondegenerate) spectra.**
Real, physically important systems, including ones a few courses away
on this platform, like the hydrogen atom's energy levels, are highly
degenerate. This course's machinery isn't a mathematical nicety; it's
required infrastructure for that content.
</Callout>

## Practice Questions

1. Using the recap table, identify the one lesson in this course whose
   result was *not* a strict generalization of something already proven,
   i.e., which lesson introduced content with no earlier "special case"
   version at all?
2. A CSCO has three observables. Explain, in your own words, what
   "complete" is doing in that name, complete with respect to what,
   exactly?
3. If $\Delta E=0$ for a state (from the Energy-Time Uncertainty lesson),
   what does this course's collapse formalism predict happens if you
   measure *any* observable $A$ twice in a row, separated by some time
   $\Delta t$? (Combine two results from this course.)

<PracticeLinks problems={practiceProblems} />

## Summary

- Spectral decomposition, compatibility, complete sets, generalized
  measurement, and energy-time uncertainty are five faces of the same
  underlying idea: the earlier courses' postulates, made precise for
  degenerate spectra and multi-observable systems.
- Every general result in this course reduces exactly to its
  already-proven nondegenerate special case, a real, checked consistency
  property, not a coincidence.
- Degeneracy isn't a mathematical edge case to note in passing. It's the
  generic situation for real physical systems with enough symmetry, and
  this course's tools are the honest prerequisite for describing them
  correctly.

## Further Exploration

<NextDiscovery>
This completes **Operators, Observables & Measurement**. The natural next
step is **One-Dimensional Quantum Systems**, extending Wave Mechanics'
solvable potentials to the finite square well and scattering off a step
potential. The density-matrix and partial-trace machinery this recap
table doesn't yet cover (needed for mixed states and the reduced state of
an entangled subsystem) arrives later, in the Quantum Computing pillar's
Entanglement, Mixed States & Bell Tests course, and gets extended again
in this course's own Advanced Quantum Mechanics material on open systems
and decoherence.
</NextDiscovery>
