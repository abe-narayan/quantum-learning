import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemMetaForLesson } from "@/lib/problems/metaRegistry";
import { LazyTwoQubitExplorer } from "@/components/simulators/two-qubit-explorer/LazyTwoQubitExplorer";

export const practiceProblems = getProblemMetaForLesson(
  "quantum-mechanics/operators-observables-measurement/degeneracy-in-practice"
);

export const lessonMeta = {
  title: "Degeneracy in Practice: A Worked System",
  description:
    "Every idea from this course applied to one real, familiar system -- a Bell state -- using two-qubit observables to build an honest complete set of commuting observables and watch it resolve degeneracy exactly.",
  course: "operators-observables-measurement",
  module: "degeneracy-in-practice",
  order: 1,
  difficulty: "advanced",
  estimatedMinutes: 22,
  prerequisites: [
    "quantum-mechanics/operators-observables-measurement/sequential-measurements-and-incompatibility",
    "quantum-computing/quantum-gates-and-circuits/bell-states-and-entanglement",
  ],
  objectives: [
    "Construct a two-observable CSCO on a two-qubit system from single-qubit Pauli operators",
    "Compute a degenerate measurement probability and collapsed state for an entangled input",
    "Connect the collapse result directly to the Bell state's known perfect correlation",
  ],
};

<LessonHook eyebrow="Why this matters">
Every tool this course has built — spectral decomposition, compatible observables, complete sets, the generalized measurement postulate — is about to get applied to one concrete, already-familiar system: a Bell state.
</LessonHook>

## Motivation

That Bell state comes from the Quantum Gates & Circuits course. No new
physics here, just this course's machinery, all at once, on a system
you already know.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/thumb/9/9f/IBM_QC.jpg/1280px-IBM_QC.jpg"
  alt="The dilution-refrigerator cryostat housing IBM's 50-qubit superconducting quantum computing prototype, displayed at CES 2018"
  caption="Real multi-qubit hardware, like this superconducting quantum processor cooled to millikelvin temperatures, is exactly the kind of physical system this lesson's two-qubit CSCO (Z0, Z1) and Bell-state collapse calculation describe."
  credit="Lars Plougmann / Wikimedia Commons"
  creditUrl="https://commons.wikimedia.org/wiki/File:IBM_QC.jpg"
  license="CC BY-SA 2.0"
/>

## Intuition

$\hat Z$ on a single qubit has two nondegenerate eigenvalues, no
degeneracy to worry about. Move to *two* qubits, and an <Term id="observable">observable</Term> that
only looks at *one* of them ($\hat Z$ on qubit 0, ignoring qubit 1
entirely) automatically becomes degenerate on the 4-dimensional combined
space: two different values of qubit 1 give the *same* eigenvalue. This
is degeneracy showing up for a completely ordinary reason, not a
contrived example.

## Mathematical Development

### Building the observables

Let $Z_0=Z\otimes I$ (Pauli-$Z$ on qubit 0, identity on qubit 1) and
$Z_1=I\otimes Z$ (identity on qubit 0, Pauli-$Z$ on qubit 1), both
constructed directly via this platform's existing `Matrix.tensor()`, no
new engine code needed. Their eigenvalues on the four basis states:

| State | $Z_0$ | $Z_1$ |
|---|---|---|
| $\lvert00\rangle$ | $+1$ | $+1$ |
| $\lvert01\rangle$ | $+1$ | $-1$ |
| $\lvert10\rangle$ | $-1$ | $+1$ |
| $\lvert11\rangle$ | $-1$ | $-1$ |

$Z_0$ alone is degenerate ($+1$ on both $|00\rangle,|01\rangle$); so is
$Z_1$. But the *pair* $(Z_0,Z_1)$ gives four distinct tuples, a genuine
CSCO for this space, exactly as Complete Sets of Commuting Observables
predicted.

### Why they commute

Operators acting on *different* tensor factors always commute: using
$(A\otimes B)(C\otimes D)=(AC)\otimes(BD)$,

$$
Z_0Z_1 = (Z\otimes I)(I\otimes Z) = (ZI)\otimes(IZ) = Z\otimes Z = (IZ)\otimes(ZI) = (I\otimes Z)(Z\otimes I) = Z_1Z_0
$$

so $[Z_0,Z_1]=0$, confirmed directly, not merely asserted, and a
completely general fact ($A\otimes I$ and $I\otimes B$ commute for *any*
$A,B$, since they act on independent subsystems).

### Measuring $Z_0$ on an entangled state

Take the Bell state $|\Phi^+\rangle=\tfrac{1}{\sqrt2}(|00\rangle+
|11\rangle)$. In amplitude order $(|00\rangle,|01\rangle,|10\rangle,
|11\rangle)$, this is $\left(\tfrac1{\sqrt2},0,0,\tfrac1{\sqrt2}\right)$.
$Z_0$'s eigenvalue $+1$ projector is $P_+=\mathrm{projectorOntoSubspace}
(\{|00\rangle,|01\rangle\})$ (degenerate, spans two basis states). By
the generalized <Term id="born-rule">Born rule</Term>,

$$
P(Z_0{=}{+}1) = \left|\frac1{\sqrt2}\right|^2 + |0|^2 = \frac12
$$

Suppose the outcome is $+1$. $P_+|\Phi^+\rangle$ keeps only the
$|00\rangle,|01\rangle$ components: $\left(\tfrac1{\sqrt2},0,0,0\right)$,
which, already <Term id="normalization">normalized</Term>, collapses to exactly $|00\rangle$.

### Then measuring $Z_1$

On the now-collapsed $|00\rangle$, $Z_1$'s eigenvalue is $+1$ with
**certainty** ($|00\rangle$ is already a $Z_1$-eigenstate). The full pair
$(Z_0,Z_1)=(+1,+1)$ now uniquely identifies the state as $|00\rangle$,
this course's CSCO machinery working exactly as designed, on a real
two-qubit state.

<InteractiveSection
  title="Measure qubit 0, watch qubit 1 fall in line"
  description="Prepare |Φ+⟩, measure qubit 0. Confirm qubit 1's outcome is forced to match, exactly as this lesson's Z₀/Z₁ collapse calculation predicts.">
  <LazyTwoQubitExplorer />
</InteractiveSection>

## Physical Interpretation

This *is* the Bell state's famous perfect correlation, seen through this
course's formalism instead of asserted as a special fact: measuring
qubit 0 as $0$ forces qubit 1 to also be $0$, because the only
$|\Phi^+\rangle$-component consistent with $Z_0{=}{+}1$ is $|00\rangle$.
There is no $|01\rangle$ component in $|\Phi^+\rangle$ at all. The
"spooky" correlation from Bell States and Entanglement is, precisely,
this lesson's projector-and-collapse mechanics applied to an entangled
input.

## Worked Example

**Suppose the $Z_0$ outcome had instead been $-1$. What state results,
and what does $Z_1$ then give with certainty?**

$P_-$ keeps the $|10\rangle,|11\rangle$ components of $|\Phi^+\rangle$:
only $|11\rangle$ is present, so the state collapses to $|11\rangle$
directly. Measuring $Z_1$ on $|11\rangle$ then gives $-1$ with certainty,
the two qubits are again perfectly correlated ($Z_0=Z_1=-1$ this time),
consistent with $|\Phi^+\rangle$ containing only the $|00\rangle$ and
$|11\rangle$ terms.

## Common Mistakes

<Callout type="note">
**Thinking $Z_0$ and $Z_1$ commuting is special to the Bell state.**
It's a property of the *operators* ($A\otimes I$ vs. $I\otimes B$),
completely independent of which state they're applied to. The Bell
state is just an interesting *input* to already-compatible observables.
</Callout>

<Callout type="warning">
**Expecting $Z_0$ alone to fully determine the state.** It's degenerate
on this 4-dimensional space by construction. Only the *pair*
$(Z_0,Z_1)$ is a complete set here.
</Callout>

## Practice Questions

1. Confirm $Z_0$'s $-1$-eigenspace projector, applied to
   $|\Phi^+\rangle$, gives exactly $|11\rangle$ after normalizing (redo
   the calculation from the worked example explicitly).
2. For the *other* Bell state $|\Psi^+\rangle=\tfrac1{\sqrt2}
   (|01\rangle+|10\rangle)$, find $P(Z_0{=}{+}1)$ and the resulting
   collapsed state.
3. Explain briefly why $X_0=X\otimes I$ and $Z_1=I\otimes Z$ commute even
   though $X$ and $Z$ individually do not (from earlier in this course).

<PracticeLinks problems={practiceProblems} />

## Summary

- $Z_0=Z\otimes I$ and $Z_1=I\otimes Z$ each have degenerate eigenvalues
  alone, but form a complete set of commuting observables together.
- Operators on different tensor factors always commute, a general,
  directly-verified fact, not specific to this example.
- Measuring $Z_0$ on the Bell state $|\Phi^+\rangle$ and collapsing gives
  exactly the correlated outcome Bell States and Entanglement already
  described qualitatively, now derived from this course's own
  machinery.

## Further Exploration

<NextDiscovery>
The [Operators, Observables & Measurement Challenge](/lessons/quantum-mechanics/operators-observables-measurement/operators-observables-measurement-challenge) is a capstone
synthesizing every idea from this course against the postulates it
generalizes.
</NextDiscovery>
