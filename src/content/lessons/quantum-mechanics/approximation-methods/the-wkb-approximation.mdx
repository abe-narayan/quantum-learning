import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemMetaForLesson } from "@/lib/problems/metaRegistry";
import { createGrid } from "@/lib/quantum/wavefunction";
import { harmonicOscillatorPotential } from "@/lib/quantum/potentials";
import { PotentialDiagram } from "@/components/visualizations/PotentialDiagram";

export const practiceProblems = getProblemMetaForLesson(
  "quantum-mechanics/approximation-methods/the-wkb-approximation"
);

export const wkbGrid = createGrid(256, 0.025);
export const wkbEnergy = 1.5;
export const wkbTurningPoint = Math.sqrt(2 * wkbEnergy);

export const wkbPotentialArray = harmonicOscillatorPotential(wkbGrid, 1, 1);
export const wkbNs = [0, 1, 2, 3];
export const wkbEnergyForN = (n) => n + 0.5;

export const wkbMomentumPointsForEnergy = (energy) => wkbGrid.x
  .map((x, i) => ({ x, gap: energy - wkbPotentialArray[i] }))
  .filter((p) => p.gap > 0)
  .map((p) => ({ x: p.x, y: Math.sqrt(2 * p.gap) }));

export const wkbMomentumFrames = wkbNs.map((n) => ({
  paramLabel: `n = ${n}, E = ${wkbEnergyForN(n).toFixed(1)}`,
  series: [{ label: "p(x) = √(2(E−V(x)))", color: "brand", points: wkbMomentumPointsForEnergy(wkbEnergyForN(n)) }]
}));

export const lessonMeta = {
  title: "The WKB Approximation",
  description:
    "The semiclassical quantization condition ∫p(x)dx=(n+½)πħ, applied to the harmonic oscillator, reproduces the exact spectrum Eₙ=(n+½)ħω to within 0.06% at this platform's grid resolution — a route through classical mechanics and de Broglie wavelengths, unrelated to perturbation theory or the variational method, arriving at (nearly) the same answer anyway.",
  course: "approximation-methods",
  module: "the-wkb-approximation",
  order: 1,
  difficulty: "advanced",
  estimatedMinutes: 25,
  prerequisites: ["quantum-mechanics/approximation-methods/the-variational-method"],
  objectives: [
    "State the WKB (Bohr-Sommerfeld) quantization condition and identify the classically allowed region it integrates over",
    "Explain physically why WKB works well for large quantum numbers (the semiclassical/correspondence-principle limit)",
    "Compute WKB-quantized energies numerically and compare them to the exact harmonic oscillator spectrum",
  ],
  related: [
    {
      slug: "quantum-mechanics/one-dimensional-systems/solving-the-finite-well-numerically",
      note: "The identical numerical strategy, bisecting on energy until a transcendental quantization condition is satisfied, reused here for the WKB action integral instead of that lesson's k tan(ka)=κ equation.",
    },
  ],
};

<LessonHook eyebrow="Why this matters">
Perturbation theory needs a small parameter; the variational method needs a good guessed shape. WKB takes a third, geometric route entirely: treat the wavefunction as a classical wave and demand it close consistently on itself.
</LessonHook>

## Motivation

WKB (Wentzel-Kramers-Brillouin) treats the <Term id="wavefunction">wavefunction</Term> as approximately a
classical wave with a slowly-varying local wavelength set by the local
classical momentum $p(x)=\sqrt{2m(E-V(x))}$, demanding that this wave
close consistently on itself between the classical turning points.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/b/b4/Kramers_1928.jpg"
  alt="Photograph of physicist Hendrik Kramers, taken around 1928 in Ann Arbor, Michigan"
  caption="Hendrik Kramers, the 'K' in WKB, photographed around 1928, the same period he, Gregor Wentzel, and Léon Brillouin independently developed the semiclassical quantization method this lesson applies to the harmonic oscillator."
  credit="AIP Emilio Segrè Visual Archives, Goudsmit Collection"
  creditUrl="https://commons.wikimedia.org/wiki/File:Kramers_1928.jpg"
  license="Public domain"
/>

## Mathematical Development

### The classically allowed region

For energy $E$ and potential $V(x)$, the classically allowed region is
wherever $E>V(x)$ (a classical particle with that energy could physically
be there); its boundary points (where $E=V(x)$) are the **turning
points**, $x_1<x_2$. The local classical momentum is
$p(x)=\sqrt{2m(E-V(x))}$, real and positive only inside this region.

For the <Term id="quantum-harmonic-oscillator">harmonic oscillator</Term>'s $n=1$ level ($E=1.5$), the turning points
sit at $x=\pm\sqrt{2E}\approx\pm1.732$, found by solving $E=V(x)=x^2/2$
directly:

<PotentialDiagram
  xValues={wkbGrid.x}
  potential={harmonicOscillatorPotential(wkbGrid, 1, 1)}
  energyLine={wkbEnergy}
  markers={[{ x: -wkbTurningPoint, label: "x₁" }, { x: wkbTurningPoint, label: "x₂" }]}
  shadedRegion={{ from: -wkbTurningPoint, to: wkbTurningPoint }}
  ariaLabel="Harmonic oscillator potential with the n=1 energy line and classically allowed region shaded"
/>

The shaded band is exactly where $E>V(x)$, the region the action
integral $\int_{x_1}^{x_2}p(x)dx$ sums over; outside it, $p(x)$ would be
imaginary, which is precisely why the integral stops at the turning
points rather than running over all $x$.

### The quantization condition

Demanding the semiclassical wave be single-valued and vanish smoothly at
both turning points (a technical boundary-matching argument this course
states rather than derives in full) gives:

$$
\int_{x_1}^{x_2} p(x)\,dx = \left(n+\frac12\right)\pi\hbar, \qquad n=0,1,2,\ldots
$$

This is the **Bohr-Sommerfeld quantization condition** (with the
$\frac12$ Maslov correction for two smooth/"soft" turning points, the
standard case for a smooth confining potential like the harmonic
oscillator). This platform's `wkbActionIntegral` computes the left side
directly, as a Riemann sum over the grid restricted to where $E>V(x)$;
`wkbQuantizedEnergy` bisects on $E$ until the integral matches the right
side for a given $n$, the same bisection strategy Wave Mechanics'
`finiteSquareWellGroundStateEnergy` already used for a different
transcendental equation.

## Physical Interpretation

WKB is a *semiclassical* approximation. It's built entirely from the
classical momentum $p(x)$, with quantum mechanics entering only through
the $\hbar$ in the quantization condition. It tends to work best when
the potential varies slowly on the scale of the local de Broglie
wavelength, physically the "large quantum number" or "correspondence
principle" regime where a quantum system starts behaving classically.
The harmonic oscillator is a special case where WKB happens to be exact
at *every* $n$, not just asymptotically for large $n$: a genuine,
checkable mathematical fact about this specific potential's shape, not a
general property of WKB.

## Worked Example

**Compute the WKB-quantized energies for the harmonic oscillator
($m=\omega=1$) at $n=0,1,2,3$, and compare to the exact
$E_n=(n+\frac12)\hbar\omega$.**

| $n$ | Exact | WKB (this engine) | Difference |
|---|---|---|---|
| 0 | 0.5000 | 0.5003 | 0.00026 |
| 1 | 1.5000 | 1.4999 | 0.00008 |
| 2 | 2.5000 | 2.5003 | 0.00031 |
| 3 | 3.5000 | 3.4996 | 0.00038 |

Every WKB value matches the exact answer to within $\sim0.04\%$ (the
small residual differences are pure grid-resolution numerical error in
the Riemann-sum action integral and bisection, not a limitation of WKB
itself: WKB is mathematically exact for the harmonic oscillator).

The table's four energies are exactly the ones the action integral
$\int_{x_1}^{x_2}p(x)dx$ is evaluated over. Watching the local classical
momentum $p(x)=\sqrt{2(E-V(x))}$ itself, across only the classically
allowed region, shows directly why higher $n$ means a bigger action
integral: both the region's width (the turning points) and the peak
momentum grow together.

WKB is usually sold as a large-$n$ approximation, so it's worth pausing
on what that predicts for this specific table before looking at it
again:

<PredictBeforeReveal
  question="WKB is a semiclassical approximation, expected to improve as the quantum number grows. Given that, which row of the table above should show the largest error against the exact spectrum?"
  options={[
    { label: "n = 0 (smallest quantum number)", value: "n0" },
    { label: "n = 3 (largest quantum number)", value: "n3" },
    { label: "None in particular — all four errors should be about the same tiny size", value: "same" },
  ]}
  correctValue="same"
  explanation="The 'large n is more accurate' rule of thumb is real for a generic potential, but it doesn't show up here: the errors run 0.00026, 0.00008, 0.00031, 0.00038 for n=0 through 3 — no downward trend at all, just grid-resolution noise scattered near zero. That's because the harmonic oscillator is one of the rare potentials where WKB is exact at every n, not just asymptotically, so there's no large-n convergence to see. The usual rule of thumb applies to potentials where WKB is only approximately right, which this one isn't."
/>

<InteractiveSection
  title="Step through n, watch the action integral grow"
  description="Step through the energy level n = 0 to 3, the same four levels as the table above. Each curve is the local classical momentum p(x), computed from the same harmonic oscillator potential, but shown only across the classically allowed region. Watch the turning points spread apart and the peak momentum grow as n increases — that growing area under the curve is exactly the action integral the quantization condition sets equal to (n+½)πħ."
>
  <ParametricCurve
    frames={wkbMomentumFrames}
    sliderLabel="Energy level n"
    ariaLabel="Classical momentum p(x) across the classically allowed region, for the harmonic oscillator's n=0 through n=3 energy levels"
  />
</InteractiveSection>

## Common Mistakes

<Callout type="note">
**Thinking WKB is always this accurate.** The harmonic oscillator's
exactness is special (tied to its potential's specific quadratic shape).
For a generic potential, WKB is an *approximation* that improves as $n$
grows, and can be noticeably off for small $n$. This course doesn't
implement a second example to contrast against, but the distinction
matters for correctly interpreting the harmonic oscillator's near-perfect
result here.
</Callout>

<Callout type="warning">
**Forgetting the $\frac12$ in $(n+\frac12)\pi\hbar$.** Omitting it
(using $n\pi\hbar$ instead) is a common error that shifts every
predicted energy level. The Maslov correction for two soft turning
points is not optional bookkeeping.
</Callout>

## Practice Questions

1. Where exactly does the classically allowed region's boundary sit, in
   terms of $E$ and $V(x)$?
2. Explain in words why $p(x)=\sqrt{2m(E-V(x))}$ becomes imaginary
   outside the classically allowed region, and why the action integral
   only sums over the region where it's real.
3. If a potential's turning points move further apart as $E$ increases,
   what does this imply about how the action integral $\int p\,dx$
   behaves as $E$ grows? (Hint: this behavior is exactly what
   `wkbQuantizedEnergy`'s bisection search relies on.)
4. Why is WKB's near-perfect harmonic-oscillator result described here
   as a "genuine, checkable fact about this potential," rather than
   general evidence that WKB is always this accurate?

<PracticeLinks problems={practiceProblems} />

## Summary

- WKB quantizes energy via
  $\int_{x_1}^{x_2}p(x)dx=(n+\frac12)\pi\hbar$, using the classical
  local momentum and the potential's turning points.
- This platform computes the action integral as a Riemann sum over the
  classically allowed region, and bisects on $E$ to match the
  quantization condition, reusing the same numerical strategy as the
  finite-well ground-state calculation.
- For the harmonic oscillator, WKB reproduces the exact spectrum to
  within grid-resolution numerical error at every $n$ tested, a special
  exactness of this potential, not a general WKB guarantee.

## Further Exploration

<NextDiscovery>
[Time-Dependent Perturbation Theory](/lessons/quantum-mechanics/approximation-methods/time-dependent-perturbation-theory) leaves stationary states behind entirely and asks about
transitions: switch on a small time-dependent coupling between two
levels, and what's the probability of finding the system in the other
one later?
</NextDiscovery>
