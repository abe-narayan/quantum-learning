import { LazyBlochSphereExplorer } from "@/components/simulators/bloch-sphere/LazyBlochSphereExplorer";
import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemsForLesson } from "@/lib/problems/registry";

export const practiceProblems = getProblemsForLesson(
  "quantum-computing/qubits-and-quantum-states/quantum-gates"
).map((problem) => problem.meta);

export const lessonMeta = {
  title: "Quantum Gates",
  description:
    "The X, Y, Z, H, S, and T gates: what they do to a qubit, why they must be unitary, and how each one moves the Bloch vector.",
  course: "qubits-and-quantum-states",
  module: "quantum-gates",
  order: 1,
  difficulty: "intermediate",
  estimatedMinutes: 25,
  prerequisites: ["quantum-computing/qubits-and-quantum-states/measurement-and-probability"],
  objectives: [
    "Explain why quantum gates must be unitary",
    "Apply X, Y, Z, H, S, and T to a given state by matrix multiplication",
    "Describe each gate as a specific rotation of the Bloch vector",
  ],
  related: [
    {
      slug: "quantum-mechanics/mathematical-foundations/linear-operators",
      note: "The general theory of linear operators and matrix representation, which every gate here is a specific (unitary) example of.",
    },
  ],
};

## Motivation

Everything up to now has described qubit *states*: points on the sphere.
This lesson introduces the *operations*: the tools that move a qubit from
one point to another. These are quantum gates, and every quantum circuit
you'll ever see is just a sequence of them.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/6/64/4_Qubit_Chip%2C_10.1103.PhysRevX.5.021027_%28A._Wallraff%2C_2015%29.png"
  alt="Micrograph of a superconducting 4-qubit chip, showing the qubits, coplanar waveguide resonators, and microwave drive lines used to apply gates"
  caption="A real 4-qubit superconducting chip from a 2015 ETH Zürich experiment. The thin drive lines snaking across the chip carry the microwave pulses that physically implement gates like the ones in this lesson."
  credit="Y. Salathé et al. (A. Wallraff group) / Wikimedia Commons"
  creditUrl="https://commons.wikimedia.org/wiki/File:4_Qubit_Chip,_10.1103.PhysRevX.5.021027_(A._Wallraff,_2015).png"
  license="CC BY-SA 3.0"
/>

## Intuition

A classical logic gate (AND, NOT, OR) takes a bit and produces a bit, and
most of them are irreversible: given the output of an AND gate, you can't
recover its two inputs. Quantum gates are different by requirement: they
must be **reversible**, which turns out to mean every quantum gate is a
*rotation* of the Bloch sphere. Rotations never destroy information.
Rotate one way, then rotate back, and you're exactly where you started.
That single constraint shapes everything about how quantum gates behave.

## Mathematical Development

### Why gates must be unitary

A gate is a matrix $U$ acting on the state vector: $|\psi'\rangle=U|\psi\rangle$.
Since $|\psi'\rangle$ must itself be a valid, normalized state, we need
$\langle\psi'|\psi'\rangle=1$ whenever $\langle\psi|\psi\rangle=1$. Expand
the left side using $\langle\psi'| = \langle\psi|U^\dagger$ (the bra
transforms with the conjugate-transpose, written $U^\dagger$):

$$
\langle\psi'|\psi'\rangle = \langle\psi|U^\dagger U|\psi\rangle
$$

For this to equal $\langle\psi|\psi\rangle$ for *every* state
$|\psi\rangle$, we need $U^\dagger U = I$. A matrix satisfying this is
called **unitary**. Unitary matrices are automatically invertible (with
$U^{-1}=U^\dagger$), which is exactly the reversibility property from the
intuition above: undoing a gate is just applying its conjugate
transpose.

### The gates

Each gate below is unitary (check $U^\dagger U=I$ yourself as practice for
any of them), acts on a single qubit, and has a specific geometric meaning
as a rotation of the Bloch vector by some angle about some axis.

**$X$: bit flip.**

$$
X = \begin{pmatrix}0&1\\1&0\end{pmatrix}, \qquad X|0\rangle=|1\rangle, \quad X|1\rangle=|0\rangle
$$

Swaps the two basis states, exactly like a classical NOT gate. Rotates the
Bloch vector by $\pi$ about the $x$-axis.

**$Y$.**

$$
Y = \begin{pmatrix}0&-i\\i&0\end{pmatrix}, \qquad Y|0\rangle=i|1\rangle, \quad Y|1\rangle=-i|0\rangle
$$

Rotates by $\pi$ about the $y$-axis. Notice $Y|0\rangle = i|1\rangle$, not
plain $|1\rangle$: an extra phase factor $i$ appears that $X|0\rangle$
didn't have. Geometrically $X|0\rangle$ and $Y|0\rangle$ land at the exact
same point on the sphere. The next lesson explains precisely why that
extra $i$ doesn't matter.

**$Z$: phase flip.**

$$
Z = \begin{pmatrix}1&0\\0&-1\end{pmatrix}, \qquad Z|0\rangle=|0\rangle, \quad Z|1\rangle=-|1\rangle
$$

Leaves $|0\rangle$ untouched and flips the sign of $|1\rangle$'s
coefficient. Rotates by $\pi$ about the $z$-axis, which is why it fixes
the poles (they sit *on* the $z$-axis) while still doing something to
every other point.

**$H$: Hadamard.**

$$
H = \frac{1}{\sqrt2}\begin{pmatrix}1&1\\1&-1\end{pmatrix}, \qquad H|0\rangle=|+\rangle, \quad H|1\rangle=|-\rangle
$$

Let's verify $H|0\rangle$ directly:
$H|0\rangle = \frac{1}{\sqrt2}\begin{pmatrix}1&1\\1&-1\end{pmatrix}\begin{pmatrix}1\\0\end{pmatrix} = \frac{1}{\sqrt2}\begin{pmatrix}1\\1\end{pmatrix} = \frac{1}{\sqrt2}(|0\rangle+|1\rangle) = |+\rangle$.
$H$ is the gate that *creates* superposition from a definite state, and
since it's its own inverse ($H^2=I$, worth checking), it also *removes*
superposition: $H|+\rangle=|0\rangle$. Geometrically, $H$ rotates by $\pi$
about the diagonal axis exactly between the $x$- and $z$-axes, the only
one of these six gates whose axis isn't a coordinate axis.

**$S$ and $T$: phase gates.**

$$
S = \begin{pmatrix}1&0\\0&i\end{pmatrix}, \qquad T = \begin{pmatrix}1&0\\0&e^{i\pi/4}\end{pmatrix}
$$

Both leave $|0\rangle$ alone and multiply $|1\rangle$'s coefficient by a
phase: $i=e^{i\pi/2}$ for $S$, $e^{i\pi/4}$ for $T$. In the canonical
Bloch form, multiplying $\beta$ by $e^{i\delta}$ is exactly adding
$\delta$ to $\varphi$: $S$ rotates by $\pi/2$ about the $z$-axis (a
quarter turn), $T$ by $\pi/4$ (an eighth turn). Unlike $X,Y,Z,H$, neither
is its own inverse: $S^2=Z$ (two quarter-turns make a half-turn) and
$S^4=I$.

## Physical Interpretation

Every gate here rotates the Bloch vector by a fixed angle about a fixed
axis. That's not a coincidence of these six examples. It's a general
fact about single-qubit gates that the next lesson proves directly. Once
you see it that way, you don't need to memorize six unrelated matrices;
you need one geometric picture ("rotate the sphere") and six axis/angle
pairs.

## Interactive Experiment

<InteractiveSection title="Survey the gates, then compose two" description="Starting from |0⟩, apply each gate one at a time (resetting to |0⟩ between each) and watch where the vector lands. Then try composing two gates without resetting, e.g. H then S then H, and predict the final position before you click, using the matrices above.">
  <LazyBlochSphereExplorer />
</InteractiveSection>

## Worked Example

**Compute $Y|0\rangle$ by matrix multiplication, and confirm it lands on
the same Bloch point as $X|0\rangle$.**

$$
Y|0\rangle = \begin{pmatrix}0&-i\\i&0\end{pmatrix}\begin{pmatrix}1\\0\end{pmatrix} = \begin{pmatrix}0\\i\end{pmatrix} = i|1\rangle
$$

Bloch coordinates use $\alpha=0,\beta=i$:
$x=2\operatorname{Re}(\alpha^*\beta)=0$, $y=2\operatorname{Im}(\alpha^*\beta)=0$,
$z=|\alpha|^2-|\beta|^2 = 0-1=-1$. That's $(0,0,-1)$, the south pole,
exactly where $X|0\rangle=|1\rangle$ also lands. Same point, different
amplitudes: $i|1\rangle$ vs. plain $|1\rangle$.

## Common Mistakes

<Callout type="mistake">
**Assuming every gate is its own inverse.** $X,Y,Z,H$ are (applying any
of them twice gives back the identity), but $S$ and $T$ are not: $S$
needs to be applied four times, $T$ needs eight, to return to identity.
</Callout>

<Callout type="mistake">
**Reading matrix-vector multiplication in the wrong order.** $U|\psi\rangle$
means "apply $U$'s rows to $|\psi\rangle$'s column," the standard
matrix-vector product. If your arithmetic keeps producing the wrong
answer, write out $|\psi\rangle$ as an explicit column vector first.
</Callout>

## Practice Questions

1. Compute $X|+\rangle$ by matrix multiplication. Does the result match
   what you'd expect geometrically (recall $|+\rangle$ sits on the
   $x$-axis)?
2. Compute $Z|+\rangle$. What state is this, in terms of
   $|0\rangle,|1\rangle$?
3. Verify $S^2=Z$ by multiplying $S$'s matrix by itself.
4. Which gate(s) from this lesson leave $|0\rangle$ completely unchanged?
   Explain geometrically why that has to be true for those gates
   specifically.
5. Predict, without a calculator, what $T^8|1\rangle$ equals. Justify it
   from the rotation-angle picture, not matrix multiplication.

<PracticeLinks problems={practiceProblems} />

## Summary

- Gates must be unitary ($U^\dagger U = I$) so that valid states map to
  valid states. This is what makes them reversible.
- $X,Y,Z$ each rotate by $\pi$ about their respective axis; $H$ rotates by
  $\pi$ about the diagonal between $x$ and $z$; $S$ and $T$ rotate by
  $\pi/2$ and $\pi/4$ about $z$.
- $Y|0\rangle=i|1\rangle$ lands on the same Bloch point as $X|0\rangle=|1\rangle$,
  same rotation, different overall phase.

## Further Exploration

Next: [Single-Qubit Rotations](/lessons/quantum-computing/qubits-and-quantum-states/single-qubit-rotations), which generalizes every gate in this
lesson into one formula, a rotation by *any* angle about *any* axis,
and proves the claim that every single-qubit gate is secretly one of
these.
