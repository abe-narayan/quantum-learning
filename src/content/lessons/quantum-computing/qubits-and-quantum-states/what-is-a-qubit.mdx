import { LazyBlochSphereExplorer } from "@/components/simulators/bloch-sphere/LazyBlochSphereExplorer";
import { LazySuperpositionJourney } from "@/components/visualizations/superposition-journey/LazySuperpositionJourney";
import { PredictBeforeReveal } from "@/components/mdx/PredictBeforeReveal";
import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemsForLesson } from "@/lib/problems/registry";

export const practiceProblems = getProblemsForLesson(
  "quantum-computing/qubits-and-quantum-states/what-is-a-qubit"
).map((problem) => problem.meta);

export const lessonMeta = {
  title: "What Is a Qubit?",
  description:
    "Why quantum computers need a new unit of information, and what a qubit actually is mathematically.",
  course: "qubits-and-quantum-states",
  module: "what-is-a-qubit",
  order: 1,
  difficulty: "foundational",
  estimatedMinutes: 20,
  prerequisites: [],
  objectives: [
    "Explain why a classical bit can't describe a quantum system",
    "Write a qubit state in Dirac notation and check it's normalized",
    "Compute measurement probabilities from a qubit's amplitudes",
  ],
};

<LessonHook eyebrow="Why this matters">
A qubit doesn't have a definite value sitting in reserve before you measure it — it can be in an actual combination of "0" and "1" at once, a precise mathematical fact about the system, not a polite way of saying "we don't know which one it is yet."
</LessonHook>

## Motivation

Right now, every bit in your phone or laptop has a definite value. Nobody
needs to check it for that to be true: it's 0, or it's 1, sitting there
waiting, whether or not anything ever reads it. That assumption is so basic
to how we think about information that it's easy to forget it's an
assumption at all.

Shrink the question down to a single electron and the assumption breaks.
Physicists studying things like an electron's spin or a photon's
polarization found exactly that. A qubit is the unit of information
built to describe it.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/0/07/Google_Sycamore_Chip_001.png"
  alt="Google's Sycamore superconducting quantum processor chip, mounted with its array of individually wired qubits"
  caption="A real physical qubit: Google's Sycamore superconducting processor. Each of its metallic loops is one qubit, cooled to near absolute zero, whose state is exactly the α|0⟩ + β|1⟩ this lesson describes mathematically."
  credit="Google"
  creditUrl="https://commons.wikimedia.org/wiki/File:Google_Sycamore_Chip_001.png"
  license="CC BY 3.0"
/>

## Intuition

A useful (if imperfect) way to start: imagine a coin that hasn't landed yet.
While it's spinning in the air, it isn't heads, and it isn't tails. It's in
a mixture of both possibilities. A qubit's superposition is similar in
spirit, but stronger. A spinning coin's outcome is merely *unknown*: it
will turn out to have been heads or tails all along, once it lands. A
qubit's superposition is a system whose state really is a mathematical
combination of $|0\rangle$ and $|1\rangle$, and that combination has
consequences you can measure, like interference between the two
possibilities, that a "maybe heads, maybe tails" coin could never produce.

<Callout type="note">
Superposition is not classical uncertainty. A classical bit you haven't
looked at yet is still secretly 0 or 1. A qubit in superposition has no
definite value until it's measured. That's a physical fact, not a gap in
your knowledge.
</Callout>

## Mathematical Development

We write a qubit's state using <Term id="dirac-notation">Dirac notation</Term>. The two basis states are
written $|0\rangle$ and $|1\rangle$, corresponding to the classical bit
values 0 and 1. A general qubit state $|\psi\rangle$ is a linear combination
of these two:

<EquationReveal
  terms={[
    { id: "psi", symbol: "|ψ⟩", gloss: "The qubit's state — everything there is to know about it." },
    { id: "alpha", symbol: "α", gloss: "The amplitude on |0⟩ — a complex number, not a probability itself." },
    { id: "zero", symbol: "|0⟩", gloss: "The basis state corresponding to the classical bit value 0." },
    { id: "beta", symbol: "β", gloss: "The amplitude on |1⟩ — a complex number, not a probability itself." },
    { id: "one", symbol: "|1⟩", gloss: "The basis state corresponding to the classical bit value 1." },
  ]}
>
$$
|\psi\rangle = \alpha|0\rangle + \beta|1\rangle
$$
</EquationReveal>

where $\alpha$ and $\beta$ are complex numbers called <Term id="amplitude">amplitudes</Term>. For
this to describe a physically valid state, the amplitudes must satisfy the
normalization condition:

$$
|\alpha|^2 + |\beta|^2 = 1
$$

This condition exists because $|\alpha|^2$ and $|\beta|^2$ will turn out to
be *probabilities*, and probabilities have to add up to 1.

## Physical Interpretation

The connection between the math and what you can actually observe is the
<Term id="born-rule">Born rule</Term>: if you measure a qubit in state $|\psi\rangle = \alpha|0\rangle + \beta|1\rangle$,
you get outcome 0 with probability $|\alpha|^2$ and outcome 1 with
probability $|\beta|^2$. Measurement is the one place where the smooth,
continuous evolution of $\alpha$ and $\beta$ gives way to a single, random,
classical outcome: the superposition <Term id="wavefunction-collapse">collapses</Term>.

This is why normalization matters physically, not just mathematically: it's
exactly the statement that "probability of 0" plus "probability of 1" must
equal 1.

## See It Collapse

The Born rule is the most important idea in this lesson, so before moving on,
watch it happen. The tool below shows a single qubit's amplitudes turning
into probabilities in real time, and lets you measure it for real: each click
samples an outcome using exactly the probabilities shown, then snaps the
state to a single definite ket.

<InteractiveSection title="Amplitudes to probabilities to collapse" description="Set α with the slider — β is fixed by normalization. Then press Measure several times without touching the slider: you're re-preparing the same superposition each time, so the running tally shows just how random measurement really is, even though the probabilities never change.">
  <LazySuperpositionJourney />
</InteractiveSection>

## Interactive Visualization

Every qubit state can be pictured as a point on the surface of a sphere, the
<Term id="bloch-sphere-term">Bloch sphere</Term>, where the north pole is $|0\rangle$, the south pole is
$|1\rangle$, and every other point is some superposition of the two.

<PredictBeforeReveal
  question="Start from |0⟩ and apply a Hadamard gate (the gate below turns the vector to point straight out to the equator). If you then measure many times, what fraction of measurements will read |1⟩?"
  options={[
    { label: "0%", value: "0" },
    { label: "25%", value: "25" },
    { label: "50%", value: "50" },
    { label: "100%", value: "100" },
  ]}
  correctValue="50"
  explanation="A Hadamard gate takes |0⟩ to the equal superposition (1/√2)|0⟩ + (1/√2)|1⟩. By the Born rule, P(1) = |1/√2|² = 1/2, so measurement reads |1⟩ on about half of your attempts and |0⟩ on the other half, never something in between, even though before you measured, the state was a genuine combination of both."
/>

<InteractiveSection title="Apply a gate, then measure" description="Try it yourself: apply a gate below and watch α, β, and the measurement probabilities update as the vector moves. Then hit Measure a few times from the same state and notice the outcomes are random, but always land on 0 or 1 with exactly the probabilities shown.">
  <LazyBlochSphereExplorer />
</InteractiveSection>

## Worked Example

Consider the state:

$$
|\psi\rangle = \frac{1}{\sqrt{2}}|0\rangle + \frac{1}{\sqrt{2}}|1\rangle
$$

<DerivationSteps>
  <DerivationStep annotation="Both amplitudes are 1/√2, so squaring each and adding must give 1 for this to be a valid state.">
$$
\left|\frac{1}{\sqrt{2}}\right|^2 + \left|\frac{1}{\sqrt{2}}\right|^2 = \frac{1}{2} + \frac{1}{2} = 1 \checkmark
$$
  </DerivationStep>
  <DerivationStep annotation="The Born rule turns each amplitude into a measurement probability by squaring its size.">
$$
P(0) = \left|\frac{1}{\sqrt{2}}\right|^2 = \frac{1}{2}, \qquad P(1) = \left|\frac{1}{\sqrt{2}}\right|^2 = \frac{1}{2}
$$
  </DerivationStep>
</DerivationSteps>

So this qubit, when measured, gives 0 or 1 with equal 50% probability: an
**equal superposition**. This is exactly the state a Hadamard gate produces
from $|0\rangle$, which you'll meet in the next course.

## Common Mistakes

<Callout type="warning">
**Forgetting to square the amplitude.** The probability of an outcome is
$|\alpha|^2$, not $\alpha$ itself. For a real amplitude like $-\tfrac{1}{\sqrt2}$,
the probability is still a positive $\tfrac12$. The sign disappears once you
square it, but it isn't irrelevant: it affects how this state interferes with
others later on.
</Callout>

<Callout type="note">
**Treating superposition as "50/50 either way."** The coefficients don't have
to be equal. $|\psi\rangle = 0.6|0\rangle + 0.8i|1\rangle$ is a valid,
normalized qubit state ($0.6^2 + 0.8^2 = 1$) with a 36% chance of measuring 0
and a 64% chance of measuring 1.
</Callout>

## Practice Questions

1. Is $|\psi\rangle = \tfrac{1}{2}|0\rangle + \tfrac{\sqrt3}{2}|1\rangle$ a
   valid qubit state? Show your work.
2. A qubit has a 25% probability of measuring 0. Give one possible pair of
   amplitudes $(\alpha, \beta)$ consistent with this.
3. Why can't a qubit's amplitudes both be negative real numbers close to $-1$?

<PracticeLinks problems={practiceProblems} />

## Summary

- A classical bit is always 0 or 1; a qubit's state is a complex linear
  combination $\alpha|0\rangle + \beta|1\rangle$.
- Physical qubit states must be normalized: $|\alpha|^2 + |\beta|^2 = 1$.
- The Born rule connects amplitudes to measurement probabilities:
  $P(0) = |\alpha|^2$, $P(1) = |\beta|^2$.
- Measurement collapses the superposition to a single classical outcome.
- Every qubit state corresponds to a point on the Bloch sphere.

## Further Exploration

<NextDiscovery>
We've been using $\alpha$ and $\beta$ as complex numbers without really using
their complex structure yet: every example above could have used real
numbers. That changes fast. Next in this course: [Complex Numbers for
Quantum Mechanics](/lessons/quantum-computing/qubits-and-quantum-states/complex-numbers-for-quantum-mechanics), which builds the machinery (polar form, Euler's
formula) that phases like $e^{i\varphi}$ on the Bloch sphere actually need,
followed by [Dirac Notation](/lessons/quantum-computing/qubits-and-quantum-states/dirac-notation), which makes the bra-ket formalism precise.
</NextDiscovery>
