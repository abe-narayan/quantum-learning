import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemsForLesson } from "@/lib/problems/registry";
import { Complex } from "@/lib/quantum/complex";
import { StateVector } from "@/lib/quantum/state";
import { VectorDiagramExplorer } from "@/components/visualizations/VectorDiagramExplorer";

export const practiceProblems = getProblemsForLesson(
  "quantum-computing/qubits-and-quantum-states/dirac-notation"
).map((problem) => problem.meta);

export const zeroState = new StateVector([Complex.ONE, Complex.ZERO]);
export const oneState = new StateVector([Complex.ZERO, Complex.ONE]);
export const plusState = new StateVector([new Complex(Math.SQRT1_2), new Complex(Math.SQRT1_2)]);
export const plusZeroOverlap = plusState.innerProduct(zeroState).re;

export const mixingAngles = [0, Math.PI / 8, Math.PI / 4, (3 * Math.PI) / 8, Math.PI / 2];

export const mixingStates = mixingAngles.map(
  (theta) => new StateVector([new Complex(Math.cos(theta)), new Complex(Math.sin(theta))])
);

export const mixingFrames = mixingStates.map((psi, i) => ({
  paramLabel: `θ = ${Math.round((mixingAngles[i] * 180) / Math.PI)}°`,
  bars: [
    { label: "⟨0|ψ⟩", value: zeroState.innerProduct(psi).re, caption: zeroState.innerProduct(psi).re.toFixed(2) },
    { label: "⟨1|ψ⟩", value: oneState.innerProduct(psi).re, caption: oneState.innerProduct(psi).re.toFixed(2) },
  ],
}));

export const mixingVectorFrames = mixingStates.map((psi, i) => ({
  paramLabel: mixingFrames[i].paramLabel,
  vectors: [
    { label: "|0⟩", x: 1, y: 0, color: "muted" },
    { label: "|1⟩", x: 0, y: 1, color: "muted" },
    { label: "|ψ⟩", x: psi.amplitudes[0].re, y: psi.amplitudes[1].re, color: "brand" },
  ],
}));

export const lessonMeta = {
  title: "Dirac Notation",
  description:
    "Kets, bras, and the inner product, the compact algebra behind every quantum expression you'll write.",
  course: "qubits-and-quantum-states",
  module: "dirac-notation",
  order: 1,
  difficulty: "foundational",
  estimatedMinutes: 20,
  prerequisites: ["quantum-computing/qubits-and-quantum-states/complex-numbers-for-quantum-mechanics"],
  objectives: [
    "Explain what a bra is and how it relates to a ket",
    "Compute inner products between qubit states",
    "Use orthonormality to simplify bra-ket expressions",
  ],
  related: [
    {
      slug: "quantum-mechanics/mathematical-foundations/bra-ket-formalism",
      note: "The same ket/bra/inner-product notation developed for a general vector space, plus outer products and the completeness relation this lesson doesn't cover.",
    },
  ],
};

## Motivation

We've been writing $|0\rangle$, $|1\rangle$, and
$|\psi\rangle=\alpha|0\rangle+\beta|1\rangle$ since the first lesson without
ever asking what the $|\cdot\rangle$ symbol actually *means* as notation, or
what rules it follows. Time to make that precise. The notation itself isn't
deep, but pinning it down now makes everything after this lesson faster to
write and harder to get wrong.

## Intuition

$|\psi\rangle$ is called a **ket**, and it's just a name for "the vector
$\psi$." Physicists write vectors this way instead of with an arrow
($\vec{\psi}$) or bold type ($\mathbf{\psi}$) mostly by convention, but the
notation earns its keep the moment you introduce its partner, the **bra**
$\langle\psi|$. A bra and a ket combine to form
$\langle\phi|\psi\rangle$: read left-to-right, it looks like a
"bra-c-ket," which is exactly where the names come from. That combined
object is a single number (in general, complex), and it plays the role
ordinary dot products play for real vectors.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/5/50/Paul_Dirac%2C_1933.jpg"
  alt="Photograph of physicist Paul Dirac from 1933"
  caption="Paul Dirac (1902–1984), who invented the bra–ket notation this lesson is built around, photographed the year he shared the Nobel Prize in Physics for his foundational work in quantum mechanics."
  credit="Nobel Foundation / Wikimedia Commons"
  creditUrl="https://commons.wikimedia.org/wiki/File:Paul_Dirac,_1933.jpg"
  license="Public domain"
/>

## Mathematical Development

### From column vectors to kets

Recall the computational basis states as column vectors:

$$
|0\rangle = \begin{pmatrix}1\\0\end{pmatrix}, \qquad |1\rangle = \begin{pmatrix}0\\1\end{pmatrix}
$$

A general ket is a linear combination, $|\psi\rangle = \alpha|0\rangle+\beta|1\rangle = \begin{pmatrix}\alpha\\\beta\end{pmatrix}$.
Nothing new yet: $|\psi\rangle$ is just notation for this column vector.

### The bra as a conjugated row vector

The bra $\langle\psi|$ is defined as the **conjugate transpose** of the
ket: take the column vector, lay it on its side, and conjugate every
entry.

$$
|\psi\rangle = \begin{pmatrix}\alpha\\\beta\end{pmatrix} \quad\Longrightarrow\quad \langle\psi| = \begin{pmatrix}\alpha^* & \beta^*\end{pmatrix}
$$

The conjugation isn't optional decoration. It's exactly what makes the
next step work.

### The inner product

A bra times a ket is ordinary matrix multiplication of a row vector by a
column vector, which produces a single number:

$$
\langle\phi|\psi\rangle = \begin{pmatrix}\phi_0^* & \phi_1^*\end{pmatrix}\begin{pmatrix}\psi_0\\\psi_1\end{pmatrix} = \phi_0^*\psi_0 + \phi_1^*\psi_1
$$

Apply this to the basis states themselves. Since $|0\rangle=(1,0)$ and
$|1\rangle=(0,1)$:

$$
\langle0|0\rangle = 1, \quad \langle1|1\rangle = 1, \quad \langle0|1\rangle = 0, \quad \langle1|0\rangle = 0
$$

This is **orthonormality**: each basis state has unit overlap with itself
and zero overlap with the other. Plotting a few real inner products side
by side makes the pattern easy to spot: full overlap with yourself, zero
overlap with your orthogonal partner, and something in between for a
state that blends both.

<BarChart
  bars={[
    { label: "⟨0|0⟩", value: 1 },
    { label: "⟨1|1⟩", value: 1 },
    { label: "⟨0|1⟩", value: 0 },
    { label: "⟨+|0⟩", value: plusZeroOverlap, highlight: true }
  ]}
  ariaLabel="Inner products: basis states give 1 or 0, while a superposition state like plus gives a value in between"
/>

The highlighted bar is $\langle+|0\rangle$, worked out explicitly below:
neither 0 nor 1, because $|+\rangle$ blends both basis states, not
aligned with or orthogonal to either one. It's the reason
$|0\rangle$ and $|1\rangle$ are a sensible basis in the first place, and
it lets you compute any inner product without matrix multiplication at
all, just distribute and cancel:

$$
\langle\phi|\psi\rangle = (\phi_0^*\langle0| + \phi_1^*\langle1|)(\psi_0|0\rangle+\psi_1|1\rangle) = \phi_0^*\psi_0\langle0|0\rangle + \phi_0^*\psi_1\langle0|1\rangle + \phi_1^*\psi_0\langle1|0\rangle + \phi_1^*\psi_1\langle1|1\rangle
$$

The two cross terms vanish by orthonormality, leaving exactly
$\phi_0^*\psi_0+\phi_1^*\psi_1$, the same formula, derived rather than
just stated.

### Norm, revisited

Set $\langle\phi|=\langle\psi|$ (i.e. take the inner product of
$|\psi\rangle$ with itself):

$$
\langle\psi|\psi\rangle = \alpha^*\alpha + \beta^*\beta = |\alpha|^2+|\beta|^2
$$

The normalization condition from Lesson 1, $|\alpha|^2+|\beta|^2=1$, is now
just $\langle\psi|\psi\rangle=1$: one clean statement, in one notation,
that will hold for quantum states of any size, not just single qubits.

One important asymmetry: $\langle\phi|\psi\rangle$ and
$\langle\psi|\phi\rangle$ are **not** generally equal. They're complex
conjugates of each other, $\langle\psi|\phi\rangle = \langle\phi|\psi\rangle^*$.
Order matters.

## Physical Interpretation

$\langle\phi|\psi\rangle$ measures how much $|\psi\rangle$ "overlaps" with
$|\phi\rangle$, geometrically something like a (complex-valued) cosine of
the angle between them. When we get to measurement in a few lessons, this
overlap will turn out to be exactly the probability amplitude for finding
a system prepared in state $|\psi\rangle$ to instead look like $|\phi\rangle$
once measured. For now, treat orthonormality as the algebraic fact that
makes every other calculation in this course tractable: whenever a term
like $\langle0|1\rangle$ appears, it's zero, and whenever $\langle0|0\rangle$
appears, it's one.

## Interactive Experiment

The bar chart above showed four fixed overlap values. Sweep continuously
between $|0\rangle$ and $|1\rangle$ and watch the "something in between"
case fill in the whole range, not just one example of it.

<InteractiveSection title="Sweep the overlap continuously" description="Drag the slider to sweep ψ = cos(θ)|0⟩ + sin(θ)|1⟩ from θ = 0 (exactly |0⟩) to θ = 90° (exactly |1⟩). Watch ⟨0|ψ⟩ shrink smoothly from 1 to 0 while ⟨1|ψ⟩ grows from 0 to 1, the full continuum of overlap values orthonormality only showed you the two endpoints of.">
  <BarChartExplorer
    frames={mixingFrames}
    sliderLabel="Mixing angle θ"
    ariaLabel="Inner products of the zero and one basis states with psi equals cosine theta zero plus sine theta one, as theta sweeps from 0 to 90 degrees"
    maxValue={1}
  />
</InteractiveSection>

The bar heights above are exactly what "cosine of the angle between them" means once you draw $|0\rangle$ and $|1\rangle$ as fixed perpendicular axes and $|\psi\rangle$ as a vector between them: $\langle0|\psi\rangle$ and $\langle1|\psi\rangle$ are just $|\psi\rangle$'s projections onto those two axes, the same $\theta$ at every step.

<InteractiveSection title="The same sweep, drawn as a vector" description="Same θ, same five steps as the bar chart above. |0⟩ and |1⟩ sit fixed as perpendicular axes; |ψ⟩ swings between them as θ goes from 0 to 90°. Its projection onto the |0⟩ axis is ⟨0|ψ⟩ and its projection onto the |1⟩ axis is ⟨1|ψ⟩, the same numbers the bar chart plots, now visible as literal cosine and sine legs of the angle θ.">
  <VectorDiagramExplorer
    frames={mixingVectorFrames}
    sliderLabel="Mixing angle θ"
    ariaLabel="Vector psi swinging from the zero axis to the one axis as theta sweeps from 0 to 90 degrees, with its projections onto both fixed orthogonal axes"
  />
</InteractiveSection>

<PredictBeforeReveal
  question="⟨+|0⟩, where |+⟩ = (|0⟩+|1⟩)/√2: closer to the ⟨0|0⟩ = 1 case, the ⟨0|1⟩ = 0 case, or somewhere strictly between?"
  options={[
    { label: "Exactly 1, like ⟨0|0⟩", value: "one" },
    { label: "Exactly 0, like ⟨0|1⟩", value: "zero" },
    { label: "Strictly between 0 and 1", value: "between" },
  ]}
  correctValue="between"
  explanation="⟨+|0⟩ = 1/√2 ≈ 0.71, strictly between the two extremes, because |+⟩ is neither aligned with nor orthogonal to |0⟩: it blends both basis states equally. That's the same number the bar chart above already highlighted, and the worked example below derives it from scratch."
/>

## Worked Example

**Compute $\langle+|0\rangle$, where $|+\rangle = \frac{1}{\sqrt2}(|0\rangle+|1\rangle)$.**

$$
\langle+| = \frac{1}{\sqrt2}(\langle0|+\langle1|)
$$

(coefficients here are real, so conjugation doesn't change them)

$$
\langle+|0\rangle = \frac{1}{\sqrt2}(\langle0|0\rangle + \langle1|0\rangle) = \frac{1}{\sqrt2}(1 + 0) = \frac{1}{\sqrt2}
$$

This number, $\tfrac{1}{\sqrt2}$, will resurface directly in the
Measurement lesson: it's the probability *amplitude* for measuring $|+\rangle$
and finding "0," and squaring it gives the actual probability, $\tfrac12$.

## Common Mistakes

<Callout type="mistake">
**Forgetting to conjugate the bra's coefficients.** If $|\phi\rangle$ has a
complex coefficient like $i$, the bra's corresponding entry is $-i$, not
$i$. Skipping this silently flips signs in the imaginary part of your
answer.
</Callout>

<Callout type="mistake">
**Assuming $\langle\phi|\psi\rangle=\langle\psi|\phi\rangle$.** They're
complex conjugates of each other. They're only equal when the inner
product happens to be real.
</Callout>

## Practice Questions

1. Compute $\langle-|1\rangle$, where $|-\rangle=\frac{1}{\sqrt2}(|0\rangle-|1\rangle)$.
2. Let $|\psi\rangle = \frac{3}{5}|0\rangle + \frac{4i}{5}|1\rangle$. Verify
   $\langle\psi|\psi\rangle=1$ directly from the inner-product formula.
3. Show that $\langle+i|-i\rangle = 0$, where
   $|{+i}\rangle=\frac{1}{\sqrt2}(|0\rangle+i|1\rangle)$ and
   $|{-i}\rangle=\frac{1}{\sqrt2}(|0\rangle-i|1\rangle)$. (These two states
   are orthogonal, just like $|0\rangle$ and $|1\rangle$: there's more than
   one orthonormal basis for a qubit.)
4. If $\langle\phi|\psi\rangle = 3-4i$, what is $\langle\psi|\phi\rangle$?

<PracticeLinks problems={practiceProblems} />

## Summary

- $|\psi\rangle$ is a ket (column vector); $\langle\psi|$ is its bra
  (conjugate-transpose row vector).
- The inner product $\langle\phi|\psi\rangle=\phi_0^*\psi_0+\phi_1^*\psi_1$
  is a single complex number measuring overlap.
- $\langle0|0\rangle=\langle1|1\rangle=1$ and $\langle0|1\rangle=\langle1|0\rangle=0$:
  the computational basis is orthonormal.
- $\langle\psi|\psi\rangle=1$ is the normalization condition, restated.
- $\langle\phi|\psi\rangle=\langle\psi|\phi\rangle^*$: order matters.

## Further Exploration

Next: [Quantum States and State Vectors](/lessons/quantum-computing/qubits-and-quantum-states/quantum-states-and-state-vectors), where we use this notation to
state the actual postulates governing which vectors count as physical
qubit states, and derive exactly how many real numbers it takes to
describe one.
