import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemMetaForLesson } from "@/lib/problems/metaRegistry";
import { InteractiveSection } from "@/components/mdx/InteractiveSection";
import { BarChartExplorer } from "@/components/visualizations/BarChartExplorer";
import { uniformSuperposition, applyCostUnitary, applyMixerUnitary, expectedCutSize } from "@/lib/quantum/qaoa";
import { GraphDiagram } from "@/components/visualizations/GraphDiagram";

export const practiceProblems = getProblemMetaForLesson(
  "quantum-computing/quantum-algorithms-ii/qaoa-and-combinatorial-optimization"
);

export const mixerDemoEdges = [[0, 1], [0, 2], [1, 2]];
export const mixerDemoGamma = Math.PI / 4;
export const mixerDemoBetas = [0, Math.PI / 8, Math.PI / 4, (3 * Math.PI) / 8, Math.PI / 2, (5 * Math.PI) / 8, (3 * Math.PI) / 4, (7 * Math.PI) / 8, Math.PI];
export const mixerDemoBitLabel = (index) => index.toString(2).padStart(3, "0");
export const mixerDemoStates = mixerDemoBetas.map((beta) => applyMixerUnitary(applyCostUnitary(uniformSuperposition(3), mixerDemoEdges, mixerDemoGamma), beta));
export const mixerDemoFrames = mixerDemoStates.map((state, i) => ({
  paramLabel: `β = ${mixerDemoBetas[i].toFixed(3)}, ⟨cut⟩ = ${expectedCutSize(state, mixerDemoEdges).toFixed(3)}`,
  bars: state.probabilities().map((p, idx) => ({
    label: mixerDemoBitLabel(idx),
    value: p,
    highlight: idx !== 0 && idx !== 7
  }))
}));
export const mixerDemoMaxProb = Math.max(...mixerDemoFrames.flatMap((f) => f.bars.map((b) => b.value)));

export const lessonMeta = {
  title: "QAOA and Combinatorial Optimization",
  description:
    "Max-Cut as a diagonal cost Hamiltonian, the alternating cost/mixer circuit, and why the resulting expectation value is exactly the expected number of cut edges, derived and checked exactly on the uniform superposition, where the answer is knowable in advance.",
  course: "quantum-algorithms-ii",
  module: "qaoa-and-combinatorial-optimization",
  order: 1,
  difficulty: "advanced",
  estimatedMinutes: 25,
  prerequisites: ["quantum-computing/quantum-algorithms-ii/vqe-a-worked-toy-example"],
  objectives: [
    "Formulate Max-Cut as a cost Hamiltonian C=Σ(1-Z_iZ_j)/2 over graph edges",
    "Derive the cost unitary's action as a pure phase e^{-iγ·(cut count)} on each basis state",
    "Explain the mixer's role and verify the uniform superposition gives exactly half of every edge cut in expectation",
  ],
};

<LessonHook eyebrow="Splitting a graph, one phase at a time">
Max-Cut asks you to split a graph's vertices into two groups so that as many edges as possible cross between them. It is NP-hard, and QAOA attacks it with the same hybrid loop VQE uses for ground energies.
</LessonHook>

## Motivation

**<Term id="qaoa">QAOA</Term>** (the quantum approximate optimization algorithm) is VQE's hybrid
loop, a parameterized circuit with a classically-optimized cost, pointed at
combinatorial optimization instead of ground-state energy.

The retargeting takes one idea: write the thing you want to maximize as a
Hamiltonian whose eigenvalue on each candidate answer *is* that answer's
score. Once you have that, "find the best coloring" and "find the lowest
energy" are the same problem, and VQE's machinery transfers unchanged.
Max-Cut is the standard first example, and the encoding takes two lines.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/9/98/A_Wafer_of_the_Latest_D-Wave_Quantum_Computers_%2839188583425%29.jpg"
  alt="A wafer of D-Wave quantum computing processors"
  caption="A processor wafer from D-Wave, a company whose quantum-annealing hardware natively encodes Ising-model optimization problems structurally related to the Max-Cut cost Hamiltonian derived below, though by a different physical mechanism than QAOA's gate-based circuit."
  credit="Steve Jurvetson"
  creditUrl="https://commons.wikimedia.org/wiki/File:A_Wafer_of_the_Latest_D-Wave_Quantum_Computers_(39188583425).jpg"
  license="CC BY 2.0"
/>

## Mathematical Development

### Max-Cut as a Hamiltonian

Represent a 2-coloring of $n$ vertices as a computational basis state
$|z\rangle$, $z\in\{0,1\}^n$. For each edge $(i,j)$, define
$Z_i,Z_j$'s eigenvalues as $+1$ (bit 0) or $-1$ (bit 1). Then
$\frac{1-Z_iZ_j}{2}$ equals $1$ exactly when $i,j$ have *different* bits
(the edge is cut) and $0$ when they match. Summing over every edge:

$$
C = \sum_{(i,j)\in E}\frac{1-Z_iZ_j}{2}
$$

is a <Term id="hamiltonians-time-evolution">Hamiltonian</Term> whose eigenvalue on $|z\rangle$ is exactly the number of
edges cut by that particular 2-coloring. Maximizing $\langle C\rangle$ is
literally Max-Cut.

For a 3-vertex triangle graph (every pair connected, 3 edges total), the
coloring $z=100$ (vertex 0 in one group, vertices 1 and 2 in the other)
cuts exactly 2 of the 3 edges:

<GraphDiagram
  nodes={[
    { id: "0", x: 0.5, y: 0.1, group: 0 },
    { id: "1", x: 0.15, y: 0.9, group: 1 },
    { id: "2", x: 0.85, y: 0.9, group: 1 }
  ]}
  edges={[
    { from: "0", to: "1", cut: true },
    { from: "0", to: "2", cut: true },
    { from: "1", to: "2", cut: false }
  ]}
  ariaLabel="Triangle graph with vertex 0 in one group and vertices 1,2 in the other, showing 2 of 3 edges cut"
/>

Edges 0-1 and 0-2 cross between groups (dashed, cut); edge 1-2 sits
entirely inside one group (solid, not cut). $\langle z|C|z\rangle=2$ for
this particular coloring, read directly off the picture by counting
dashed edges.

### The cost unitary is a pure phase

$C$ is diagonal in the computational basis (built entirely from $Z$
operators), so $U_C(\gamma)=e^{-i\gamma C}$ acts on each basis state by a
phase equal to its own eigenvalue:

$$
U_C(\gamma)|z\rangle = e^{-i\gamma\cdot(\text{cut count of }z)}|z\rangle
$$

`applyCostUnitary` implements exactly this, computing each basis index's
cut count directly (summing $1$ for every edge whose two endpoint bits
differ) and multiplying by the corresponding phase, with **no change to
any measurement probability** (a pure phase operator, checked directly:
probabilities before and after `applyCostUnitary` match to $10^{-9}$).

### The mixer

$U_C$ alone can't change which outcomes are likely. It only adds phases.
The **mixer** $U_B(\beta)=e^{-i\beta B}$, $B=\sum_iX_i$, is what actually
moves amplitude between basis states. Because $B$ is a sum of single-qubit
$X$ terms acting on different qubits, those terms commute and $e^{-i\beta
B}$ factors into a tensor product of single-qubit exponentials
$e^{-i\beta X_i}$, one per qubit. With this platform's rotation
convention $R_x(\theta)=e^{-i\theta X/2}$, each factor $e^{-i\beta X_i}$
is exactly $R_x(2\beta)$ on qubit $i$, so $U_B(\beta)$ is implemented as
$R_x(2\beta)$ applied independently to every qubit (`applyMixerUnitary`),
reusing the ordinary single-qubit rotation gate, not a new primitive. Alternating $U_C$ and
$U_B$ for $p$ layers, starting from the uniform superposition
(`qaoaCircuit`), is the full QAOA circuit; $p=1$ is the smallest
nontrivial case this course's worked example uses.

<InteractiveSection title="The mixer's pull is periodic, not one-directional" description="Slide β from 0 to π. At β=0 the mixer is the identity, so the state is the cost-phased uniform superposition: still flat at 1/8 on every basis state, with ⟨cut⟩ locked at the baseline 1.5. β is a rotation angle, not a dial that keeps helping the further you turn it. ⟨cut⟩ climbs to a peak at β=π/8 (⟨cut⟩=1.875, amplitude pulled onto the six 1-vs-2 split colorings, highlighted), then reverses and bottoms out at β=3π/8 (⟨cut⟩=0.375, weight pushed back onto the two uncut colorings 000 and 111), returns to the baseline at β=π/2, and repeats the cycle on the way to π. Same overshoot lesson as Grover.">
  <BarChartExplorer
    frames={mixerDemoFrames}
    sliderLabel="mixer angle β (γ fixed at π/4)"
    ariaLabel="Measurement probability over all 8 triangle colorings after one cost layer (fixed γ=π/4) and one mixer layer, as the mixer angle β varies"
    maxValue={mixerDemoMaxProb}
  />
</InteractiveSection>

### A checkable baseline

<PredictBeforeReveal
  question="Before any cost or mixer layers run, on the plain uniform superposition, what's the expected cut size for the 3-edge triangle graph?"
  options={[
    { label: "0, since there is no bias toward any coloring yet", value: "zero" },
    { label: "1.5, half of the 3 edges on average", value: "half" },
    { label: "2, the true maximum, by symmetry", value: "max" },
    { label: "3, every edge cut on average", value: "all" },
  ]}
  correctValue="half"
  explanation="Every basis state is equally likely at probability 1/8, and for any single edge exactly half of all colorings cut it. Summed over the triangle's 3 edges the expected cut size is 3 × 0.5 = 1.5, matching expectedCutSize's output on the uniform superposition below. Note that this baseline is also what random guessing achieves, so it is the number every QAOA result in this lesson and the next has to beat before it has shown anything."
/>

Before running any optimization, check the *starting point*: the uniform
superposition itself, with no cost/mixer layers applied at all, gives
every basis state equal probability $1/2^n$, so the expected cut size is
just the average over *all* $2^n$ colorings. For any single edge, exactly
half of all colorings cut it (matching the endpoints' bits differently)
and half don't, so the expected cut count from the uniform superposition
alone is exactly $|E|/2$, half the edge count, for *any* graph. For a
3-edge triangle: `expectedCutSize` on the plain uniform superposition
gives exactly $1.5$, confirmed directly, not just argued.

## Physical Interpretation

QAOA's cost unitary "tags" each candidate coloring with a phase
proportional to its quality, and the mixer lets those phases <Term id="phase-interference">interfere</Term>.
This is structurally the same idea as Grover's oracle-then-diffusion
pattern, now applied to a continuous-valued cost instead of a single
marked item, and tuned by a classical optimizer choosing $\gamma,\beta$
rather than a fixed formula.

## Worked Example

**Confirm the uniform-superposition baseline for a single edge (2 nodes,
1 edge): expected cut size should be exactly $0.5$.**

Two colorings cut the edge ($|01\rangle,|10\rangle$), two don't
($|00\rangle,|11\rangle$), each with probability $0.25$ in the uniform
superposition. Expected cut size
$=0.25(0)+0.25(1)+0.25(1)+0.25(0)=0.5$, exactly half of the single edge,
matching the general $|E|/2$ rule and the engine's direct output.

## Common Mistakes

<Callout type="warning">
**Expecting the cost unitary alone to improve the measured cut size.**
It is a pure phase operator, so measurement probabilities, and with them
the expected cut size, are unchanged by $U_C$ alone. Improvement happens
only once the mixer converts those phases into amplitude differences.
</Callout>

<Callout type="warning">
**Assuming QAOA always finds the exact maximum cut.** It is a heuristic,
approximate algorithm with no optimality guarantee. The next lesson's
worked example shows $p=1$ QAOA landing close to but not on the true
maximum, and adding layers ($p>1$) usually helps without promising to.
</Callout>

## Practice Questions

1. Compute the expected cut size of the uniform superposition for a
   4-edge graph, using the $|E|/2$ rule.
2. Explain why $U_C(\gamma)$'s action can be computed directly from a
   basis state's index alone (no matrix multiplication needed), using its
   diagonal structure.
3. For a single edge, write out $U_C(\gamma)$'s phase on each of the 4
   basis states explicitly ($|00\rangle,|01\rangle,|10\rangle,|11\rangle$).
4. Why is $B=\sum_iX_i$ (independent single-qubit rotations) a reasonable
   choice of mixer, given that the goal is to explore *different*
   colorings rather than stay fixed at one?

<PracticeLinks problems={practiceProblems} />

## Summary

- Max-Cut's cost Hamiltonian $C=\sum(1-Z_iZ_j)/2$ has eigenvalue equal to
  the cut count, for any coloring.
- The cost unitary is a pure phase operator (verified: leaves every
  probability unchanged); the mixer is what actually moves amplitude.
- The uniform superposition's expected cut size is exactly $|E|/2$ for
  any graph, a checkable baseline before any optimization runs.

## Further Exploration

<NextDiscovery>
[QAOA: A Worked Max-Cut Example](/lessons/quantum-computing/quantum-algorithms-ii/qaoa-a-worked-max-cut-example) runs $p=1$ QAOA on a
triangle graph and a single edge, both reaching within a few percent of
the true optimum.
</NextDiscovery>
