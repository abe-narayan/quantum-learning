import { Complex } from "@/lib/quantum/complex";
import { StateVector } from "@/lib/quantum/state";
import { applySingleQubitGate, HADAMARD } from "@/lib/quantum/gates";
import { QuantumStateDisplay } from "@/components/quantum/QuantumStateDisplay";
import { BarChartExplorer } from "@/components/visualizations/BarChartExplorer";
import { InteractiveSection } from "@/components/mdx/InteractiveSection";
import { PredictBeforeReveal } from "@/components/mdx/PredictBeforeReveal";
import { ExternalFigure } from "@/components/mdx/ExternalFigure";
import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemsForLesson } from "@/lib/problems/registry";

export const practiceProblems = getProblemsForLesson(
  "quantum-computing/quantum-algorithms-i/quantum-parallelism-and-the-oracle-model"
).map((problem) => problem.meta);

export const lessonMeta = {
  title: "Quantum Parallelism and the Oracle Model",
  description:
    "Applying a Hadamard to every qubit puts every possible input into superposition at once, and why that alone doesn't let you read out a function's values on all of them, setting up exactly the problem the rest of this course solves.",
  course: "quantum-algorithms-i",
  module: "quantum-parallelism-and-the-oracle-model",
  order: 1,
  difficulty: "advanced",
  estimatedMinutes: 25,
  prerequisites: ["quantum-computing/entanglement-and-measurement/capstone-analyzing-quantum-correlations"],
  objectives: [
    "Derive the result of applying a Hadamard to every qubit of the all-zeros state: the uniform superposition over all n-bit strings",
    "State the reversible oracle model, where a query maps input x and output register y to x and y XOR f(x), and explain why it's required",
    "Explain precisely why superposition alone doesn't let you extract all of f's values from one query",
  ],
};

export const twoQubitPlus = new StateVector([new Complex(0.5), new Complex(0.5), new Complex(0.5), new Complex(0.5)]);

export const parallelismQubitCounts = [1, 2, 3, 4];

export const parallelismStates = parallelismQubitCounts.map((n) =>
  Array.from({ length: n }, (_, q) => q).reduce(
    (s, q) => applySingleQubitGate(s, HADAMARD, q),
    StateVector.zero(n)
  )
);

export const parallelismFrames = parallelismQubitCounts.map((n, i) => ({
  paramLabel: `n = ${n} qubit${n === 1 ? "" : "s"} (${2 ** n} branches)`,
  bars: parallelismStates[i].probabilities().map((p, x) => ({
    label: `|${parallelismStates[i].basisLabel(x)}⟩`,
    value: p,
    caption: `${(p * 100).toFixed(1)}%`,
  })),
}));

## Motivation

Every algorithm in this course starts from the same trick: put a register into
superposition over every possible input, apply some computation once, and
somehow get useful information out at the end. The "somehow" is the entire
subject: quantum parallelism sounds like it should give free exponential
speedups, and it does not, for a reason worth understanding precisely before
building anything on top of it.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/thumb/9/9f/IBM_QC.jpg/1280px-IBM_QC.jpg"
  alt="The dilution-refrigerator cryostat housing IBM's 50-qubit superconducting quantum computing prototype, displayed at CES 2018"
  caption="A dilution-refrigerator cryostat: real hardware that physically hosts the qubit register this lesson puts into superposition. Its nested gold stages cool the superconducting chip inside to near absolute zero so the H^⊗n step derived below actually holds."
  credit="Lars Plougmann / Wikimedia Commons"
  creditUrl="https://commons.wikimedia.org/wiki/File:IBM_QC.jpg"
  license="CC BY-SA 2.0"
/>

## Mathematical Development

### H^⊗n creates the uniform superposition

Applying $H$ to a single qubit gives
$H|0\rangle=\frac{1}{\sqrt2}(|0\rangle+|1\rangle)$. Applying $H$ to *each*
qubit of an $n$-qubit register independently, starting from
$|0\rangle^{\otimes n}$, gives a tensor product of $n$ such states:

$$
H^{\otimes n}|0\rangle^{\otimes n} = \bigotimes_{i=1}^{n}\frac{1}{\sqrt2}(|0\rangle+|1\rangle) = \frac{1}{\sqrt{2^n}}\sum_{x=0}^{2^n-1}|x\rangle
$$

Expanding the tensor product term by term reproduces every possible $n$-bit
string exactly once, each with amplitude $1/\sqrt{2^n}$, a direct
consequence of distributing the product over the sum in each factor, not a
separate fact about "parallelism." For $n=2$:

<QuantumStateDisplay state={twoQubitPlus} label="H⊗H|00⟩: all four 2-bit strings, equal amplitude" />

### The oracle model

To use this superposition to say anything about a function $f$, you need a
way to apply $f$ *quantum mechanically*. A classical function is often
irreversible ($\text{AND}(0,0)=\text{AND}(0,1)=0$: two different inputs, one
output, no way to run it backward), but every quantum gate must be unitary,
hence reversible. The standard fix is the **oracle model**: represent $f$ as
a unitary $U_f$ acting on an input register $|x\rangle$ plus one extra output
qubit $|y\rangle$:

$$
U_f|x\rangle|y\rangle = |x\rangle|y\oplus f(x)\rangle
$$

This is reversible for *any* $f$, including non-invertible ones: applying
$U_f$ twice returns the original state exactly, since $y\oplus f(x)\oplus
f(x) = y$ (XOR-ing the same value twice cancels). `applyBitOracle` implements
exactly this, and this involution property is checked directly in its tests.

### Why superposition alone isn't enough

Apply $U_f$ to $H^{\otimes n}|0\rangle^{\otimes n}\,|0\rangle$:

$$
U_f\left(\frac{1}{\sqrt{2^n}}\sum_x|x\rangle\right)|0\rangle = \frac{1}{\sqrt{2^n}}\sum_x|x\rangle|f(x)\rangle
$$

Every value of $f$ really is encoded in this one state; that part of
"quantum parallelism" is real.

<PredictBeforeReveal
  question="The state above has a term |x⟩|f(x)⟩ for every possible input x, all at once. If you measure it right now, what do you get?"
  options={[
    { label: "The full table of every f(x), all 2^n values", value: "full-table" },
    { label: "About half of the f(x) values, chosen at random", value: "half" },
    { label: "Exactly one (x, f(x)) pair, chosen at random", value: "one" },
    { label: "Nothing useful — the state is too scrambled to measure", value: "nothing" },
  ]}
  correctValue="one"
  explanation="Measurement collapses the superposition to exactly one basis state, so you get exactly one (x, f(x)) pair, with probability 1/2^n for each possible x. Every other value of f that was sitting in the superposition a moment earlier is simply gone once you look. This is the entire reason 'quantum parallelism' needs a second trick (interference) to be useful at all."
/>

But **measuring this state collapses it to
one random $x$ and its one value $f(x)$**, with probability $1/2^n$ each,
exactly like measuring any other uniform superposition. You learn $f$ at one
point, chosen randomly, having spent the same one query a classical computer
would have spent to learn one point. No speedup has been extracted yet:
superposition alone gives you a single random sample, dressed up. For the
$n=2$ case above, measurement collapses the four-branch superposition to
exactly one of them, each equally likely:

<MeasurementTree
  root={{
    label: "H⊗H|00⟩",
    children: [
      { label: "|00⟩, f(00)", probability: 0.25 },
      { label: "|01⟩, f(01)", probability: 0.25 },
      { label: "|10⟩, f(10)", probability: 0.25 },
      { label: "|11⟩, f(11)", probability: 0.25 }
    ]
  }}
  ariaLabel="Measurement tree showing the uniform superposition over 4 inputs collapsing to exactly one random branch, each with probability 25%"
/>

Only one of these four branches survives measurement. The other three
values of $f$, still sitting in the pre-measurement superposition a moment
earlier, are gone the instant you look.

The $n=2$ case above is just one point on a pattern that holds for any
register size: $H^{\otimes n}|0\rangle^{\otimes n}$ always spreads equally
over $2^n$ branches, each with probability exactly $1/2^n$, real numbers
from `applySingleQubitGate` applied $n$ times in a row, not just algebra:

<InteractiveSection
  title="2ⁿ branches, each shrinking as n grows"
  description="Scrub n from 1 to 4 qubits and watch H applied to every qubit spread the state across exactly 2 to the n equally likely branches, each shrinking as n grows. This is the real probability distribution computed from applySingleQubitGate, not a picture: it is exactly why quantum parallelism alone is not a speedup, one measurement still reveals only one of these branches no matter how many there are."
>
  <BarChartExplorer
    frames={parallelismFrames}
    sliderLabel="Number of qubits (n)"
    ariaLabel="Uniform superposition probability distribution over 2 to the n computational basis states, for n from 1 to 4 qubits, each outcome equally likely"
    maxValue={1}
  />
</InteractiveSection>

Every bar in every frame sits at exactly $1/2^n$: doubling $n$ halves each
branch's share of the total probability, even as the number of branches
doubles. Total probability always sums to $1$; only its distribution across
an exponentially larger set of branches changes.

The entire content of Deutsch-Jozsa, phase estimation, and Grover's algorithm
is a specific way to make the *interference* between these $2^n$ branches
concentrate the measurement probability onto an answer that reveals a
**global** property of $f$ (constant vs. balanced; a period; a marked item)
that no single query (classical or quantum) could reveal on its own. That
constructive/destructive interference step, not the superposition step, is
where the real work happens.

## Physical Interpretation

"Quantum computers try every input at once" is the half of the story that
makes headlines and the half that, alone, is useless: a superposition over
$2^n$ branches is not $2^n$ independent computations you get to read out;
it's one probabilistic system that gives you exactly one measurement's
worth of information, same as always. Every algorithm in this course earns
its speedup by arranging interference deliberately, not by superposition
alone.

## Worked Example

**A classical algorithm queries $f:\{0,1\}^2\to\{0,1\}$ on all 4 inputs to
learn the full truth table. Show that querying $U_f$ once on a uniform
superposition gives a state that, if measured immediately, reveals no more
than one classical query would.**

$U_f\left(\frac{1}{2}\sum_{x=0}^3|x\rangle\right)|0\rangle =
\frac{1}{2}\sum_{x=0}^3|x\rangle|f(x)\rangle$. Measuring this state collapses
it to one specific $x$ (each with probability $1/4$) together with the
correct $f(x)$, exactly one $(x,f(x))$ pair, the same information a single
classical query $f(x)$ would give for a chosen $x$. The other three values of
$f$ are still completely unknown after this one query, quantum or classical.

## Common Mistakes

<Callout type="mistake">
**Believing one query to $U_f$ on a superposition reveals the full truth
table.** It doesn't: measurement still collapses to one branch. The
speedups in this course come from clever interference *before* measurement,
never from reading out multiple branches at once.
</Callout>

<Callout type="mistake">
**Treating the oracle model as free.** $U_f$ is assumed to exist as a given
unitary; building one for a specific real-world $f$ can itself be
expensive. Query complexity (how many calls to $U_f$ an algorithm needs) is
what these algorithms actually bound, not the cost of constructing $U_f$
itself.
</Callout>

## Practice Questions

1. Write out $H^{\otimes 3}|000\rangle$ as a sum over all 3-bit strings, and
   confirm every amplitude has the same magnitude.
2. Explain, using $U_f$'s definition, why $U_f$ applied twice in a row always
   returns the original state, for *any* function $f$, not just invertible
   ones.
3. A function $f:\{0,1\}^3\to\{0,1\}$ is queried once via $U_f$ on a uniform
   superposition, then measured immediately. What is the probability the
   measured $x$ equals a specific value you care about in advance?
4. Explain in one or two sentences why "quantum parallelism" is a misleading
   name if taken to mean "computes every output simultaneously and readably."

<PracticeLinks problems={practiceProblems} />

## Summary

- $H^{\otimes n}|0\rangle^{\otimes n}=\frac{1}{\sqrt{2^n}}\sum_x|x\rangle$:
  the uniform superposition over every $n$-bit string, derived directly from
  distributing the tensor product.
- The oracle model $U_f|x\rangle|y\rangle=|x\rangle|y\oplus f(x)\rangle$
  makes any function reversible, hence implementable as a unitary.
- A superposition carrying every value of $f$ still collapses to one
  random sample upon measurement, no speedup without deliberate
  interference.
- Every algorithm in this course is a specific interference pattern
  designed to extract a *global* property of $f$ that a single query
  otherwise couldn't reveal.

## Further Exploration

Next: [Phase Kickback](/lessons/quantum-computing/quantum-algorithms-i/phase-kickback), the single algebraic trick (encoding $f(x)$'s
value as a phase rather than a separate qubit's value) that every algorithm
in this course reuses.
