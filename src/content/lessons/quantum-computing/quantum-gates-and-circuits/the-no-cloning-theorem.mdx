import { QuantumStateDisplay } from "@/components/quantum/QuantumStateDisplay";
import { StateVector } from "@/lib/quantum/state";
import { Complex } from "@/lib/quantum/complex";
import { applySingleQubitGate, applyCNOT, HADAMARD } from "@/lib/quantum/gates";
import { LazyTwoQubitExplorer } from "@/components/simulators/two-qubit-explorer/LazyTwoQubitExplorer";
import { PredictBeforeReveal } from "@/components/mdx/PredictBeforeReveal";
import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemMetaForLesson } from "@/lib/problems/metaRegistry";

export const practiceProblems = getProblemMetaForLesson(
  "quantum-computing/quantum-gates-and-circuits/the-no-cloning-theorem"
);

export const plusSingle = new StateVector([new Complex(Math.SQRT1_2), new Complex(Math.SQRT1_2)]);

export const actualCnotOutput = applyCNOT(
  applySingleQubitGate(StateVector.zero(2), HADAMARD, 0),
  0,
  1
);

export const hypotheticalCloneOutput = plusSingle.tensor(plusSingle);

export const lessonMeta = {
  title: "The No-Cloning Theorem",
  description:
    "A complete proof by contradiction: no unitary can copy an arbitrary, unknown qubit state, derived, not asserted.",
  course: "quantum-gates-and-circuits",
  module: "the-no-cloning-theorem",
  order: 1,
  difficulty: "intermediate",
  estimatedMinutes: 20,
  prerequisites: ["quantum-computing/quantum-gates-and-circuits/multi-qubit-measurement"],
  objectives: [
    "State the no-cloning theorem precisely",
    "Reproduce the proof by contradiction using linearity",
    "Explain why copying a known basis state is not a counterexample",
  ],
};

<LessonHook eyebrow="Why this matters">
No quantum device can copy an unknown qubit's state onto a blank one. Not because nobody has found a clever enough circuit, but because linearity, which every quantum gate has, forbids it.
</LessonHook>

## Motivation

Why can't you copy a qubit? Copying is the most ordinary operation in
classical computing. You do it constantly without thinking about it, and
`ctrl-C` has never once failed on the grounds that the data was in an
unknown state.

The answer turns on linearity, a property every quantum gate has had since
the moment you learned gates are matrices. The proof is four lines, and it
is worth following closely: no-cloning is the load-bearing fact behind
quantum cryptography, and it is the reason teleportation has to destroy
its original.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/f/fd/Wojciech_H._Zurek.gif"
  alt="Portrait photograph of physicist Wojciech H. Zurek"
  caption="Wojciech Zurek, of Los Alamos National Laboratory, who with William Wootters published the linearity argument reproduced below in a 1982 paper titled, memorably, 'A single quantum cannot be cloned.'"
  credit="Los Alamos National Laboratory"
  creditUrl="https://commons.wikimedia.org/wiki/File:Wojciech_H._Zurek.gif"
  license="Public Domain (LANL)"
/>

<HistoricalMoment date="1982">
Wojciech Zurek and William Wootters derive the no-cloning theorem, independently of a related result by Dennis Dieks the same year. Their linearity argument is the one this lesson walks through.
</HistoricalMoment>

## Intuition

A "cloning machine" would need to be a single, fixed device that works
for *any* input state, since the whole point is you don't know the state
in advance (if you already knew it, you could just prepare a second copy
yourself with no machine needed). But a fixed quantum device is a fixed
<Term id="unitary-operator">unitary</Term> matrix, and matrices are linear. Linear operations are
extremely constrained: what they do to a superposition is *completely
determined* by what they do to the pieces of that superposition
separately. That single fact is enough to break cloning.

## Mathematical Development

### Setting up the claim

Suppose a cloning unitary $U$ existed: for *any* single-qubit state
$|\psi\rangle$, acting on $|\psi\rangle$ together with a fixed "blank"
qubit $|0\rangle$ produces two copies:

$$
U(|\psi\rangle\otimes|0\rangle) = |\psi\rangle\otimes|\psi\rangle \qquad \text{for every } |\psi\rangle
$$

This has to hold for *every* single-qubit state, since $U$ is one fixed
device with no way to know in advance which $|\psi\rangle$ it's been
handed.

### Testing it on two specific inputs

Apply the assumption to $|0\rangle$ and to $|1\rangle$ individually:

$$
U(|0\rangle\otimes|0\rangle) = |0\rangle\otimes|0\rangle, \qquad U(|1\rangle\otimes|0\rangle) = |1\rangle\otimes|1\rangle
$$

Nothing wrong yet. A device that measures the input in the <Term id="computational-basis">computational
basis</Term> and prepares a matching copy achieves this much, and such devices
exist.

### The contradiction: apply it to a superposition

Now apply the *same* assumption to $|+\rangle=\frac{1}{\sqrt2}(|0\rangle+|1\rangle)$.
If cloning works for every state, it must work for this one too, giving
$U(|+\rangle\otimes|0\rangle)=|+\rangle\otimes|+\rangle$. But $U$ is
linear, and $|+\rangle\otimes|0\rangle=\frac{1}{\sqrt2}(|0\rangle+|1\rangle)\otimes|0\rangle=\frac{1}{\sqrt2}(|00\rangle+|10\rangle)$,
so linearity forces:

<DerivationSteps>
  <DerivationStep annotation="U is linear, so it distributes over the sum. Apply the assumption from the previous section to each term separately.">
$$
U(|+\rangle\otimes|0\rangle) = \frac{1}{\sqrt2}\Big(U(|0\rangle\otimes|0\rangle)+U(|1\rangle\otimes|0\rangle)\Big) = \frac{1}{\sqrt2}(|00\rangle+|11\rangle)
$$
  </DerivationStep>
  <DerivationStep annotation="Compare against what true cloning of |+⟩ would have to produce: two independent copies of |+⟩, expanded by distributing the tensor product.">
$$
|+\rangle\otimes|+\rangle = \frac12(|00\rangle+|01\rangle+|10\rangle+|11\rangle)
$$
  </DerivationStep>
</DerivationSteps>

That's the <Term id="bell-states">Bell state</Term> $|\Phi^+\rangle$ from two lessons ago, and it does
not match what cloning was supposed to produce. Both sides are computable
states rather than symbols, so put the engine's output for each of them
side by side. First the attempted-cloning circuit, which is the same
H-then-CNOT construction as the Bell-state derivation:

<StaticCircuitDiagram
  numQubits={2}
  instructions={[
    { gate: "H", targets: [0] },
    { gate: "CNOT", targets: [0, 1] },
  ]}
  ariaLabel="A two-qubit circuit: H on qubit 0, then CNOT with qubit 0 as control and qubit 1 as target, an attempted cloning of |+⟩"
/>

<PredictBeforeReveal
  question="Apply H to qubit 0 (making it |+⟩), then CNOT(0,1) with qubit 1 as a blank |0⟩, in an attempt to 'copy' |+⟩. Does this circuit produce two independent copies, |+⟩ ⊗ |+⟩?"
  options={[
    { label: "Yes, |+⟩ ⊗ |+⟩", value: "clones" },
    { label: "No, it produces an entangled Bell state", value: "bell" },
    { label: "No, it produces |0⟩ ⊗ |0⟩", value: "zero" },
    { label: "Yes, but only approximately", value: "approx" },
  ]}
  correctValue="bell"
  explanation="H then CNOT is the circuit this lesson's linearity argument predicts will fail. It produces the Bell state |Φ+⟩ = (|00⟩+|11⟩)/√2, which has zero amplitude on |01⟩ and |10⟩. Cloning would need |+⟩⊗|+⟩ = ½(|00⟩+|01⟩+|10⟩+|11⟩), with equal amplitude on all four terms. The two panels below show the computed states side by side. They don't match, and that mismatch is the contradiction the proof derives algebraically."
/>

<div className="not-prose my-6 grid grid-cols-1 gap-4 sm:grid-cols-2">
  <QuantumStateDisplay state={actualCnotOutput} label="What a real device (H then CNOT) produces" />
  <QuantumStateDisplay state={hypotheticalCloneOutput} label="What cloning |+⟩ would require: |+⟩ ⊗ |+⟩" />
</div>

These are not the same state: $|\Phi^+\rangle$ has zero amplitude on
$|01\rangle$ and $|10\rangle$; $|+\rangle\otimes|+\rangle$ doesn't.
**Contradiction.** A $U$ that correctly clones $|0\rangle$ and $|1\rangle$
individually is *forced by linearity* to fail at cloning $|+\rangle$.
Since $U$ was assumed to work for every state, no such $U$ can exist.

## Physical Interpretation

This isn't a technological limitation waiting on better engineering. The
proof only used linearity, a property that holds for *every* possible
quantum gate, so no future device of any kind can evade it. No-cloning is
also not an arbitrary restriction bolted onto the theory: if cloning were
possible, you could make unlimited copies of one unknown qubit, measure
each copy in a different basis, and reconstruct the full state to arbitrary
precision, which would flatly contradict the measurement postulate this
course has been building on. Worse, a device that clones one
half of an entangled pair many times over would let its holder detect,
instantaneously, which basis someone light-years away chose to measure
their half in: a faster-than-light signal. No-cloning isn't a
restriction on quantum mechanics; it's a requirement for quantum
mechanics to be a consistent theory at all.

<InsightBlock>
No-cloning isn't a restriction on quantum mechanics; it's a requirement for quantum mechanics to be a consistent theory at all.
</InsightBlock>

This is also the fact that makes <Term id="qkd-bb84">quantum key distribution</Term>
(BB84, a few lessons ahead) secure against eavesdropping. If an
attacker could clone a qubit in transit, they could keep one copy,
forward the other, and measure their copy at leisure without ever
touching the qubit that reaches its destination, undetected. Because
cloning is impossible, an eavesdropper has to measure the qubit itself,
which disturbs it, which is what lets Alice and Bob detect the attack.

## Interactive Experiment

The Common Mistakes section below addresses a tempting objection: "CNOT
copies bits, so cloning must be possible." Test it on the platform's
two-qubit engine, using the H-then-CNOT sequence from the contradiction
step.

<InteractiveSection title="Try to clone |+⟩ with CNOT" description="Use the Create a Bell state guided walkthrough (H on qubit 0, then CNOT), which is this lesson's U(|+⟩⊗|0⟩) computation. Check the resulting state against two independent copies of |+⟩: it lands on the entangled Bell state instead, the same mismatch derived above, this time produced by the simulator engine rather than by algebra.">
  <LazyTwoQubitExplorer />
</InteractiveSection>

## Worked Example

**Someone claims their device correctly clones $|0\rangle$, $|1\rangle$,
*and* $|+\rangle$. Show this is still impossible using the same method.**

If the device clones all three, it must also (by the same linearity
argument, since $|-\rangle=\frac{1}{\sqrt2}(|0\rangle-|1\rangle)$ is
another superposition of $|0\rangle$ and $|1\rangle$) satisfy:

$$
U(|-\rangle\otimes|0\rangle) = \frac{1}{\sqrt2}\Big(U(|0\rangle\otimes|0\rangle)-U(|1\rangle\otimes|0\rangle)\Big) = \frac{1}{\sqrt2}(|00\rangle-|11\rangle) = |\Phi^-\rangle
$$

But correct cloning of $|-\rangle$ requires
$U(|-\rangle\otimes|0\rangle)=|-\rangle\otimes|-\rangle=\frac12(|00\rangle-|01\rangle-|10\rangle+|11\rangle)$,
again not equal to $|\Phi^-\rangle$. Cloning $|0\rangle$, $|1\rangle$,
and $|+\rangle$ correctly already forces incorrect behavior on
$|-\rangle$; you can't patch the theorem by cloning "just a few more"
states without breaking somewhere else.

## Common Mistakes

<Callout type="mistake">
**"CNOT copies bits, so cloning must be possible."** $\text{CNOT}(|b\rangle\otimes|0\rangle)=|b\rangle\otimes|b\rangle$
for $b=0$ or $1$ individually, but apply linearity to a superposition
input, just as the proof above does, and CNOT produces the Bell state,
not two copies of the superposition. CNOT copies *known classical
basis states*; it does not clone unknown quantum states.
</Callout>

<Callout type="warning">
**Thinking no-cloning forbids preparing many copies of a *known* state.**
If you know $|\psi\rangle$ in advance, you can prepare as many copies as
you like using ordinary gates. The theorem only forbids a single fixed
device correctly copying an *arbitrary, unknown* input.
</Callout>

## Practice Questions

1. At which step does the proof use linearity? What would go
   wrong with the argument if quantum gates weren't linear operations?
2. A device is claimed to clone $|0\rangle$ and $|-\rangle$ correctly.
   Using the same method as the worked example, find the state it must
   produce for $|1\rangle$'s input, and show it can't equal
   $|1\rangle\otimes|1\rangle$.
3. Explain, in a sentence or two, the connection between no-cloning and
   the measurement postulate: why would perfect cloning let you learn an
   unknown state's exact amplitudes, something a single measurement
   cannot do?

<PracticeLinks problems={practiceProblems} />

## Summary

- No fixed unitary can map $|\psi\rangle\otimes|0\rangle\to|\psi\rangle\otimes|\psi\rangle$
  for *every* single-qubit $|\psi\rangle$.
- The proof: assume it works for $|0\rangle$ and $|1\rangle$, apply
  linearity to $|+\rangle$, and show the required output disagrees with
  actual cloning of $|+\rangle$.
- Copying *known* classical states (via CNOT) is unaffected. The theorem
  is specifically about arbitrary, unknown quantum states.
- No-cloning is required for the theory's consistency, not an incidental
  restriction, and it's the reason an eavesdropper on a quantum channel
  can't clone-and-measure without disturbing the original, the basis for
  BB84's security.

## Further Exploration

<NextDiscovery>
[Quantum Teleportation](/lessons/quantum-computing/quantum-gates-and-circuits/quantum-teleportation) is next. You can't copy an unknown state, but
you *can* move it, destroying the original in the process. The full
protocol uses entanglement, CNOT, and partial measurement, all from this
course, plus the fact you just proved.
</NextDiscovery>
