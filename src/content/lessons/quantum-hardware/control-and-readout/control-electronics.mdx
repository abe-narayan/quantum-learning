import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemMetaForLesson } from "@/lib/problems/metaRegistry";
import { LazyRabiExplorer } from "@/components/simulators/rabi-explorer/LazyRabiExplorer";
import { ControlSignalChainDiagram } from "@/components/visualizations/ControlSignalChainDiagram";

export const practiceProblems = getProblemMetaForLesson(
  "quantum-hardware/control-and-readout/control-electronics"
);

export const lessonMeta = {
  title: "Control Electronics",
  description:
    "Turning an abstract gate like X or Rz(θ) into an actual microwave pulse, reusing the exact resonant two-level Rabi model already verified in Approximation Methods, now applied as a real control-engineering calculation: pulse duration and amplitude from a target rotation angle.",
  course: "control-and-readout",
  module: "control-electronics",
  order: 1,
  difficulty: "intermediate",
  estimatedMinutes: 22,
  prerequisites: ["quantum-hardware/control-and-readout/cryogenic-systems"],
  objectives: [
    "Explain how a classical microwave pulse implements a single-qubit rotation via resonant driving",
    "Compute pulse duration for an arbitrary target rotation angle, not just a full π-pulse",
    "Distinguish pulse duration control from pulse amplitude control as two equivalent ways to reach the same rotation",
  ],
  related: [
    {
      slug: "quantum-mechanics/approximation-methods/time-dependent-perturbation-theory",
      note: "The exact two-level Rabi model this lesson drives a real qubit with is the same RK4-verified 'exact' solution that lesson checks first-order perturbation theory against.",
    },
  ],
};

<LessonHook eyebrow="A circuit diagram is a wish list">
Every <Term id="single-qubit-gates">gate</Term> this platform's Quantum Computing pillar has discussed — X, H, Rz(θ) — is, physically, implemented by a classical control system generating a precisely-shaped microwave pulse and sending it to the qubit.
</LessonHook>

## Motivation

The control pulse can equally be a laser pulse rather than a microwave
one, depending on the platform. This lesson makes that translation
concrete, reusing machinery already built and verified: Approximation
Methods' exact two-level <Term id="rabi-oscillation">Rabi model</Term>.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/8/8d/Microwave_Apparatus_%286029992084%29.jpg"
  alt="A pair of trapped ions inside a glass vacuum chamber, illuminated by a green LED for visibility, being manipulated by microwave fields as part of a NIST quantum computing experiment"
  caption="Real control hardware in action: a pair of trapped ions held by electric fields and manipulated with microwaves at NIST, the same resonant-driving physics this lesson's pulse-duration formula computes, just implemented here with ions instead of a superconducting circuit."
  credit="Y. Colombe / NIST"
  creditUrl="https://commons.wikimedia.org/wiki/File:Microwave_Apparatus_(6029992084).jpg"
  license="Public Domain (U.S. Government work)"
/>

## Engineering Development

### Resonant driving reproduces exact Rabi oscillation

Physical Qubit Platforms' superconducting-qubit lesson already
established: at exact resonance ($E_i=E_f$), this platform's
`exactTwoLevelTransitionProbability(0,0,\Omega,t)` reproduces
$P_1(t)=\sin^2(\Omega t)$ to machine precision, a real, verified
identity connecting the abstract two-level Schrödinger equation to
observable population transfer. Driving a real qubit with a microwave
tone at its resonance frequency, for a controlled duration $t$, realizes
exactly this dynamics.

<InteractiveSection title="Detune it, watch the ceiling drop" description={`Set the detuning slider below to zero and increase the coupling strength V (playing the role of Ω here): watch the population curve trace out P_1(t)=sin²(Vt) exactly, and the Bloch vector sweep smoothly from the north pole to the south pole and back at t_π=π/(2V). Then move detuning away from zero and watch the population curve stop reaching 1: this is what happens when a control pulse's frequency doesn't exactly match the qubit's resonance frequency.`}>
  <LazyRabiExplorer />
</InteractiveSection>

### From target rotation angle to pulse duration

A rotation by angle $\theta$ about the driven axis (in the idealized
resonant, rotating-frame picture) corresponds to $P_1(t)=\sin^2(\theta/2)$,
so an X gate ($\theta=\pi$, full population transfer) needs
$\Omega t=\pi/2$, while a half-way rotation ($\theta=\pi/2$) needs
$\Omega t=\pi/4$, exactly half the X gate's duration at the same drive
strength. This is a direct, computable relationship: given a fixed Rabi
frequency $\Omega$ (set by the hardware's drive amplitude), the *pulse
duration* alone determines which rotation angle is implemented.

### Two equivalent control knobs

Physically, there are two ways to reach a target rotation: fix $\Omega$
and vary pulse *duration* $t$ (used throughout this lesson), or fix a
reference duration and vary drive *amplitude* (which sets $\Omega$
itself, since $\Omega$ is proportional to the microwave field strength).
Real control electronics typically use whichever is more precisely
controllable in a given setup. Both reach the identical target physics,
$\Omega t=\theta/2$.

## Physical Interpretation

This is the concrete link between "abstract quantum circuit" (this
platform's Quantum Computing pillar) and "real hardware" (this pillar):
every gate in a circuit diagram ultimately becomes a specific,
computed pulse duration and amplitude, generated by classical arbitrary
waveform generators and up-converted to the qubit's microwave frequency.

None of this happens at a single temperature. The pulse this section
computes is generated at room temperature, then travels down through
the same staged cooling Cryogenic Systems described, attenuated at each
stage to strip the thermal noise that stage's wiring would otherwise
inject, before ever reaching the qubit; the reflected readout signal
(Qubit Readout Techniques, next) makes the same trip in reverse, back up
through an amplifier chain to a room-temperature digitizer:

<ControlSignalChainDiagram ariaLabel="The full drive and readout signal chain: a room-temperature AWG and IQ mixer send a pulse down through the fridge's cooling stages, attenuated at each one, to the qubit, while the readout tone returns up through an amplifier chain to a room-temperature digitizer" />

The generator has to be fast enough to actually place a pulse edge where
the math says. A control-grade arbitrary waveform generator typically
samples at 1-2 GS/s, a 0.5-1 ns tick, which is coarse next to the
$4.17\text{ ns}$ pulse computed below but still gives several samples
across it; slower, cheaper AWGs (a few hundred MS/s) start to blur a
pulse that short. There's real cable delay to account for too: a meter
of coax adds roughly 5 ns of one-way propagation delay, and by the time
a signal has gone from a room-temperature rack down through a
meter-plus of cryostat wiring to the mixing chamber and back, tens of
nanoseconds have typically elapsed, latency that matters directly for
anything needing a fast measure-then-react loop, like active qubit
reset or mid-circuit feedback.

## Worked Example

**What pulse duration implements a $\theta=\pi/3$ rotation, at Rabi
frequency $\Omega=2\pi\times20\text{ MHz}$?**

$$
\Omega t = \frac\theta2 = \frac\pi6 \implies t = \frac\pi{6\Omega} = \frac\pi{6(2\pi\times20\times10^6)} \approx 4.17\text{ ns}
$$

Verified directly: `exactTwoLevelTransitionProbability(0,0,Ω,t)` at
this $t$ gives $P_1\approx\sin^2(\pi/6)=0.25$, matching the target
rotation's expected population transfer exactly.

## Common Mistakes

<Callout type="warning">
**Confusing rotation angle θ with the Rabi phase Ωt.** The relationship
is $\Omega t=\theta/2$, not $\Omega t=\theta$. This factor of 2
(already seen in Physical Qubit Platforms' $t_\pi=\pi/(2\Omega)$ formula
for a full $\theta=\pi$ rotation) is easy to drop.
</Callout>

<Callout type="note">
**Assuming pulse duration is the only way to control rotation angle.**
Amplitude control (varying $\Omega$ at fixed duration) reaches the same
physics. Real hardware uses whichever is more practical to control
precisely, not one universally "correct" method.
</Callout>

## Practice Questions

1. Using $\Omega t=\theta/2$, find the pulse duration for a $\theta=\pi/2$
   rotation at $\Omega=2\pi\times20\text{ MHz}$.
2. If a fixed pulse duration of 5 ns is used, what Rabi frequency
   $\Omega$ would implement a full $\theta=\pi$ (X gate) rotation?
3. Explain why $P_1(t)=\sin^2(\theta/2)$, not $\sin^2(\theta)$, is the
   correct relationship between measured population and rotation angle.
4. Why does this lesson describe pulse-duration and pulse-amplitude
   control as "equivalent," rather than one being strictly better?

<PracticeLinks problems={practiceProblems} />

## Summary

- Resonant microwave driving realizes exactly the two-level Rabi model
  already verified in Approximation Methods: $P_1(t)=\sin^2(\Omega t)$.
- A target rotation angle $\theta$ requires pulse duration
  $t=\theta/(2\Omega)$, a direct, computable translation from abstract
  gate to real control-pulse parameters.
- Duration and amplitude are two equivalent control knobs reaching the
  same target rotation; real hardware picks whichever is more precisely
  controllable.

## Further Exploration

<NextDiscovery>
[Qubit Readout Techniques](/lessons/quantum-hardware/control-and-readout/qubit-readout-techniques) covers the reverse direction:
extracting the qubit's state back out after gates have run.
</NextDiscovery>
