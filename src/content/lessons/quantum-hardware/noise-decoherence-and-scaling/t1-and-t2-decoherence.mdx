import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemsForLesson } from "@/lib/problems/registry";
import { LazyNoiseExplorer } from "@/components/simulators/noise-explorer/LazyNoiseExplorer";

export const practiceProblems = getProblemsForLesson(
  "quantum-hardware/noise-decoherence-and-scaling/t1-and-t2-decoherence"
).map((problem) => problem.meta);

export const WORKED_EXAMPLE_T1_US = 80;
export const T2_CEILING_US = 2 * WORKED_EXAMPLE_T1_US;
export const TIME_AXIS_MAX_US = 200;
export const T2_SWEEP_US = [10, 20, 30, 40, 55, 70, 80, 100, 120, 140, 160];

export const t1T2DecayFrames = T2_SWEEP_US.map((t2) => {
  const ratio = t2 / WORKED_EXAMPLE_T1_US;
  const regime = ratio >= 1.5 ? "T1-limited" : "T2-limited";
  const curvePoints = (T) =>
    Array.from({ length: 60 }, (_, i) => {
      const t = (i / 59) * TIME_AXIS_MAX_US;
      return { x: t, y: Math.exp(-t / T) };
    });
  return {
    paramLabel: `T2 = ${t2}μs (T2/T1 = ${ratio.toFixed(2)}, ${regime})`,
    series: [
      { label: "P1(t) = e^(-t/T1)", color: "brand", points: curvePoints(WORKED_EXAMPLE_T1_US) },
      { label: "|ρ01(t)| = e^(-t/T2)", color: "warning", points: curvePoints(t2) },
      {
        label: `t = T1 (${WORKED_EXAMPLE_T1_US}μs)`,
        color: "muted",
        points: [
          { x: WORKED_EXAMPLE_T1_US, y: 0 },
          { x: WORKED_EXAMPLE_T1_US, y: 1 },
        ],
      },
      {
        label: `t = T2 (${t2}μs)`,
        color: "accent",
        points: [
          { x: t2, y: 0 },
          { x: t2, y: 1 },
        ],
      },
    ],
  };
});

export const lessonMeta = {
  title: "T1 & T2 Decoherence",
  description:
    "T1 and T2 aren't new physics. They're Advanced Topics in Quantum Mechanics' amplitude damping and dephasing channels, connected to continuous exponential decay via decayProbabilityForTimestep, verified exactly: stepping a discrete Kraus channel over one full T1 gives P(1)=e⁻¹ to 9 decimal places, for any step count.",
  course: "noise-decoherence-and-scaling",
  module: "t1-and-t2-decoherence",
  order: 1,
  difficulty: "advanced",
  estimatedMinutes: 25,
  prerequisites: ["quantum-hardware/noise-decoherence-and-scaling/sources-of-noise"],
  objectives: [
    "State the definitions of T1 (energy relaxation time) and T2 (dephasing/coherence time) as continuous exponential decay constants",
    "Connect T1/T2 to this platform's discrete Kraus-channel model via decayProbabilityForTimestep",
    "Explain the general inequality T2 ≤ 2T1, and why T2 cannot exceed this bound",
  ],
  related: [
    {
      slug: "quantum-mechanics/advanced-quantum-mechanics/open-quantum-systems-and-kraus-operators",
      note: "The Kraus-operator machinery behind T1/T2 decay was built here first, in the general case.",
    },
  ],
};

<LessonHook eyebrow="Beyond the spec-sheet line">
Every hardware press release leads with T1 and T2, and it's easy to nod along without actually knowing what "T1 = 100 μs" commits a device to.
</LessonHook>

## Motivation

Every time a hardware company announces a new chip, the press release
leads with two numbers: <Term id="t1-t2-coherence-times">T1 and T2</Term>, usually in microseconds, sometimes
now creeping into milliseconds for the best trapped-ion and
neutral-atom systems. Reporters repeat them, and competitors get
compared by them. Physical Qubit Platforms and Advanced
Topics in Quantum Mechanics already built everything needed to define
both numbers precisely and compute with them directly, so this lesson
turns them from a spec-sheet line into something you can reason about
the moment you see it quoted.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/f/f3/MRI_Scanner_Mark_One.jpg"
  alt="The Mark One, the first MRI scanner ever used clinically, now on permanent display at Aberdeen Royal Infirmary"
  caption="T1 and T2 weren't invented for qubits. Felix Bloch defined both time constants in 1946 for nuclear spin relaxation, and every MRI scan, including ones taken on this machine, the first ever used clinically, measures the same T1 and T2 this lesson computes, just for protons in tissue instead of a qubit's energy levels."
  credit="AndyGaskell / Wikimedia Commons"
  creditUrl="https://commons.wikimedia.org/wiki/File:MRI_Scanner_Mark_One.jpg"
  license="CC BY-SA 4.0"
/>

<HistoricalMoment date="1946">
Felix Bloch defines both T1 and T2 for nuclear spin relaxation — the same time constants every MRI scan measures for protons in tissue, and every qubit spec sheet measures for energy levels.
</HistoricalMoment>

## Engineering Development

### T1: energy relaxation

**T1** is the exponential time constant for a qubit prepared in
$|1\rangle$ to decay toward $|0\rangle$: $P_1(t)=e^{-t/T_1}$. This is
exactly `amplitudeDampingChannel`'s continuous-time limit. Connecting the
discrete channel to this continuous law uses
`decayProbabilityForTimestep(T1,dt)`$=1-e^{-dt/T_1}$ as the per-step
damping probability $\gamma$, chosen so that $N$ repeated applications
over total time $t=N\,dt$ give $(1-\gamma)^N=e^{-N\,dt/T_1}=e^{-t/T_1}$
**exactly**, for any step count $N$ (not merely as a fine-stepping
approximation; this platform's test suite confirms $N=10$, $100$, and
$1000$ all give $P_1(T_1)=e^{-1}$ to 9 decimal places).

### T2: dephasing / coherence time

**T2** is the analogous time constant for off-diagonal <Term id="coherence">coherence</Term> decay:
$|\rho_{01}(t)|=|\rho_{01}(0)|\,e^{-t/T_2}$, exactly
`dephasingChannel`'s continuous-time limit, connected the same way via
`decayProbabilityForTimestep(T2,dt)` as the per-step dephasing
probability $\lambda$.

### The T2 ≤ 2T1 bound

T1 processes (energy relaxation) *also* randomize phase as a side
effect; losing populational information necessarily disturbs
coherence too, while T2 processes (pure dephasing) need not involve any
energy loss at all. This asymmetry gives the general bound
$T_2\leq2T_1$: coherence can never outlast twice the energy-relaxation
time, though it can decay much faster (if pure dephasing dominates)
independent of T1. A device with $T_2$ close to $2T_1$ is described as
"T1-limited" (dephasing is not the bottleneck); one with $T_2\ll2T_1$ is
"T2-limited" (dephasing dominates).

Watching both channels decay side by side, from the same starting point
and at the same per-step strength, makes the asymmetry visible as two
different directions of travel rather than two different formulas:

<DecoherenceBlochDecay
  ariaLabel="Two Bloch-circle panels comparing amplitude damping (T1) and dephasing (T2) decay from the same equatorial starting state: amplitude damping pulls the vector toward the north pole |0⟩, while dephasing shrinks it straight toward the center along z=0"
/>

<InteractiveSection title="Dephasing vs. amplitude damping" description="Compare the two channels directly: start from |+⟩ (equal parts |0⟩ and |1⟩, maximal initial coherence) and step through dephasing, watching only x and y shrink while z stays at 0. Then switch to amplitude damping from the same starting state and watch z move too, as the state gets pulled all the way to the north pole — the same T1-vs-T2 distinction just derived above, now visible qubit by qubit.">
  <LazyNoiseExplorer />
</InteractiveSection>

## Physical Interpretation

T1 and T2 aren't independent free parameters a device designer can set
arbitrarily. They reflect two physically distinct (if related)
processes: T1 requires an energy-exchanging coupling to the environment
(the qubit must be able to release energy somewhere); T2 requires only a
fluctuating, energy-neutral perturbation (a randomly shifting local
field is enough). This is why realistic devices often report $T_2<2T_1$
substantially; dephasing processes with no direct T1 analogue are
common (Sources of Noise's flux-noise example).

T1 and T2 are not notation invented for qubits. Felix Bloch introduced
both time constants in 1946 to describe nuclear spin relaxation, the
foundation of NMR spectroscopy, and the same two numbers are measured in
every voxel of a clinical MRI scan today (T1-weighted and T2-weighted are
standard scan types), decades before either label appeared on a
superconducting-qubit datasheet. A qubit's T1 and T2 are the same
physical relaxation and dephasing processes Bloch defined, just measured
on a different physical system.

<Callout type="note">
**Reading a T1/T2 number in a press release.** A quoted T1 or T2 is a
time constant, not a "the qubit is dead here" cutoff — at $t=T_1$, 37%
($e^{-1}$) of the population is still there, not zero. Two numbers only
mean something read together: is $T_2$ close to $2T_1$ (T1-limited,
dephasing is well under control) or well below it (T2-limited, some
extra dephasing source is eating coherence)? And it's worth asking how
the number was measured: a single best qubit on the chip, or an average
across all of them, and over what run of the device. A headline T1 from
one cherry-picked qubit is a different claim than a chip-wide median.
</Callout>

## Worked Example

**A device has $T_1=80\mu s$. What is the absolute maximum possible
$T_2$, and what does it mean if the measured $T_2$ is only $30\mu s$?**

Maximum: $T_2\leq2T_1=160\mu s$. A measured $T_2=30\mu s$, far below
this ceiling, indicates the device is strongly **T2-limited**: some
additional pure-dephasing process (beyond whatever contributes to T1) is
the dominant coherence-loss mechanism, and improving T1 alone would do
very little for overall coherence. The engineering priority should be
identifying and suppressing the extra dephasing source instead.

<PredictBeforeReveal
  question="Same device: T1 = 80 μs, T2 = 30 μs. At t = 80 μs (one full T1), which has decayed further: the population P1(t), or the coherence |ρ01(t)|?"
  options={[
    { label: "Coherence has decayed further (it's lower)", value: "coherence-lower" },
    { label: "Population has decayed further (it's lower)", value: "population-lower" },
    { label: "They're equal at t = 80 μs", value: "equal" },
  ]}
  correctValue="coherence-lower"
  explanation="P1(80) = e^(-80/80) = e^-1 ≈ 0.37, but |ρ01(80)| = e^(-80/30) = e^-2.67 ≈ 0.07. Coherence has fallen much further, exactly what a T2-limited device (T2 well under the 2T1 ceiling) means: it loses phase information faster than it loses energy. Drag the slider below to see both curves move together and check this at any point along the axis, not just t = 80 μs."
/>

<InteractiveSection title="Drag T2 toward the 2T1 ceiling" description="T1 is fixed at the Worked Example's 80μs. Drag T2 from a strongly dephasing-dominated 10μs up toward the 160μs = 2T1 ceiling and watch both real-time-axis curves, P1(t)=e^(-t/T1) (population) and |ρ01(t)|=e^(-t/T2) (coherence). The vertical markers show t=T1 and t=T2 directly; the frame label reports the live T2/T1 ratio and names the regime T1-limited or T2-limited the same way the prose above does. Set T2=30μs to see the Worked Example's own numbers.">
  <ParametricCurve
    frames={t1T2DecayFrames}
    sliderLabel="T2"
    referenceLines={[
      { y: 1, label: "full population / coherence" },
      { y: Math.exp(-1), label: "1/e ≈ 0.37 (one time constant)" },
    ]}
    ariaLabel="Population decay P1(t) = e^(-t/T1) and coherence decay |ρ01(t)| = e^(-t/T2) plotted against real time in microseconds, with T1 fixed at 80 microseconds and T2 sweeping from 10 microseconds up to the 160-microsecond 2T1 ceiling, with vertical markers at t=T1 and t=T2"
  />
</InteractiveSection>

## Common Mistakes

<Callout type="warning">
**Assuming T1 and T2 are independent, unrelated numbers.** They're
physically related via $T_2\leq2T_1$. Reported $T_2$ values exceeding
this bound would indicate a measurement or definition error, not a
device that's actually better.
</Callout>

<Callout type="note">
**Assuming decayProbabilityForTimestep's exact match to $e^{-t/T}$ is
merely a good approximation.** It's an EXACT identity, for any step
count. The telescoping product
$(1-\gamma)^N=(e^{-dt/T})^N=e^{-Ndt/T}$ has no approximation error at
all, unlike (for example) the WKB approximation from Approximation
Methods, which actually is only approximate.
</Callout>

## Practice Questions

1. For $T_1=100\mu s$, what per-step damping probability $\gamma$
   corresponds to a $dt=1\mu s$ timestep?
2. If a device's measured $T_2$ equals exactly $2T_1$, what does this
   say about its dominant decoherence mechanism?
3. Using the worked example's numbers, would doubling $T_1$ (to $160\mu s$)
   necessarily double the measured $T_2$? Why or why not?
4. Explain physically why T1 processes necessarily disturb phase
   coherence too, while T2 (pure dephasing) processes need not involve
   any energy exchange.

<PracticeLinks problems={practiceProblems} />

## Summary

- T1 (energy relaxation) and T2 (dephasing) are the continuous-time
  limits of `amplitudeDampingChannel` and `dephasingChannel`, connected
  exactly (not approximately) via `decayProbabilityForTimestep`.
- The bound $T_2\leq2T_1$ reflects that energy relaxation necessarily
  disturbs phase, while pure dephasing needs no energy exchange.
- A device's $T_2/T_1$ ratio diagnoses whether it's T1-limited or
  T2-limited, a real, actionable engineering distinction.
- A quoted T1 or T2 is a time constant (37% remaining at $t=T$), not a
  hard cutoff, and it's only fully meaningful alongside the other number
  and a sense of how and over how many qubits it was measured.

## Further Exploration

<NextDiscovery>
[Crosstalk](/lessons/quantum-hardware/noise-decoherence-and-scaling/crosstalk) is an inherently two-qubit noise source that this lesson's
single-qubit T1/T2 picture doesn't capture.

The $T_2\leq2T_1$ bound stated here as a physical fact is *derived*, from
the Lindblad generator's structure, in [The Lindblad Master Equation](/lessons/quantum-mastery/quantum-information-theory/the-lindblad-master-equation),
which also proves this lesson's discrete Kraus-channel picture and the
continuous-time master equation are exactly the same physics, not two
independent models that happen to agree.
</NextDiscovery>
