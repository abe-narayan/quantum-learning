import { PredictBeforeReveal } from "@/components/mdx/PredictBeforeReveal";
import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemsForLesson } from "@/lib/problems/registry";
import { LazyNoiseExplorer } from "@/components/simulators/noise-explorer/LazyNoiseExplorer";

export const practiceProblems = getProblemsForLesson(
  "quantum-hardware/noise-decoherence-and-scaling/sources-of-noise"
).map((problem) => problem.meta);

export const lessonMeta = {
  title: "Sources of Noise",
  description:
    "Every hardware course so far treated noise as a single abstract 'decoherence.' This lesson catalogs its actual physical sources, and classifies each as a coherent (gate) error or an incoherent (environment) error using the Kraus-channel language already built in Advanced Topics in Quantum Mechanics.",
  course: "noise-decoherence-and-scaling",
  module: "sources-of-noise",
  order: 1,
  difficulty: "advanced",
  estimatedMinutes: 20,
  prerequisites: ["quantum-hardware/control-and-readout/calibration"],
  objectives: [
    "Distinguish coherent errors (miscalibration, systematic) from incoherent errors (genuine environmental decoherence)",
    "Name at least four concrete physical noise sources across this platform's covered hardware types",
    "Explain why classifying a noise source correctly (coherent vs. incoherent) determines how it can be mitigated",
  ],
  related: [
    {
      slug: "quantum-mechanics/advanced-quantum-mechanics/decoherence-and-the-quantum-to-classical-transition",
      note: "The abstract mathematics behind this lesson's 'incoherent error' category: a numerical demonstration of exactly why environmental coupling is irreversible and can't be recalibrated away.",
    },
  ],
};

## Motivation

Ask an experimentalist why yesterday's calibration is off today, and
the honest answer is usually "could be a dozen things." Advanced Topics
in Quantum Mechanics built the mathematics of open-system decoherence in
the abstract; Control & Readout hinted at specific error sources
(readout error, miscalibration) without organizing them. This lesson
does the sorting: it catalogs the real physical culprits and splits them
into two categories that call for completely different fixes.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/4/48/Basic_Science_%2854395491965%29.jpg"
  alt="A neutral-atom quantum computing vacuum chamber at Sandia National Laboratories, showing objective lenses for single-atom traps and surrounding magnetic field coils"
  caption="Real noise-mitigation hardware: at Sandia, a custom Faraday cage inside this vacuum chamber minimizes the stray electric field around trapped atoms. That's physical isolation, the fix this lesson prescribes for incoherent (environmental) errors, and a fundamentally different move than a coherent error's recalibration fix."
  credit="Craig Fritz / Sandia National Laboratories, U.S. Department of Energy"
  creditUrl="https://commons.wikimedia.org/wiki/File:Basic_Science_(54395491965).jpg"
  license="Public Domain (U.S. Government work)"
/>

## Engineering Development

### Coherent vs. incoherent errors

A **coherent error** is a systematic, deterministic deviation: the
device does something slightly wrong, consistently, the same way every
time (Control & Readout's Calibration lesson: a stale $\Omega$ value
implements a systematically wrong rotation angle). This is, in
principle, fully correctable once diagnosed; recalibration fixes it
exactly.

An **incoherent error** is a real, irreversible loss of information to
an uncontrolled environment, the same process Advanced Topics in
Quantum Mechanics' Kraus channels model. It can be *mitigated* (better
isolation, error correction) but recalibrating a number won't fix it,
because the lost information was never sitting somewhere on the device
to recover.

### A catalog of physical sources

- **Miscalibration drift** (coherent): Control & Readout's Calibration
  lesson; slow parameter drift between calibration runs.
- **Amplitude damping / energy relaxation** (incoherent): the qubit
  spontaneously decays to its ground state, releasing energy to the
  environment; modeled by `amplitudeDampingChannel`.
- **Dephasing** (incoherent): random phase kicks from fluctuating
  fields (e.g., flux noise in superconducting qubits, magnetic field
  noise for trapped ions); modeled by `dephasingChannel`.
- **Thermal photon population** (incoherent): Cryogenic Systems'
  $\bar n$; residual thermal photons directly perturb the qubit.
- **Control line noise**: imperfections in the classical electronics
  generating drive pulses (Control Electronics), which can be either
  coherent (a systematic pulse-shape distortion) or incoherent (random
  electronic noise on the line), depending on its specific origin.
- **Crosstalk**: unwanted coupling between supposedly-independent
  qubits (next lesson's dedicated topic).

The catalog sorts cleanly into the two categories, and each branch
points to a different fix:

<MeasurementTree
  root={{
    label: "Noise source",
    children: [
      {
        label: "Coherent (systematic)",
        children: [
          { label: "Miscalibration drift" },
          { label: "Fix: recalibration", highlight: true },
        ],
      },
      {
        label: "Incoherent (environmental)",
        children: [
          { label: "Amplitude damping" },
          { label: "Dephasing" },
          { label: "Thermal photon population" },
          { label: "Fix: isolation or error correction", highlight: true },
        ],
      },
    ],
  }}
  ariaLabel="Noise sources split into coherent errors (fixed by recalibration) and incoherent errors (mitigated by isolation or error correction)"
/>

Control line noise and crosstalk aren't shown above since, as the
catalog notes, which branch they fall into depends on their specific
origin rather than being fixed in advance.

Two of the incoherent sources in the catalog above,
`amplitudeDampingChannel` and `dephasingChannel`, are directly steppable
below. A coherent miscalibration is a deterministic, reversible rotation
by the wrong angle: undo the rotation and the error is gone. Watch
instead how each incoherent channel below pulls the Bloch vector toward
a fixed point it can never rotate back from on its own. That's the
irreversibility that makes "just recalibrate it" the wrong fix for this
branch of the catalog:

<PredictBeforeReveal
  question="A qubit starts on the Bloch sphere's equator (a superposition, z=0). Repeated applications of dephasing act on it. Where does the Bloch point end up?"
  options={[
    { label: "At the north pole |0⟩", value: "north" },
    { label: "At the south pole |1⟩", value: "south" },
    { label: "Shrinks straight to the exact center of the sphere", value: "center" },
    { label: "Stays exactly on the equator's surface", value: "equator" },
  ]}
  correctValue="center"
  explanation="Dephasing shrinks x and y toward zero while leaving z untouched. Starting at the equator, z is already 0, so shrinking x and y to 0 lands the point at the exact center: the maximally mixed state. Amplitude damping moves the state differently: it pulls everything toward the north pole instead. Neither motion is a rotation a later recalibration could undo, which is why both sit in the incoherent branch of the tree above."
/>

<InteractiveSection title="Watch an incoherent channel act" description="Pick amplitude damping or dephasing, pick a starting state, and step through applications. Amplitude damping pulls everything toward |0⟩; dephasing shrinks x and y toward zero while leaving z untouched. Neither motion is a rotation a later, better-calibrated pulse could undo, and that's why these sit in the incoherent branch of the tree above.">
  <LazyNoiseExplorer />
</InteractiveSection>

### Why the classification matters for mitigation

A coherent miscalibration error is fixed by better or more frequent
calibration (Control & Readout), with no new hardware needed. An
incoherent decoherence error can't be fixed this way at all; it needs
either better physical isolation (reducing the coupling to the
environment in the first place) or active error correction (Quantum
Computing's Error Correction & Fault Tolerance course, redundantly
encoding information so it survives some amount of unavoidable loss).
Misidentify which category an error falls into and the engineering
effort is wasted: recalibrating against a source that's actually
environmental accomplishes nothing.

## Physical Interpretation

This distinction is the practical, hardware-facing version of a theme
this platform's Advanced Topics in Quantum Mechanics course already
established: decoherence (this lesson's incoherent errors) is
irreducibly different from a deterministic, correctable mistake. Every
real device's overall error budget is a mix of both categories, and
serious engineering effort goes into correctly attributing observed
errors to the right bucket.

## Worked Example

**A device's gate fidelity drops over the course of a day, but is
restored to its original value immediately after recalibration. Is this
most likely a coherent or incoherent error source?**

Coherent: recalibration alone restoring full fidelity, with no hardware
change, is the signature of a systematic, deterministic parameter drift
(Control & Readout's Calibration lesson). A genuinely environmental
error wouldn't be fixable just by re-measuring and re-setting a
classical control parameter.

## Common Mistakes

<Callout type="mistake">
**Assuming all noise is "decoherence."** Miscalibration and other
coherent errors are a different (and often more easily fixed) category.
Lumping everything together as "decoherence" obscures which mitigation
strategy actually applies.
</Callout>

<Callout type="mistake">
**Assuming incoherent errors can be "calibrated away."** Recalibration
fixes systematic, deterministic errors; it cannot recover information
truly lost to an uncontrolled environment. That requires isolation
improvements or active error correction instead.
</Callout>

## Practice Questions

1. Classify each as coherent or incoherent: (a) a stale Rabi frequency
   value from Control & Readout, (b) amplitude damping from spontaneous
   emission, (c) dephasing from fluctuating magnetic flux.
2. Why does recalibration fully fix a coherent error but not an
   incoherent one?
3. Name two engineering mitigation strategies for incoherent errors,
   and explain why neither is "recalibration."
4. If crosstalk between two qubits were purely systematic (the same
   deterministic shift every time), would it belong in the coherent or
   incoherent category? Explain your reasoning.

<PracticeLinks problems={practiceProblems} />

## Summary

- Coherent errors are systematic and deterministic (fixable by better
  calibration); incoherent errors are genuine information loss to an
  environment (mitigated by isolation or error correction, not
  recalibration).
- Real noise sources include miscalibration drift, amplitude damping,
  dephasing, thermal photon population, control line noise, and
  crosstalk, spanning both categories.
- Correctly classifying an observed error is a prerequisite for choosing
  an effective mitigation strategy.

## Further Exploration

Next: [T1 & T2 Decoherence](/lessons/quantum-hardware/noise-decoherence-and-scaling/t1-and-t2-decoherence), which quantifies the two specific
incoherent error channels (amplitude damping, dephasing) this lesson
named, connecting Advanced Topics in Quantum Mechanics' discrete Kraus
channels directly to the continuous decay times real hardware datasheets
report.
