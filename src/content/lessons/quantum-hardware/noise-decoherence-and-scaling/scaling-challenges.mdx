import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemsForLesson } from "@/lib/problems/registry";

export const practiceProblems = getProblemsForLesson(
  "quantum-hardware/noise-decoherence-and-scaling/scaling-challenges"
).map((problem) => problem.meta);

export const FIDELITIES = [0.95, 0.99, 0.999, 0.9999];
export const MAX_GATES = 1200;

export const lessonMeta = {
  title: "Scaling Challenges",
  description:
    "Compounding per-gate error rates across a deep circuit, computed directly: even a 99.9% per-gate fidelity drops below 37% success probability by 1000 sequential gates. The concrete numerical reason 'just add more qubits' badly understates what scaling a useful quantum computer actually requires.",
  course: "noise-decoherence-and-scaling",
  module: "scaling-challenges",
  order: 1,
  difficulty: "advanced",
  estimatedMinutes: 22,
  prerequisites: ["quantum-hardware/noise-decoherence-and-scaling/crosstalk"],
  objectives: [
    "Compute how per-gate error compounds multiplicatively across a circuit of many sequential gates",
    "Explain why qubit COUNT alone is an incomplete measure of a device's usefulness",
    "Connect compounding error directly to Error Correction & Fault Tolerance's threshold theorem",
  ],
};

## Motivation

A press release announcing "1000 qubits" sounds like an order of
magnitude more computer than one announcing "100 qubits." It might not
be. Physical Qubit Platforms and this course's earlier lessons built up
individual error sources, T1/T2, crosstalk, readout error, one at a
time, in isolation. This lesson asks the question that actually decides
whether a device is useful: what happens when hundreds or thousands of
imperfect operations run back to back?

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/thumb/9/95/Quantum_metamaterial_with_24_artificial_atoms.png/1280px-Quantum_metamaterial_with_24_artificial_atoms.png"
  alt="An electron micrograph of a real superconducting chip patterned with 24 transmon qubit 'artificial atoms', coupled through shared microwave resonators"
  caption="A real 24-qubit chip. Raw qubit count like this is the incomplete metric this lesson warns about: every one of these qubits' gates still compounds error multiplicatively, the actual bottleneck this lesson's p^N table quantifies."
  credit="FMN Laboratory, Bauman Moscow State Technical University"
  creditUrl="https://commons.wikimedia.org/wiki/File:Quantum_metamaterial_with_24_artificial_atoms.png"
  license="CC BY 4.0"
/>

## Engineering Development

### Errors compound multiplicatively

If each gate succeeds with probability $p$ (independent, per-gate
fidelity), then $N$ sequential gates succeed with probability $p^N$.
Not $p$, and not $1-N(1-p)$ either: it's an exponential decay in $N$,
not a linear one.

| Per-gate fidelity | N=10 | N=100 | N=1000 |
|---|---|---|---|
| 99.9% | 99.0% | 90.5% | 36.8% |
| 99% | 90.4% | 36.6% | $4.3\times10^{-5}$ |
| 95% | 59.9% | 0.6% | $\approx0$ |

A 99.9% per-gate fidelity is better than most current real hardware
achieves, and it still drops to 36.8% overall success by 1000 gates,
barely better than a coin flip's worth of confidence in the final
answer. This single table is the quantitative core of why
"quantum-supremacy"-scale algorithms need either extremely low error
rates or active error correction, not just more qubits.

The table samples three values of $N$; the full curve makes the decay's
shape, and how differently each fidelity ages, visible directly:

<PredictBeforeReveal
  question="Two devices run 200 sequential gates: one at 99% per-gate fidelity, one at 99.9%. Both numbers 'round to about 99%.' How different is their overall success probability at N=200?"
  options={[
    { label: "About the same, both round to ~99%", value: "same" },
    { label: "Modestly different, maybe 20-30% apart", value: "modest" },
    { label: "Dramatically different, several times apart", value: "dramatic" },
  ]}
  correctValue="dramatic"
  explanation="0.99^200 ≈ 13.4% success; 0.999^200 ≈ 81.9% success. That's a factor of six apart, from two numbers that look nearly identical written as percentages. Exponentiation is unforgiving of small differences in the base once N gets large, which is exactly what the curve below lets you watch happen as you step from 95% up to 99.99%."
/>

<InteractiveSection title="Step through per-gate fidelities from 95% to 99.99%" description="Step through the fidelity values from 95% up to 99.99%. At each one, watch how far along the x-axis the curve survives before collapsing toward zero. The gap between 99% and 99.9% is enormous, even though both round to 'about 99%.'">
  <ParametricCurve
    frames={FIDELITIES.map((fidelity) => ({
      paramLabel: `p = ${(fidelity * 100).toFixed(2)}%`,
      series: [{
        label: `p^N, p=${fidelity}`,
        color: "brand",
        points: Array.from({ length: 61 }, (_, i) => ({ x: Math.round((i / 60) * MAX_GATES), y: fidelity ** Math.round((i / 60) * MAX_GATES) }))
      }]
    }))}
    sliderLabel="Per-gate fidelity p"
    referenceLines={[{ y: 0.5, label: "50% success" }]}
    ariaLabel="Success probability p^N versus number of sequential gates N, for several per-gate fidelities"
  />
</InteractiveSection>

### Qubit count is an incomplete metric

A device with 1000 "noisy" qubits (a common marketing framing) is not
automatically more capable than a much smaller device with much lower
error rates. The compounding-error table above applies per *qubit*
too, and a computation using many qubits for many sequential gates
compounds error across both dimensions at once. Metrics like **quantum
volume** (not derived in full here, but conceptually: a combined
measure of qubit count, connectivity, and per-gate error together)
exist precisely because raw qubit count alone is misleading.

### Where error correction fits in

Error Correction & Fault Tolerance's **threshold theorem** result
(already built in the Quantum Computing pillar) is the direct answer to
this lesson's compounding-error problem: if the *physical* per-gate
error rate is below a certain threshold, encoding logical qubits
redundantly (Quantum Computing's bit-flip and Shor codes) can suppress
the *logical* error rate arbitrarily far below the physical rate, at
the cost of needing many physical qubits per logical qubit. This
lesson's compounding-error table is precisely the motivating problem
that result exists to solve.

## Physical Interpretation

"Scaling" a quantum computer means two different, both-necessary things
simultaneously: increasing qubit count, AND decreasing per-gate error
rate (or adding error correction overhead to compensate). A device that
scales only the first while ignoring the second doesn't become more
useful, per this lesson's own compounding-error numbers.

## Worked Example

**How many sequential 99.9%-fidelity gates can run before overall
success probability drops below 50%?**

$$
0.999^N = 0.5 \implies N = \frac{\ln0.5}{\ln0.999} \approx 693
$$

Fewer than 700 gates, even at a fidelity most current hardware hasn't
reached. This concretely illustrates why useful, deep quantum circuits
(many algorithms need thousands to millions of gates) are out of reach
without either substantially better physical fidelities or active error
correction.

## Common Mistakes

<Callout type="mistake">
**Treating "N qubits" as a complete description of a device's
capability.** As this lesson's table shows, per-gate error rate matters
at least as much as qubit count. A large, noisy device can be less
useful than a smaller, cleaner one for a given task.
</Callout>

<Callout type="mistake">
**Assuming error compounds additively (subtracting N×(1-p) from 1)
rather than multiplicatively (p^N).** These give very different answers
at large N. The multiplicative form is what unitarity/independent-trial
probability actually requires.
</Callout>

## Practice Questions

1. Using $p^N$, compute the success probability for 500 gates at 99.9%
   per-gate fidelity.
2. Redo the worked example's calculation for a 99.99% per-gate fidelity
   target: how many gates can run before dropping below 50% success?
3. Explain in one sentence why quantum volume (or similar combined
   metrics) exist, rather than reporting qubit count alone.
4. How does this lesson's compounding-error problem directly motivate
   the existence of Error Correction & Fault Tolerance's threshold
   theorem?

<PracticeLinks problems={practiceProblems} />

## Summary

- Per-gate errors compound multiplicatively: $N$ sequential gates at
  fidelity $p$ succeed with probability $p^N$, an exponential decay in
  circuit depth.
- Even 99.9% per-gate fidelity drops to 36.8% success by 1000 gates,
  the concrete numerical reason deep circuits need either very low error
  rates or active error correction.
- Qubit count alone is an incomplete capability metric; per-gate error
  rate (and connectivity) matter at least as much.

## Further Exploration

Next: [Roadmaps to Fault Tolerance](/lessons/quantum-hardware/noise-decoherence-and-scaling/roadmaps-to-fault-tolerance), which closes this course by
connecting every error source covered so far to the concrete overhead
numbers (physical qubits per logical qubit) that real fault-tolerant
quantum computing roadmaps target.
