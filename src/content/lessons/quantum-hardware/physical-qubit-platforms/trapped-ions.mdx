import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemsForLesson } from "@/lib/problems/registry";
import { LazyRabiExplorer } from "@/components/simulators/rabi-explorer/LazyRabiExplorer";
import { PredictBeforeReveal } from "@/components/mdx/PredictBeforeReveal";

export const practiceProblems = getProblemsForLesson(
  "quantum-hardware/physical-qubit-platforms/trapped-ions"
).map((problem) => problem.meta);

export const lessonMeta = {
  title: "Trapped Ions",
  description:
    "A qubit encoded in an individual ion's internal electronic (or hyperfine) states, held in place by oscillating electric fields: trading superconducting qubits' speed for dramatically longer coherence times and, uniquely, all-to-all connectivity via a shared vibrational mode.",
  course: "physical-qubit-platforms",
  module: "trapped-ions",
  order: 1,
  difficulty: "intermediate",
  estimatedMinutes: 20,
  prerequisites: ["quantum-hardware/physical-qubit-platforms/superconducting-qubits"],
  objectives: [
    "Explain how an ion trap confines a charged atom using oscillating electric fields",
    "State why trapped ions have much longer coherence times than superconducting qubits, and at what cost",
    "Explain how the shared motional (vibrational) mode enables two-qubit gates between ANY pair of trapped ions, not just neighbors",
  ],
};

<LessonHook eyebrow="A real atom, not an artificial circuit">
Suspend a single charged atom in empty space, no walls, no wires, nothing physically touching it, then hold it steady enough that a laser can address its internal states one operation at a time.
</LessonHook>

## Motivation

Holding it steady, for long enough, to entangle it with a neighbor on
demand, is the hard part. A static electric field can't do this job
alone; Earnshaw's theorem, a result from classical electrostatics,
rules out trapping a charge in 3D with any static field configuration
at all.

<Question>
So how does a trapped-ion quantum computer hold a single ion still enough to compute with it?
</Question>

That engineering problem, and its solution, define the whole platform.
Where a superconducting qubit is an entirely artificial circuit, a
trapped-ion qubit uses a real atom, with the qubit encoded in two of its
naturally-occurring internal energy levels. This is a fundamentally
different engineering starting point, and it shows up directly in the
platform's tradeoffs.

<AnnotatedFigure
  src="https://upload.wikimedia.org/wikipedia/commons/thumb/1/1c/Quantum_Computing%3B_Ion_Trapping_%285941055642%29.jpg/1280px-Quantum_Computing%3B_Ion_Trapping_%285941055642%29.jpg"
  alt="Two trapped beryllium ions held roughly 40 micrometers apart above a gold chip trap, surrounded by copper shielding and gold mesh, at NIST"
  caption="Two real trapped beryllium ions at NIST, held above a gold chip trap and swapping energy back and forth. This is the actual apparatus behind this lesson's Paul trap: oscillating electric fields confining charged atoms in place for use as qubits."
  pins={[
    { id: "trap", x: 50, y: 43, label: "The gold chip trap itself — the ions are held here, roughly 40 μm apart, by the Paul trap's oscillating RF field." },
    { id: "clamp", x: 50, y: 8, label: "Copper mount, clamping and thermally anchoring the trap package in place." },
    { id: "rf", x: 83, y: 23, label: "RF feedthrough, carrying the oscillating drive field into the vacuum chamber." },
    { id: "traces", x: 30, y: 48, label: "Wire-bonded control lines routing RF and DC voltages to the trap electrodes." },
    { id: "cables", x: 83, y: 78, label: "Bundled wiring exiting the chamber to room-temperature control electronics." },
  ]}
  credit="Y. Colombe / NIST"
  creditUrl="https://commons.wikimedia.org/wiki/File:Quantum_Computing;_Ion_Trapping_(5941055642).jpg"
  license="Public Domain (U.S. Government work)"
/>

## Engineering Development

### The trap itself

The answer to the Motivation section's puzzle is a **Paul trap**: it
confines the ion using rapidly oscillating (radio-frequency) electric
fields rather than a static one, exactly because Earnshaw's theorem rules
the static option out. The field oscillates fast enough that the ion
feels an effective, time-averaged confining potential. Multiple ions
in the same trap arrange themselves in a line, held apart by mutual
Coulomb repulsion balanced against the trap's confinement:

<HardwarePlatformSchematic variant="trapped-ion" ariaLabel="A linear chain of trapped ions held between RF electrodes, sharing a vibrational mode" />

The chain's ions aren't independent; the dashed line through them above
is the shared vibrational mode the next section relies on for two-qubit
gates.

### Why coherence times are so much longer

The qubit states are real atomic energy levels (electronic or hyperfine,
meaning nuclear-spin-related, transitions), which are, unlike a
lithographically-fabricated superconducting circuit, identical for every
ion of the same species and extremely well isolated from environmental
noise. Nature already built a "device" whose levels don't drift.
Trapped-ion coherence times routinely reach seconds, several orders of
magnitude longer than superconducting qubits' microseconds. The cost
falls mainly on **two-qubit** gate speed: these mediate the interaction
through the ions' shared mechanical (vibrational) motion, which is slow
compared to a superconducting circuit's direct capacitive coupling, often
microseconds rather than nanoseconds. A single-qubit gate, driving one
ion's internal transition directly with a laser and no shared motion
involved, isn't bottlenecked the same way and can be considerably faster;
it's the entangling step specifically that pays the speed cost.

<PredictBeforeReveal
  question="If the driving laser is tuned slightly off the ion's resonance (nonzero detuning) instead of exactly on it, what happens to the maximum population you can transfer?"
  options={[
    { label: "Still reaches 100%, just more slowly", value: "full-slower" },
    { label: "Caps below 100%, no matter how long you drive", value: "capped" },
    { label: "Overshoots past 100%", value: "overshoot" },
    { label: "Nothing changes; detuning doesn't matter for lasers", value: "no-change" },
  ]}
  correctValue="capped"
  explanation="Off resonance, the two-level Rabi formula caps the achievable population below 1, the same physics driving any resonantly-controlled qubit, laser or microwave. Move detuning away from zero in the Explorer below and watch the peak population drop measurably below 1, exactly the resonance-imperfection penalty every driven platform pays."
/>

<InteractiveSection title="Drive an atomic transition resonantly" description="A single-qubit gate here works exactly like the superconducting-qubit lesson's microwave drive, just implemented with a laser tuned to the ion's internal transition instead: set detuning to zero and vary the coupling strength V (playing the role of the laser's Rabi frequency) to watch the population sweep from 0 to 1 and back at t_π=π/(2V), the same two-level Rabi physics driving any resonantly-controlled qubit, regardless of whether the drive is a laser or a microwave tone. Move detuning away from zero and see the achievable population drop below 1, exactly the penalty for imperfect resonance on any platform.">
  <LazyRabiExplorer />
</InteractiveSection>

### All-to-all connectivity via the shared motional mode

The ions in a linear trap share collective vibrational modes (the whole
chain can oscillate together, like coupled pendulums). A two-qubit gate
works by using one ion's internal state to briefly excite this shared
motion, which the *other* ion's internal state can then sense, entangling
them regardless of physical separation along the chain. This gives
trapped ions a real engineering advantage over most solid-state
platforms: any ion can be entangled with any other in the same trap, not
just its physical neighbors, a fundamentally different connectivity
graph from superconducting qubits' typically nearest-neighbor layout.
The two connectivity graphs, drawn on the same five qubits, make the
contrast concrete rather than just asserted in prose:

<div className="not-prose grid gap-4 sm:grid-cols-2">

<div>
<GraphDiagram
  nodes={[
    { id: "0", x: 0.5, y: 0.1 },
    { id: "1", x: 0.186, y: 0.376 },
    { id: "2", x: 0.306, y: 0.824 },
    { id: "3", x: 0.694, y: 0.824 },
    { id: "4", x: 0.814, y: 0.376 }
  ]}
  edges={[
    { from: "0", to: "1" },
    { from: "0", to: "2" },
    { from: "0", to: "3" },
    { from: "0", to: "4" },
    { from: "1", to: "2" },
    { from: "1", to: "3" },
    { from: "1", to: "4" },
    { from: "2", to: "3" },
    { from: "2", to: "4" },
    { from: "3", to: "4" }
  ]}
  ariaLabel="Five trapped-ion qubits arranged in a pentagon with an edge between every pair, showing all-to-all connectivity via the shared motional mode"
/>
<p className="mt-1 text-center text-xs text-muted-foreground">Trapped ions: all-to-all</p>
</div>

<div>
<GraphDiagram
  nodes={[
    { id: "0", x: 0.5, y: 0.1 },
    { id: "1", x: 0.186, y: 0.376 },
    { id: "2", x: 0.306, y: 0.824 },
    { id: "3", x: 0.694, y: 0.824 },
    { id: "4", x: 0.814, y: 0.376 }
  ]}
  edges={[
    { from: "0", to: "1" },
    { from: "1", to: "2" },
    { from: "2", to: "3" },
    { from: "3", to: "4" }
  ]}
  ariaLabel="Five superconducting qubits arranged in a pentagon with edges only between adjacent neighbors, showing nearest-neighbor connectivity"
/>
<p className="mt-1 text-center text-xs text-muted-foreground">Superconducting: nearest-neighbor</p>
</div>

</div>

## Physical Interpretation

The core platform tradeoff is now visible directly: superconducting
qubits sacrifice coherence time for speed and (relatively) easier
fabrication at scale; trapped ions sacrifice speed for coherence time and
gain flexible connectivity. Neither is strictly better; which matters
more depends on the specific algorithm and error-correction overhead, a
question the Noise, Decoherence & Scaling course returns to
quantitatively.

IonQ and Quantinuum (built on Honeywell's original ion-trap group) have
staked entire product lines on this exact tradeoff, betting that fewer,
longer-lived, fully-connected qubits beat a much larger but noisier and
sparsely-wired array for near-term algorithms. Quantinuum's H-series
systems have repeatedly reported among the highest published two-qubit
gate fidelities of any hardware platform, a direct commercial payoff of
the atomic-level isolation this lesson's coherence-time argument
predicts, not a separate engineering achievement layered on top of it.

## Worked Example

**A trapped-ion two-qubit gate takes about $10\mu s$; the ion's coherence
time is about $1s$. Compare this "gate count budget" to a superconducting
qubit's ($10ns$ gate, $10\mu s$ coherence).**

Trapped ion: $1s/10\mu s=10^5$ gates fit within one coherence time.
Superconducting: $10\mu s/10ns=10^3$ gates fit within one coherence
time. Despite trapped ions being roughly 1000× *slower* per gate, their
much longer coherence time gives them a *larger* raw gate budget in this
comparison, a concrete numerical illustration of why "gate speed alone"
is a misleading platform comparison metric.

## Common Mistakes

<Callout type="warning">
**Assuming faster gates always mean a "better" platform.** As the worked
example shows directly, the ratio of coherence time to gate time, not
gate speed alone, sets the realistic operation budget.
</Callout>

<Callout type="warning">
**Thinking trapped-ion connectivity is unlimited for free.** All-to-all
connectivity via the shared motional mode is a real architectural
advantage, but it doesn't scale trivially: as more ions share one
trap's vibrational modes, gate fidelity and speed both degrade, a real
engineering constraint the Noise, Decoherence & Scaling course develops.
</Callout>

## Practice Questions

1. Why can't a static (non-oscillating) electric field alone trap a
   charged ion in 3D?
2. Using the worked example's numbers, if a superconducting device
   somehow achieved a $100\mu s$ coherence time at the same $10ns$ gate
   speed, would its gate budget exceed the trapped ion's $10^5$? Compute
   the new ratio.
3. Explain, in your own words, how the shared vibrational mode allows a
   two-qubit gate between two ions that aren't in direct contact.
4. Name one advantage and one disadvantage of trapped ions relative to
   superconducting qubits, using this lesson's specific numbers.

<PracticeLinks problems={practiceProblems} />

## Summary

- Trapped ions use real atoms confined by oscillating electric fields
  (a Paul trap), a fundamentally different physical starting point from
  superconducting circuits.
- Coherence times reach seconds (vs. superconducting qubits' microseconds),
  at the cost of slower gates (microseconds, not nanoseconds).
- The shared motional mode gives real all-to-all connectivity within
  one trap, a real architectural advantage, though one that faces its
  own scaling limits.

## Further Exploration

<NextDiscovery>
[Neutral Atoms](/lessons/quantum-hardware/physical-qubit-platforms/neutral-atoms)
keeps trapped ions' long coherence times while addressing scalability
differently, using light, not electric fields, for confinement.
</NextDiscovery>
