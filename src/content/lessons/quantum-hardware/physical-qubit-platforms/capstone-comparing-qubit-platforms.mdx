import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemsForLesson } from "@/lib/problems/registry";
import { BarChartExplorer } from "@/components/visualizations/BarChartExplorer";

export const gateBudgetFrames = [
  {
    paramLabel: "Superconducting: ~10 ns gate, ~10 μs coherence",
    bars: [{ label: "Gate budget (10^n gates)", value: 3, caption: "~10³ gates" }],
  },
  {
    paramLabel: "Trapped ion: ~10 μs gate, ~1 s coherence",
    bars: [{ label: "Gate budget (10^n gates)", value: 5, highlight: true, caption: "~10⁵ gates" }],
  },
];

export const practiceProblems = getProblemsForLesson(
  "quantum-hardware/physical-qubit-platforms/capstone-comparing-qubit-platforms"
).map((problem) => problem.meta);

export const lessonMeta = {
  title: "Capstone: Comparing Qubit Platforms",
  description:
    "No single platform wins on every axis: putting superconducting circuits, trapped ions, neutral atoms, photons, and spin qubits side by side on coherence time, gate speed, connectivity, and scalability makes the actual engineering tradeoff space visible at once.",
  course: "physical-qubit-platforms",
  module: "capstone-comparing-qubit-platforms",
  order: 1,
  difficulty: "intermediate",
  estimatedMinutes: 20,
  prerequisites: ["quantum-hardware/physical-qubit-platforms/spin-qubits"],
  objectives: [
    "Compare all five platforms on the same four axes: coherence time, gate speed, connectivity, and scalability path",
    "Explain why no platform dominates on every axis simultaneously",
    "Identify which platform a given application's priorities (e.g. long computations vs. fast iteration) would favor",
  ],
};

## Motivation

Five separate lessons each made one platform's physics feel like the
obvious choice, in isolation. Read side by side, none of them do:
superconducting circuits, trapped ions, neutral atoms, photons, and spin
qubits each trade something real for something else. This capstone adds
no new physics. It puts all five in the same room, which is where the
actual engineering decision-making happens in practice.

## The Tradeoff Space, Side by Side

Same four axes, five platforms, one table: this is what an actual
engineering decision between them rests on.

| Platform | Coherence time | Gate speed | Connectivity | Scaling path |
|---|---|---|---|---|
| Superconducting | ~microseconds | ~nanoseconds (fast) | mostly nearest-neighbor | lithographic fabrication, many labs/companies actively scaling |
| Trapped ions | ~seconds (long) | ~microseconds (slow) | all-to-all within a trap | chain length limited; modular/networked traps an active research direction |
| Neutral atoms | ~seconds (long) | ~microseconds | flexible, reconfigurable geometry | hundreds of qubits already demonstrated; among the largest current counts |
| Photonic | effectively no decoherence in flight; loss-limited instead | very fast per operation | naturally networked (light travels) | strong for communication/networking; two-qubit gates remain the hard problem |
| Spin qubits | intermediate–long | ~nanoseconds | nearest-neighbor (dot-to-dot) | leverages existing semiconductor fabrication tooling |

(These are representative orders of magnitude for illustrating the
tradeoff space, not precise specifications of any particular device;
real numbers vary by generation, vendor, and continue to improve.)

The table's five rows are five genuinely different physical structures,
not just five sets of numbers; each platform lesson's own schematic,
side by side, makes that concrete:

<div className="not-prose grid gap-4 sm:grid-cols-2 lg:grid-cols-3">

<div>
<HardwarePlatformSchematic variant="superconducting" ariaLabel="Superconducting transmon qubit: an LC circuit with a Josephson junction replacing the inductor" />
<p className="mt-1 text-center text-xs text-muted-foreground">Superconducting</p>
</div>

<div>
<HardwarePlatformSchematic variant="trapped-ion" ariaLabel="Trapped ions: a linear chain of ions confined by RF electrodes, sharing a motional mode" />
<p className="mt-1 text-center text-xs text-muted-foreground">Trapped ions</p>
</div>

<div>
<HardwarePlatformSchematic variant="neutral-atom" ariaLabel="Neutral atoms: an optical tweezer array with a reconfigurable subset of traps loaded" />
<p className="mt-1 text-center text-xs text-muted-foreground">Neutral atoms</p>
</div>

<div>
<HardwarePlatformSchematic variant="photonic" ariaLabel="Photonic qubits: a single beam's polarization, viewed head-on as two perpendicular field-oscillation directions" />
<p className="mt-1 text-center text-xs text-muted-foreground">Photonic</p>
</div>

<div>
<HardwarePlatformSchematic variant="spin-qubit" ariaLabel="Spin qubits: an electron confined in a gate-defined quantum dot on a semiconductor substrate" />
<p className="mt-1 text-center text-xs text-muted-foreground">Spin qubits</p>
</div>

</div>

Five different physical mechanisms for the same underlying job (an
isolated, controllable two-level system) is exactly why the table above
has no row that wins on every column: each structure's own geometry is
what produces its particular coherence/speed/connectivity tradeoff.

The table's coherence-time column, read as a relative ordering rather
than exact numbers (bar height is ordinal; the caption under each bar
is the table's own qualitative entry, not a precise measurement):

<BarChart
  ariaLabel="Relative coherence-time ordering across the five platforms, from the table above"
  bars={[
    { label: "Super-conducting", value: 1, caption: "~μs" },
    { label: "Spin qubits", value: 2, caption: "intermediate" },
    { label: "Trapped ions", value: 3, caption: "~s" },
    { label: "Neutral atoms", value: 3, caption: "~s" },
    { label: "Photonic", value: 4, highlight: true, caption: "no decoherence" },
  ]}
/>

Photonic's bar is highlighted because it isn't really on the same
scale as the others at all: "no decoherence in flight" is a
qualitatively different statement from a long-but-finite coherence
time, which is exactly the nuance a single ranked bar can't fully
capture and the table's loss-limited caveat spells out.

### The general pattern: no free lunch

Every platform trades something for something else: superconducting
qubits trade coherence for speed and fabrication maturity; trapped ions
and neutral atoms trade gate speed for coherence and (for neutral atoms)
flexible geometry; photons trade an entirely different error model
(loss, not decoherence) for a much harder gate mechanism; spin qubits
trade smaller, more standard fabrication for intermediate performance on
every other axis. This isn't a temporary state of immaturity to be
"solved." It reflects genuine underlying physics tradeoffs: strong
environmental isolation, which improves coherence, tends to also
weaken the coupling needed for fast gates.

## Worked Example

**A research group needs to run a long calculation requiring many
sequential gates, but connectivity between arbitrary qubits matters less
than raw operation count. Which platform's tradeoff profile favors this?**

Trapped ions or neutral atoms: both offer long coherence times, giving a
large "gate budget" (as computed directly in the Trapped Ions lesson's
worked example: $\sim10^5$ gates before decoherence dominates, versus
$\sim10^3$ for a representative superconducting device), favoring
platforms where coherence time, not raw gate speed, is the limiting
resource for this specific priority.

<InteractiveSection title="Toggle between the two gate budgets" description="Slide between the exact two data points computed above: a representative superconducting device's ~10 ns gate and ~10 μs coherence time give a gate budget of ~10³ operations, while a trapped ion's much slower ~10 μs gate paired with its ~1 s coherence time gives ~10⁵, a factor of 100 more room despite each individual gate being roughly 1000 times slower. The bar height is the base-10 exponent (3 or 5), not the raw count, so both fit on one readable scale.">
  <BarChartExplorer
    frames={gateBudgetFrames}
    sliderLabel="Platform"
    ariaLabel="Gate budget comparison: superconducting device at roughly 10 to the 3 gates versus trapped ion at roughly 10 to the 5 gates, from this lesson's worked example"
    maxValue={5}
  />
</InteractiveSection>

## Common Mistakes

<Callout type="mistake">
**Looking for a single "best" platform.** This lesson's comparison table
has no row that wins on every column. The right platform depends on
which axis a specific application actually needs most.
</Callout>

<Callout type="mistake">
**Treating these numbers as fixed, permanent specifications.** Every
platform's numbers have improved substantially over time and continue
to; this table describes representative *orders of magnitude* and
qualitative tradeoffs, not a snapshot to memorize as timeless fact.
</Callout>

## Practice Questions

1. Using this lesson's table, which platform would you recommend for an
   application prioritizing photon-based communication between two
   separate quantum processors?
2. Explain why "strong environmental isolation" (good for coherence)
   tends to work against "fast, strong coupling" (good for gate speed),
   using at least one specific platform's physical mechanism as an
   example.
3. If a new fabrication breakthrough dramatically improved superconducting
   qubits' coherence time without changing anything else, would this
   table's fundamental "no free lunch" pattern disappear? Why or why not?
4. Which platform's connectivity advantage comes from a shared physical
   mode, and which platform's comes from reconfigurable geometry?
   Explain the difference between these two mechanisms.

<PracticeLinks problems={practiceProblems} />

## Summary

- Comparing coherence time, gate speed, connectivity, and scalability
  side by side shows every platform trading strength on one axis for
  weakness on another; no platform dominates across the board.
- This reflects genuine physical tradeoffs (isolation vs. coupling
  strength), not simply immature engineering that will resolve itself.
- Application priorities (raw gate count vs. fast iteration vs.
  networking) should drive platform choice, not a single "best" ranking.

## Further Exploration

None of these five platforms has appeared, in this course, as anything
more than the physics inside a vacuum chamber or a cryostat. **Control &
Readout** is where that changes: cryogenics, control electronics, and
calibration, the classical engineering that actually drives and reads
out the qubits described here, one layer deeper into whichever platform
a reader is most drawn to. **Physical Qubit Platforms** closes on that
note.
