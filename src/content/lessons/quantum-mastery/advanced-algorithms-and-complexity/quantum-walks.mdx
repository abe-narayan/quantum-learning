import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemMetaForLesson } from "@/lib/problems/metaRegistry";
import { Complex } from "@/lib/quantum/complex";
import { GraphDiagram } from "@/components/visualizations/GraphDiagram";

export const practiceProblems = getProblemMetaForLesson(
  "quantum-mastery/advanced-algorithms-and-complexity/quantum-walks"
);

export function hadamardWalk(steps) {
  const size = 2 * steps + 1;
  const offset = steps;
  let up = new Array(size).fill(Complex.ZERO);
  let down = new Array(size).fill(Complex.ZERO);
  const s = Math.SQRT1_2;
  up[offset] = new Complex(s, 0);
  down[offset] = new Complex(0, s);

  for (let t = 0; t < steps; t++) {
    const newUp = new Array(size).fill(Complex.ZERO);
    const newDown = new Array(size).fill(Complex.ZERO);
    for (let x = 0; x < size; x++) {
      const a0 = up[x];
      const a1 = down[x];
      const coinUp = a0.add(a1).scale(s);
      const coinDown = a0.sub(a1).scale(s);
      if (x - 1 >= 0) newUp[x - 1] = newUp[x - 1].add(coinUp);
      if (x + 1 < size) newDown[x + 1] = newDown[x + 1].add(coinDown);
    }
    up = newUp;
    down = newDown;
  }

  const probs = new Array(size).fill(0);
  let variance = 0;
  for (let x = 0; x < size; x++) {
    const p = up[x].magnitudeSquared() + down[x].magnitudeSquared();
    probs[x] = p;
    const pos = x - offset;
    variance += pos * pos * p;
  }
  return { probs, variance, offset };
}

export function classicalWalk(steps) {
  const size = 2 * steps + 1;
  const offset = steps;
  let p = new Array(size).fill(0);
  p[offset] = 1;
  for (let t = 0; t < steps; t++) {
    const next = new Array(size).fill(0);
    for (let x = 0; x < size; x++) {
      if (p[x] === 0) continue;
      if (x - 1 >= 0) next[x - 1] += 0.5 * p[x];
      if (x + 1 < size) next[x + 1] += 0.5 * p[x];
    }
    p = next;
  }
  let variance = 0;
  for (let x = 0; x < size; x++) {
    const pos = x - offset;
    variance += pos * pos * p[x];
  }
  return { probs: p, variance, offset };
}

export const walkStepValues = [10, 20, 40, 80, 160];
export const walkResults = walkStepValues.map((steps) => {
  const q = hadamardWalk(steps);
  const c = classicalWalk(steps);
  return { steps, quantumVariance: q.variance, classicalVariance: c.variance, quantumRatio: q.variance / (steps * steps), classicalRatio: c.variance / steps };
});

export const walkCurveFrames = [
  {
    paramLabel: "",
    series: [
      { label: "quantum ⟨x²⟩ (Hadamard walk)", color: "brand", points: walkResults.map((r) => ({ x: r.steps, y: r.quantumVariance })) },
      { label: "classical ⟨x²⟩ (random walk)", color: "muted", points: walkResults.map((r) => ({ x: r.steps, y: r.classicalVariance })) },
    ],
  },
];

export const walk4 = hadamardWalk(4);
export const classical4 = classicalWalk(4);
export const walk4Bars = Array.from({ length: 9 }, (_, i) => ({
  label: String(i - 4),
  value: walk4.probs[i],
  caption: walk4.probs[i].toFixed(4),
}));
export const classical4Bars = Array.from({ length: 9 }, (_, i) => ({
  label: String(i - 4),
  value: classical4.probs[i],
  caption: classical4.probs[i].toFixed(4),
}));

export const walkExplorerSteps = [0, 4, 8, 16, 24, 32, 40];
export const walkExplorerFrames = walkExplorerSteps.map((steps) => {
  const walk = hadamardWalk(steps);
  const bars = [];
  for (let i = 0; i < walk.probs.length; i += 2) {
    bars.push({
      label: String(i - walk.offset),
      value: walk.probs[i],
      caption: walk.probs[i].toFixed(4),
    });
  }
  return { paramLabel: `T = ${steps}`, bars };
});

export function reachablePositions(offset) {
  const positions = [];
  for (let i = 0; i <= 2 * offset; i++) {
    if ((i - offset) % 2 === 0) positions.push(i);
  }
  return positions;
}

export function walkGraphNodes(result) {
  const idxs = reachablePositions(result.offset);
  const mean = 1 / idxs.length;
  return idxs.map((i, k) => ({
    id: String(i - result.offset),
    label: String(i - result.offset),
    x: 0.04 + (k / (idxs.length - 1)) * 0.92,
    y: 0.5,
    group: result.probs[i] > mean ? 1 : 0,
  }));
}

export function walkGraphEdges(nodes) {
  const edges = [];
  for (let k = 0; k < nodes.length - 1; k++) {
    edges.push({ from: nodes[k].id, to: nodes[k + 1].id });
  }
  return edges;
}

export const walkGraphSteps = 12;
export const walkGraphQuantum = hadamardWalk(walkGraphSteps);
export const walkGraphClassical = classicalWalk(walkGraphSteps);
export const walkGraphQuantumNodes = walkGraphNodes(walkGraphQuantum);
export const walkGraphClassicalNodes = walkGraphNodes(walkGraphClassical);
export const walkGraphQuantumEdges = walkGraphEdges(walkGraphQuantumNodes);
export const walkGraphClassicalEdges = walkGraphEdges(walkGraphClassicalNodes);

export const lessonMeta = {
  title: "Quantum Walks",
  description:
    "Deriving the coined discrete-time quantum walk on a line via a coin operator and conditional shift, deriving its momentum-space dispersion relation to show the spreading is ballistic (∝t) rather than the classical walk's diffusive (∝√t), verified numerically against a real classical random walk, plus the continuous-time walk's exact identity with Hamiltonian simulation of a graph's adjacency matrix.",
  course: "advanced-algorithms-and-complexity",
  module: "quantum-walks",
  order: 3,
  difficulty: "master",
  estimatedMinutes: 40,
  prerequisites: [
    "quantum-mastery/advanced-algorithms-and-complexity/hamiltonian-simulation-and-trotterization",
    "quantum-computing/quantum-algorithms-i/grovers-algorithm-oracle-and-diffusion",
  ],
  objectives: [
    "Derive the coined discrete-time quantum walk on a line from a coin operator and a conditional shift operator",
    "Derive the walk's momentum-space dispersion relation and show it forces ballistic (∝t) rather than diffusive (∝√t) spreading",
    "Verify ballistic vs. diffusive spreading numerically for a real coined walk against a real classical random walk, and connect continuous-time quantum walks to Hamiltonian simulation of a graph's adjacency matrix",
  ],
};

<LessonHook eyebrow="Diffusive versus ballistic">
  A classical random walker spreads as $O(\sqrt T)$. A quantum walker,
  on the same line with the same step size, spreads
  ballistically — $O(T)$, quadratically farther.
</LessonHook>

## Motivation

A classical random walker on a line diffuses: after $T$ steps its
typical displacement is $O(\sqrt T)$, the signature of a sum of
independent random steps (the central limit theorem). The quantum
walker's ballistic spread is not a vague claim about "quantum
speedup": it's a precise,
derivable fact about interference between paths that a classical walk's
independent, memoryless steps can never produce, and it's the mechanism
<Term id="quantum-walks">behind quantum walk search algorithms</Term> achieving the same quadratic
Grover-style speedup on graph search problems.

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/a/ad/Yakir_aharonov.jpg"
  alt="Yakir Aharonov, physicist who co-introduced the quantum random walk"
  caption="Aharonov, Davidovich, and Zagury's 1993 paper 'Quantum Random Walks' first framed a coin-and-shift process on a line as a unitary evolution rather than a quantum-dressed classical Markov chain — the object this lesson diagonalizes to derive its dispersion relation. The same coined-walk construction now underlies quantum walk search algorithms and has been physically realized with trapped ions and single photons."
  credit="Kobi Kalmanowitz / Wikimedia Commons"
  creditUrl="https://commons.wikimedia.org/wiki/File:Yakir_aharonov.jpg"
  license="CC BY-SA 3.0"
/>

## Mathematical Development

### The coined walk: coin operator and conditional shift

The walker's Hilbert space is $\mathcal{H}_{\text{position}}\otimes
\mathcal{H}_{\text{coin}}$: an unbounded position register (indexed by
integer $x$) tensored with a single qubit "coin," $\{|\uparrow\rangle,
|\downarrow\rangle\}$. One step is coin-flip-then-shift:

$$
W = S\cdot(I\otimes H), \qquad H=\frac1{\sqrt2}\begin{pmatrix}1&1\\1&-1\end{pmatrix}
$$

$H$ is the ordinary Hadamard gate, now interpreted as mixing "move left"
and "move right" instead of $|0\rangle,|1\rangle$. $S$ is the
**conditional shift**: $S|x\rangle|\uparrow\rangle=|x-1\rangle
|\uparrow\rangle$, $S|x\rangle|\downarrow\rangle=|x+1\rangle
|\downarrow\rangle$, move left if the coin reads $\uparrow$, right if
$\downarrow$, entangling position with coin at every single step. Both
$H$ (unitary on the coin) and $S$ (a pure permutation of an orthonormal
basis, hence unitary) are unitary, so $W$ is unitary and the walk is
norm-preserving quantum evolution, not a classical Markov chain
dressed up in bra-ket notation.

Starting from $|0\rangle\otimes|\psi_c\rangle$ and applying $W$ exactly
$T$ times gives the walk's state after $T$ steps; measuring position
gives a probability distribution over $x$, like the classical
walk's distribution over final positions after $T$ coin flips, but built
from *interfering amplitudes* rather than summed probabilities.

**Initial coin state.** Starting in $|\uparrow\rangle$ produces an
asymmetric distribution (the Hadamard coin doesn't treat $\uparrow$ and
$\downarrow$ symmetrically). The standard fix (Konno 2002, cited here
rather than re-derived) is the symmetric initial coin state
$|\psi_c\rangle=\frac1{\sqrt2}(|\uparrow\rangle+i|\downarrow\rangle)$,
which makes the resulting position distribution exactly symmetric under
$x\to-x$, the choice used throughout this lesson's numerics below.

### Why the spreading is ballistic: the dispersion relation

Because the walk is translation-invariant (every position behaves
identically), it's diagonalized by momentum eigenstates $|k\rangle$,
as in Momentum Space and the Fourier Transform: translation by
one unit acts on $|k\rangle$ as multiplication by a pure phase. Shifting
by $-1$ (the $\uparrow$ branch) contributes phase $e^{ik}$, shifting by
$+1$ (the $\downarrow$ branch) contributes $e^{-ik}$, so in the
$2$-dimensional coin subspace at fixed momentum $k$, one step acts as
the $2\times2$ unitary

$$
W(k) = \begin{pmatrix}e^{ik}&0\\0&e^{-ik}\end{pmatrix}H = \frac1{\sqrt2}\begin{pmatrix}e^{ik}&e^{ik}\\e^{-ik}&-e^{-ik}\end{pmatrix}
$$

Its trace is $\frac{e^{ik}-e^{-ik}}{\sqrt2}=i\sqrt2\sin k$, and its
determinant is $(1)(-1)=-1$. Writing $W(k)$'s two eigenvalues as
$e^{i(\pi/2\pm\omega(k))}$ (a valid parametrization exactly when their
product is $-1$, matching the determinant), their sum is
$i\cdot2\cos\omega(k)$, so matching the trace:

$$
\boxed{\cos\omega(k) = \frac{\sin k}{\sqrt2}}
$$

a derived **dispersion relation**. Since $|\sin k|\leq1$,
$\cos\omega(k)\in[-\tfrac1{\sqrt2},\tfrac1{\sqrt2}]$ always; $\omega(k)$
never approaches $0$ or $\pi$, unlike a classical walk's trivial
"dispersion." Differentiating $\cos\omega=\sin(k)/\sqrt2$ implicitly:

$$
v(k) \equiv \frac{d\omega}{dk} = \frac{-\cos k}{\sqrt2\sin\omega(k)}, \qquad \sin\omega(k)=\sqrt{1-\tfrac{\sin^2k}2}
$$

so $v(k)^2=\dfrac{\cos^2k}{2-\sin^2k}=\dfrac{\cos^2k}{1+\cos^2k}$, an
increasing function of $u=\cos^2k\in[0,1]$, maximized at $u=1$ ($k=0$ or
$\pi$): $v_{\max}^2=\tfrac12$, i.e. $v_{\max}=\tfrac1{\sqrt2}$.

A wavepacket built from a superposition of momentum modes has each mode
$k$ moving at its own group velocity $v(k)$; after $T$ steps, mode $k$'s
contribution sits near $x\approx v(k)\,T$ (stationary-phase reasoning,
the same principle used for wave-packet spreading in Wave Mechanics).
Since **every** mode's speed is bounded by $v_{\max}=1/\sqrt2$, the
entire distribution stays confined to a "light cone" $|x|\lesssim T/
\sqrt2$ that grows *linearly* in $T$, precisely what
**ballistic** spreading means, and it's derived directly from the
dispersion relation's bounded-but-nonzero group velocity, not merely
observed after the fact. The exact limiting probability density inside
this light cone, and the resulting exact variance prefactor
$(1-1/\sqrt2)$ used below, is a more technical stationary-phase integral
(Konno 2002; Nayak & Vishwanath 2000), cited here rather than re-derived
in full; but the light cone's *linear* growth, the qualitative and
quantitative origin of ballistic-vs-diffusive scaling, is fully derived
above.

### Contrast: the classical walk is diffusive by the central limit theorem

A classical unbiased walk's position after $T$ steps is a sum of $T$
independent $\pm1$ steps, each with variance $1$; by linearity of
variance for independent random variables, $\text{Var}(x_T)=T$ exactly,
an $O(\sqrt T)$ typical displacement, growing strictly slower than the
quantum walk's $O(T)$. No dispersion relation or interference is
involved at all; this is pure independence.

## Physical Interpretation

The classical walk forgets its history at every step: each $\pm1$ move
is drawn independently, so the central limit theorem's $\sqrt T$
scaling is inevitable. The quantum walk never forgets: every path to a
given final position interferes with every other path reaching the same
position, and the dispersion relation derived above shows that
interference doesn't just perturb the classical answer, it changes the
power law entirely, from diffusive to ballistic. This is the same kind
of interference-driven speedup Grover's algorithm exploits (quadratic in
its own arena), here appearing as a literal quadratic difference in how
fast probability mass can move through space.

## Worked Example

**Compute both distributions after $T=4$ steps by direct simulation (a
self-contained implementation using this platform's real `Complex`
arithmetic for the quantum amplitudes, and a direct classical
Markov-chain recursion for the classical walk) and compare.**

<BarChart bars={walk4Bars} ariaLabel="Quantum (Hadamard coin) walk position probability distribution after 4 steps, positions -4 to 4" maxValue={0.4} />

<BarChart bars={classical4Bars} ariaLabel="Classical random walk position probability distribution after 4 steps, positions -4 to 4" maxValue={0.4} />

Both distributions are supported only on positions with the same parity
as $T=4$ (even positions), as they must be: every step changes position
by exactly $\pm1$, so after an even number of steps only an even
displacement is reachable. But their *shapes* are already visibly
different at this small scale: the classical distribution is
single-peaked at the center (binomial-shaped), while the quantum
distribution is bimodal, with probability pulled *out* toward the edges
$x=\pm2$ (each at $0.375$) rather than concentrated at the center
($0.125$ at $x=0$), an early, visible sign of the light cone derived
above pushing probability outward ballistically rather than letting it
diffuse inward.

**Watch the light cone open up: scrub $T$ from $0$ to $40$.**

<BarChartExplorer
  sliderLabel="Steps T"
  ariaLabel="Quantum (Hadamard coin) walk position probability distribution, scrubbing the number of steps T from 0 to 40, showing the two horns pulling apart and the support widening at a constant, linear rate in T"
  frames={walkExplorerFrames}
  maxValue={1}
/>

At $T=0$ all the probability sits at the origin. As $T$ increases the
distribution splits into the same bimodal horns seen above at $T=4$,
and the horns march outward at a *constant rate*: the support spans
roughly $[-T/\sqrt2,\,T/\sqrt2]$ at every frame, the linearly
growing light cone derived from the bounded group velocity above. Each
horn's peak height *generally shrinks* as $T$ grows (the same total
probability of $1$ spread over a linearly wider region), the opposite
of a classical bell curve, which stays single-peaked at the center and
widens only $\propto\sqrt T$, quadratically slower; the trend is not
perfectly monotonic frame to frame (scrub from $T=32$ to $T=40$ and the
peak ticks back up slightly, an interference fringe near the
light cone's edge, not a bug; see Common Mistakes below), but the
overall envelope shrinking against the classical curve's fixed center
height is unmistakable.

### The same data, redrawn as a graph

A bar chart plots probability against position; laying the reachable
positions out as an actual line graph, nodes lit up when their
probability exceeds the average $1/(\text{number of reachable
positions})$, makes the *shape* of where probability sits easier to
read at a glance than a skyline of bars. At $T=12$ (13 reachable
positions, mean threshold $\approx0.0769$):

<GraphDiagram
  nodes={walkGraphQuantumNodes}
  edges={walkGraphQuantumEdges}
  ariaLabel="Quantum (Hadamard) walk after 12 steps, positions -12 to 12 drawn as a line graph, with nodes lit above-average probability: two separate lit clusters near positions -8 to -4 and 4 to 8, with both the center and the far edges dark"
/>

<GraphDiagram
  nodes={walkGraphClassicalNodes}
  edges={walkGraphClassicalEdges}
  ariaLabel="Classical random walk after 12 steps, positions -12 to 12 drawn as a line graph, with nodes lit above-average probability: a single lit cluster centered at position 0 spanning -4 to 4, with the far edges dark"
/>

The classical walk lights up one cluster, centered on the origin, and
goes dark toward the edges: probability mass diffusing outward from
where it started, thinning as it goes. The quantum walk lights up *two*
clusters, straddling $\pm8\approx T/\sqrt2$ — the light-cone edge
derived above — while the center goes dark: the "pushed
outward, not spread inward" signature the bar charts above already
showed, now visible as literal geography on the line the walker moves
along rather than as bar heights.

<PredictBeforeReveal
  question="Quadruple the number of steps T (say from 10 to 40). The classical walk's typical displacement scales as √T. What happens to the quantum walk's typical displacement, which scales as T?"
  options={[
    { label: "It also roughly doubles, same as the classical walk", value: "double" },
    { label: "It roughly quadruples — the same factor T was multiplied by", value: "quadruple" },
    { label: "It grows by 16×, the square of the classical factor", value: "sixteen" },
  ]}
  correctValue="quadruple"
  explanation="Classical displacement ∝ √T, so quadrupling T only doubles it (√4=2). That is the diffusive slowdown. Quantum displacement ∝ T directly, so quadrupling T quadruples it. The table below shows this: going from T=10 to T=40, √(classical variance) goes from √10≈3.16 to √40≈6.32 (doubled), while √(quantum variance) goes from √29.95≈5.47 to √469.1≈21.66 (quadrupled) — the same gap the dispersion relation's bounded group velocity predicts, now checked against real simulated numbers instead of just the scaling exponent."
/>

**Scaling check across $T=10,20,40,80,160$:**

<ParametricCurve
  frames={walkCurveFrames}
  ariaLabel="Quantum walk variance and classical walk variance, both plotted against number of steps T, showing quantum growing much faster (quadratically) than classical (linearly)"
/>

| $T$ | quantum $\langle x^2\rangle$ | classical $\langle x^2\rangle$ | quantum$/T^2$ | classical$/T$ |
|---|---|---|---|---|
| $10$ | $29.953$ | $10.000$ | $0.2995$ | $1.0000$ |
| $20$ | $117.595$ | $20.000$ | $0.2940$ | $1.0000$ |
| $40$ | $469.095$ | $40.000$ | $0.2932$ | $1.0000$ |
| $80$ | $1875.002$ | $80.000$ | $0.2930$ | $1.0000$ |
| $160$ | $7498.565$ | $160.000$ | $0.2929$ | $1.0000$ |

`classical/T` sits at exactly $1.0000$ throughout: the exact classical
$\text{Var}(x_T)=T$ result derived above, reproduced by direct
Markov-chain simulation, not just asserted. `quantum/T²` converges to
$0.2929\ldots$ as $T$ grows, matching $1-\tfrac1{\sqrt2}=0.292893\ldots$
to four decimal places, the cited Konno constant confirmed directly by
this lesson's own real simulation, not merely quoted from a textbook.

## Common Mistakes

<Callout type="warning">
**Assuming any quantum evolution on a line automatically spreads
ballistically.** It's specifically the *bounded, nonzero* group velocity
from the dispersion relation that forces ballistic spreading. A trivial
"coin" (e.g. the identity instead of Hadamard) gives no shift alternation
at all, and other coin/shift constructions can behave differently; the
Hadamard walk's ballistic scaling is a derived consequence of
*this* dispersion relation, not a generic fact about "being quantum."
</Callout>

<Callout type="warning">
**Confusing the light cone's edge (at $|x|\approx T/\sqrt2$, from the
maximum group velocity) with the variance prefactor ($1-1/\sqrt2$, from
the full limiting density).** They're related but numerically distinct
quantities: the light cone bounds where probability *can* be, while the
variance integrates the actual (highly non-uniform, in fact peaked near
the edges, as the $T=4$ worked example already shows) density within it.
</Callout>

<Callout type="note">
**Expecting the horn peak to shrink *monotonically* at every step.**
This lesson's own explorer above shows otherwise: computed directly
with the same `hadamardWalk` function driving the frames, the peak
height across $T=0,4,8,16,24,32,40$ is $1.000,\,0.375,\,0.2266,
\,0.2105,\,0.1596,\,0.1156,\,0.1235$, a real, reproducible *increase*
from $T=32$ to $T=40$, not a mistake to hide. The overall $1/\sqrt T$-
style envelope (peak $\times$ light-cone width stays order-$1$, since
probability is conserved) does shrink on average, but the exact density
inside the light cone has interference fringes near its edges,
so the peak — which tracks whichever fringe happens to be tallest at a
given $T$ — can tick up locally even while the envelope it rides on is
trending down. This is the discrete-walk analog of the fringes in a
diffraction pattern getting locally brighter or dimmer while the
envelope narrows.
</Callout>

## Practice Questions

1. Using $\text{Var}(x_T)=T$ for the classical walk (derived above),
   compute the classical walk's typical displacement $\sqrt{\text{Var}}$
   at $T=10000$, and compare it to the quantum walk's typical
   displacement $\sqrt{(1-1/\sqrt2)}\,T$ at the same $T$.
2. From the dispersion relation $\cos\omega(k)=\sin(k)/\sqrt2$, verify
   directly that $k=\pi/2$ gives $\omega=\pi/2$ and (using the group
   velocity formula) $v(\pi/2)=0$, a "trapped," non-propagating mode.
   What feature of the $T=4$ worked example's quantum distribution (a
   local peak or dip at the center) is this mode responsible for?
3. Explain, using only the fact that classical steps are independent,
   why $\text{Var}(x_T)=T$ exactly for *any* number of classical steps
   $T$, with no large-$T$ approximation needed; contrast this with the
   quantum result, which is only an asymptotic ($T\to\infty$) statement.
4. A continuous-time quantum walk on a graph with adjacency matrix $A$
   evolves by $i\,d|\psi\rangle/dt=A|\psi\rangle$. Explain precisely why
   this is *literally* an instance of Hamiltonian simulation from the
   previous lesson, with $H=A$, and why Trotterization becomes the
   practical tool for running it on a graph whose adjacency matrix is a
   sum of non-commuting pieces (e.g. edge sets from different graph
   colors).

<PracticeLinks problems={practiceProblems} />

## Summary

- The coined quantum walk is built from a coin operator (Hadamard, here)
  and a conditional shift entangling position with coin at every step,
  both unitary, so the whole walk is unitary evolution.
- Diagonalizing in momentum space gives an exact dispersion relation
  $\cos\omega(k)=\sin(k)/\sqrt2$, whose bounded, nonzero group velocity
  (maximum $1/\sqrt2$, derived via calculus, not asserted) forces
  ballistic ($\propto T$, so $\langle x^2\rangle\propto T^2$) spreading.
- The classical random walk's $\text{Var}(x_T)=T$ follows directly from
  independence (central limit theorem territory), diffusive,
  confirmed by direct simulation to be exactly $T$ at every $T$ tested.
- A real coined-walk simulation (using this platform's `Complex`
  arithmetic) confirms $\langle x^2\rangle/T^2\to1-1/\sqrt2\approx0.2929$
  as $T$ grows, matching the cited Konno constant to four decimal
  places, computed, not quoted.
- Continuous-time quantum walks ($i\,d|\psi\rangle/dt=A|\psi\rangle$ for
  graph adjacency matrix $A$) are literally Hamiltonian simulation with
  $H=A$, directly connecting to the previous lesson's Trotterization
  machinery for graphs whose adjacency matrix doesn't split into
  commuting pieces.

## Further Exploration

<NextDiscovery>
  [Phase Estimation Precision and QFT Depth](/lessons/quantum-mastery/advanced-algorithms-and-complexity/phase-estimation-precision-and-qft-depth)
  takes up the same move this lesson's dispersion analysis made, reading
  a continuous parameter out of an interference pattern, and asks what it
  costs when the parameter is a phase with no exact representation in any
  finite register. The answer is a precision-versus-depth tradeoff rather
  than a clean formula.
</NextDiscovery>
