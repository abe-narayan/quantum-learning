import { PracticeLinks } from "@/components/problems/PracticeLinks";
import { getProblemMetaForLesson } from "@/lib/problems/metaRegistry";
import { ExternalFigure } from "@/components/mdx/ExternalFigure";
import { TheoremBox } from "@/components/mdx/TheoremBox";
import { DefinitionBox } from "@/components/mdx/DefinitionBox";
import { Complex } from "@/lib/quantum/complex";
import { StateVector } from "@/lib/quantum/state";
import { pureStateDensityMatrix, vonNeumannEntropy } from "@/lib/quantum/densityMatrix";
import { reducedDensityMatrixQubit0 } from "@/lib/quantum/partialTrace";
import { BarChart } from "@/components/visualizations/BarChart";

export const practiceProblems = getProblemMetaForLesson(
  "quantum-mastery/quantum-shannon-theory/entanglement-distillation-and-typical-subspaces"
);

export const lessonMeta = {
  title: "Entanglement Distillation and Typical Subspaces",
  description:
    "Turns Schmidt Decomposition and Purification's abstract entanglement entropy into an operational quantity: the typical-subspace argument behind entanglement concentration, computed concretely, that says exactly how many near-perfect Bell pairs n copies of a partially-entangled state yield.",
  course: "quantum-shannon-theory",
  module: "entanglement-distillation-and-typical-subspaces",
  order: 5,
  difficulty: "master",
  estimatedMinutes: 27,
  prerequisites: [
    "quantum-mastery/quantum-shannon-theory/quantum-entropy-and-information-measures",
    "quantum-mastery/quantum-information-theory/schmidt-decomposition-and-purification",
  ],
  objectives: [
    "Explain entanglement distillation: converting many copies of a partially-entangled (or noisy) state into fewer, nearly-perfect Bell pairs using only LOCC",
    "State the typical subspace idea: for many i.i.d. copies of a state, almost all of the probability weight concentrates on a subspace of dimension approximately 2^(n S(rho)), not the full 2^n-dimensional space",
    "Connect the typical-subspace dimension directly to the distillable entanglement rate, and to Schumacher compression's classical analogue",
  ],
  related: [
    {
      slug: "quantum-mastery/quantum-information-theory/schmidt-decomposition-and-purification",
      note: "The n-copy joint Schmidt coefficients this lesson builds a typical subspace out of are what that lesson's SVD-based theorem guarantees exist for any bipartite pure state, applied here to |psi>^⊗n itself.",
    },
  ],
};

export const pWorked = 0.9;

export const psiWorked = new StateVector([
  new Complex(Math.sqrt(pWorked)),
  new Complex(0),
  new Complex(0),
  new Complex(Math.sqrt(1 - pWorked)),
]);

export const rhoAWorked = reducedDensityMatrixQubit0(pureStateDensityMatrix(psiWorked));
export const entropyWorked = vonNeumannEntropy(rhoAWorked);

export const binomialCoefficient = (n, k) => {
  let result = 1;
  for (let i = 0; i < k; i += 1) {
    result = (result * (n - i)) / (i + 1);
  }
  return result;
};

export const schmidtTypeDistribution = (n, p) => {
  const q = 1 - p;
  return Array.from({ length: n + 1 }, (_, k) => {
    const count = binomialCoefficient(n, k);
    const probability = count * Math.pow(p, k) * Math.pow(q, n - k);
    return { k, count, probability };
  });
};

export const n6 = 6;
export const dist6 = schmidtTypeDistribution(n6, pWorked);
export const mean6 = n6 * pWorked;
export const stdDev6 = Math.sqrt(n6 * pWorked * (1 - pWorked));
export const typicalLower6 = mean6 - stdDev6;
export const typicalUpper6 = mean6 + stdDev6;
export const isTypical6 = (k) => k >= typicalLower6 && k <= typicalUpper6;
export const typicalEntries6 = dist6.filter((entry) => isTypical6(entry.k));
export const typicalMass6 = typicalEntries6.reduce((sum, entry) => sum + entry.probability, 0);
export const complementMass6 = 1 - typicalMass6;
export const typicalDimension6 = typicalEntries6.reduce((sum, entry) => sum + entry.count, 0);
export const totalDimension6 = Math.pow(2, n6);
export const predictedTypicalDimension6 = Math.pow(2, n6 * entropyWorked);

export const individualLambdaAllZero6 = Math.pow(pWorked, n6);
export const individualLambdaOneFlip6 = Math.pow(pWorked, n6 - 1) * (1 - pWorked);
export const flatnessRatio6 = individualLambdaAllZero6 / individualLambdaOneFlip6;

export const distillableBellPairsWorked = n6 * entropyWorked;

export const distributionBars6 = dist6.map((entry) => ({
  label: `k=${entry.k}`,
  value: entry.probability,
  highlight: isTypical6(entry.k),
  caption: `${(entry.probability * 100).toFixed(2)}%`,
}));

<LessonHook eyebrow="Turning imperfect entanglement into the real thing">
  Share many copies of a partially entangled state and, using nothing
  but local operations and classical communication, distill them into a
  smaller number of nearly perfect Bell pairs. The reason it works is a
  single sharp idea: almost all the probability mass concentrates on a
  comparatively tiny typical subspace.
</LessonHook>

## Entropy as a Rate, Not a Description

The Data-Processing Inequality treated von Neumann entropy and mutual
information as abstract bookkeeping quantities: numbers a channel or a
state carries, useful for proving inequalities. This lesson asks a much
more physical question about entanglement specifically: if two parties,
Alice and Bob, share $n$ copies of a *partially* entangled state, not a
perfect Bell pair, can they use only **local operations and classical
<Term id="locc">communication (LOCC)</Term>** (no direct interaction between them, which is
exactly the restriction that makes entanglement a resource rather than
something either party could produce alone) to convert those $n$
imperfect copies into a smaller number of *nearly perfect* Bell pairs?
The answer is yes: the protocol is called **entanglement distillation**,
and the idea behind it is adapted directly from Schumacher's proof
that quantum data can be compressed: for many
identical copies of a state, almost all of the probability mass
concentrates on a comparatively tiny **typical subspace**. This lesson
derives that idea from scratch and then computes the numbers, instead of
stopping at the clean asymptotic formula.

## The Typical Subspace, and Concentration Inside It

### Setup: $n$ copies and their joint Schmidt coefficients

Take the partially entangled 2-qubit state

$$
|\psi\rangle = \sqrt{p}\,|00\rangle + \sqrt{1-p}\,|11\rangle, \qquad p\neq \tfrac12,
$$

already in Schmidt form (Schmidt Decomposition and Purification), with
Schmidt coefficients $p$ and $1-p$ and entanglement entropy
$S(\rho_A)=H(p)\equiv -p\log_2 p-(1-p)\log_2(1-p)$, the **binary Shannon
entropy**: the classical quantity this course's entropy lesson
built, now reappearing because $\rho_A=\mathrm{diag}(p,1-p)$ is already
diagonal, so its von Neumann entropy is *literally* a classical Shannon
entropy with no further work.

Now take $n$ independent copies, $|\psi\rangle^{\otimes n}$, still a
single bipartite pure state (system $A$ = all $n$ of Alice's qubits,
system $B$ = all $n$ of Bob's). Expanding the tensor power term by term,
every one of the $2^n$ terms is fixed by choosing, independently for
each of the $n$ copies, whether that copy landed in the $|00\rangle$
branch (weight $\sqrt p$) or the $|11\rangle$ branch (weight $\sqrt{1-p}$).
A term with exactly $k$ of its $n$ copies in the $|00\rangle$ branch
(and $n-k$ in the $|11\rangle$ branch) contributes Schmidt coefficient

$$
\lambda_k = p^{\,k}(1-p)^{\,n-k}
$$

and $|\psi\rangle^{\otimes n}$ is *already* in Schmidt form over the
$2^n$-dimensional computational basis on each side (every term is a
distinct, orthogonal product basis state on each side, paired index for
index, the Schmidt structure Schmidt Decomposition and
Purification proved holds for any bipartite pure state). There are
$\binom{n}{k}$ terms of type $k$ (choosing which $k$ of the $n$ copies
are the $|00\rangle$ ones), each carrying the same coefficient $\lambda_k$,
so the *total* Schmidt weight at type $k$ is

$$
P(k) = \binom{n}{k}\,p^{k}(1-p)^{n-k}
$$

This is exactly the **binomial distribution** $\mathrm{Binomial}(n,p)$
evaluated at $k$, with mean $np$: since $p$ is the *larger* of the two
branch weights whenever $p>\tfrac12$, the typical types cluster near
$k\approx np$, i.e. near "almost all copies in the $|00\rangle$ branch,"
matching intuition.

### The typical subspace

<TheoremBox title="Typical subspace concentration (Schumacher)" provenance="cited">

**Hypotheses.** The $n$ copies are **i.i.d.**: independent, and all in
the same state $|\psi\rangle$ with the same Schmidt coefficients. That
is what makes the type counts binomial and lets the law of large numbers
apply at all. The local dimension is finite and fixed, $\epsilon>0$ is
fixed *before* the limit is taken, and every statement below is
asymptotic in $n$ with $\epsilon$ held still. Nothing here holds at fixed
$n$; the worked example below is a trend, not an instance.

By the law of large numbers (this is the **asymptotic equipartition
property**), as $n\to\infty$ the binomial distribution
above concentrates its probability mass on types whose fraction of
$|00\rangle$-branch copies, $k/n$, lies within any fixed $\epsilon>0$ of
$p$ (equivalently, whose exponential rate $-\tfrac1n\log_2\lambda_k$ lies
within $\epsilon$ of $H(p)$). Call this the **typical set** $T_\epsilon$. Its probability
mass $\to 1$ as $n\to\infty$, while its *size* is only
$|T_\epsilon|\approx 2^{nH(p)}$, exponentially smaller than the full
space of dimension $2^n$ whenever $H(p)<1$, i.e. whenever $|\psi\rangle$
is not already maximally entangled.

</TheoremBox>

This is the same theorem, applied to Schmidt coefficients instead of
classical letter probabilities, that Shannon's source coding theorem and
its quantum analogue (Schumacher compression) both rest on: a source
that looks like it needs $n$ bits (or qubits) to describe can actually
be compressed into only $\approx nH$ of them, because only the typical
outcomes ever occur. Here the same counting argument says
something about *entanglement* rather than compression: the typical
subspace, restricted to just its $\approx 2^{nH(p)}$ dimensions, carries
almost all of $|\psi\rangle^{\otimes n}$'s probability weight, and its
Schmidt coefficients are all *nearly equal* to
$2^{-nH(p)}$ within that subspace (every typical type $k$ has
$-\tfrac1n\log_2\lambda_k\approx H(p)$ by definition, so
$\lambda_k\approx 2^{-nH(p)}$ for every one of them). A state whose
nonzero Schmidt coefficients are all nearly equal is, by definition,
nearly **maximally entangled** within that subspace.

### Entanglement concentration: distilling Bell pairs from the typical subspace

<DefinitionBox title="Entanglement concentration">

Alice performs a local projective measurement onto the typical subspace
(a real quantum operation: project her $n$ qubits onto the subspace
spanned by typical strings, and discard the rare failure outcome). With
probability $\to 1$ as $n\to\infty$, this succeeds, and the post-projection
state is nearly maximally entangled across a space of dimension
$\approx 2^{nH(p)}$. Relabeling that space's basis (a local unitary on
each side, no communication needed beyond agreeing on the labeling in
advance) turns it into $m\approx nH(p)$ ordinary qubits per side in a
state extremely close to $m$ perfect Bell pairs $|\Phi^+\rangle^{\otimes m}$.

</DefinitionBox>

This protocol, due to Bennett, Bernstein, Popescu, and Schumacher (1996),
is called **entanglement concentration**: it is the special, cleanest
case of general distillation, restricted to *pure* partially-entangled
states, and it is worth deriving explicitly because every step above is
a checkable claim (a projection, a counting argument, a
relabeling), not a black box. The achievable rate,
$m/n\to H(p)=S(\rho_A)$ Bell pairs per copy, is also *optimal*: no LOCC
protocol on pure states can do better, because entanglement entropy
cannot increase on average under LOCC for pure bipartite states (a
standard entanglement-monotone result, cited here rather than proved
anywhere in this track).

<ExternalFigure
  src="https://upload.wikimedia.org/wikipedia/commons/8/80/Dr._Charles_Bennett_IBM_Fellow.jpg"
  alt="Charles H. Bennett in a portrait made for IBM Research, where he holds the title of IBM Fellow, shown from the chest up against a neutral background"
  caption="Charles H. Bennett, the 'B' in Bennett-Bernstein-Popescu-Schumacher (1996), the paper that turns this lesson's typical-subspace counting argument into the entanglement-concentration protocol above. Bennett's earlier work with Gilles Brassard on BB84 and with colleagues on teleportation and superdense coding makes him a recurring figure across this platform's quantum information lessons. It is the same collaboration structure (a shared-entanglement resource, converted between forms by LOCC) running through all of them."
  credit="IBM Research, via Wikimedia Commons"
  creditUrl="https://commons.wikimedia.org/wiki/File:Dr._Charles_Bennett_IBM_Fellow.jpg"
  license="CC BY-SA 2.0"
/>

### Beyond pure states: mixed states and coherent information

Real shared entanglement is never a perfect, isolated pure state:
transmission through fiber or free space adds noise, so Alice and Bob
share $n$ copies of some *mixed* state $\rho_{AB}$. The typical
subspace idea still applies (via the state's purification: distilling
$\rho_{AB}^{\otimes n}$ is equivalent to distilling entanglement from a
purification $|\psi\rangle_{ABE}$ against an inaccessible environment
$E$), but the clean projective argument above is no longer enough on
its own, because $E$ carries information that could be
correlated with $A$ or $B$ in ways a single projection doesn't remove.
Two families of general mixed-state distillation protocol handle this at
the cost of more involved LOCC. **Entanglement pumping** distills a
smaller number of higher-fidelity pairs from many noisy ones and then
iterates. **Hashing** spends many copies' worth of local Clifford
operations and classical syndrome measurements, and is structurally
close to a quantum error-correcting code. The achievable rate is bounded above by, and for
important state classes (e.g. states arising from degradable channels)
asymptotically equals, the **coherent information**
$I_c(A\rangle B)=S(\rho_B)-S(\rho_{AB})$ defined in Quantum Entropy and
Information Measures: the same quantity that lesson introduced as the
control on whether a channel can carry quantum information at all, now
reappearing as the bound on how much entanglement noisy channel *use*
can generate. That
coincidence is not an accident: it is the technical heart of this
course's own capstone on channel capacities, which this lesson's typical
subspace argument is the concrete, fully-worked special case of.

## Why Repeaters Depend on This

Distillation is not a mathematical curiosity: it is the specific reason
long-distance quantum communication is possible at all. A pair of
entangled photons sent through real optical fiber picks up loss and
decoherence over distance, so the entanglement Alice and Bob
end up sharing is never a perfect Bell pair: it degrades roughly
exponentially with distance. **Quantum repeaters** solve this by
chaining shorter, higher-fidelity entangled links and periodically
*distilling*: sacrificing some pairs' worth of noisy entanglement to
purify the rest, the entanglement-pumping idea above, before
that entanglement is used for teleportation or quantum key distribution
(QKD). Without distillation, entanglement-based QKD's security
guarantees degrade along with the fidelity of the shared state; with it,
a noisy channel can still support secure key generation, provided the
noise is below distillation's threshold.

Distillation also answers a question the Quantum Computing track's own
[Entanglement Entropy for Pure Bipartite States](/lessons/quantum-computing/entanglement-and-measurement/entanglement-entropy-for-pure-states)
lesson only ever *asserted*: why should entanglement entropy, a number
computed from an abstract density matrix's eigenvalues, be *the*
measure of "how entangled" a state is, rather than just one convenient
choice among many? This lesson supplies the missing operational answer.
**Distillable entanglement** (the asymptotic rate of perfect Bell pairs
extractable per copy by LOCC) is a measure defined purely in terms of
what a physical protocol can *do* with the state, with no
reference to entropy at all. The fact that this operational quantity
turns out to equal $S(\rho_A)$ exactly, for pure states, is what makes
entanglement entropy more than an abstract formula: it is the honest
exchange rate between raw partially-entangled resource and usable,
near-perfect entanglement, and it is provable.

## Six Copies, Enumerated

**Take $p=0.9$, so $|\psi\rangle=\sqrt{0.9}\,|00\rangle+\sqrt{0.1}\,|11\rangle$.
Compute $H(0.9)$, then take $n=6$ copies (small enough to enumerate all
$2^6=64$ Schmidt coefficients by type) and check how much of the
probability mass the typical set captures.**

`vonNeumannEntropy` applied to $\rho_A=\mathrm{diag}(0.9,0.1)$ (already
diagonal, since $|\psi\rangle$'s off-diagonal amplitudes are zero) gives

$$
H(0.9) = -0.9\log_2(0.9) - 0.1\log_2(0.1) \approx 0.468996 \text{ bits}
$$

(`entropyWorked` above, built by feeding `psiWorked` through the same
`pureStateDensityMatrix` $\to$ `reducedDensityMatrixQubit0` $\to$
`vonNeumannEntropy` pipeline the Schmidt lesson used, agrees with this
hand calculation to six decimal places, as it must, since both routes
diagonalize the same $2\times2$ matrix).

For $n=6$, recall $k$ counts how many of the six copies land in the
$|00\rangle$ branch (weight $\sqrt{0.9}$ each), so the joint Schmidt
coefficient at type $k$ is $\lambda_k=0.9^{k}\cdot0.1^{6-k}$, occurring
with multiplicity $\binom{6}{k}$, giving type-probability
$P(k)=\binom{6}{k}\,0.9^{k}\,0.1^{6-k}$:

| $k$ | $\binom{6}{k}$ | $P(k)$ |
|---|---|---|
| 0 | 1 | 0.000001 |
| 1 | 6 | 0.000054 |
| 2 | 15 | 0.001215 |
| 3 | 20 | 0.014580 |
| 4 | 15 | 0.098415 |
| 5 | 6 | 0.354294 |
| 6 | 1 | 0.531441 |

(`dist6` above, computed directly from `schmidtTypeDistribution`; the
seven values sum to exactly $1.000000$.) The mean type is
$np=6\times0.9=5.4$, with standard deviation
$\sigma=\sqrt{np(1-p)}\approx0.7348$. Defining the typical set here as
types within one standard deviation of the mean, $k\in[4.665,\,6.135]$,
gives exactly $k\in\{5,6\}$: the two largest-$k$ types, since $p=0.9$
means most copies are expected in the $|00\rangle$ branch.

That one-sigma window is a stand-in chosen so this example can be
enumerated by hand, and it is *not* the $\epsilon$-typical set
$T_\epsilon$ of the theorem box above. The two differ in how they scale:
a one-sigma window is $O(\sqrt n)$ types wide, while $T_\epsilon$ is
$O(\epsilon n)$ types wide, which is what lets its probability mass tend
to $1$. Keep the distinction in view while reading the numbers below,
because it is exactly what Practice Question 1 turns on.

<PredictBeforeReveal
  question="The typical set at n=6 is just two of the seven possible types (k=5 and k=6), together only 7 of the 64 total basis strings, about 11% of the full dimension. How much of the total probability do you expect those two types to carry?"
  options={[
    { label: "Almost none, since probability should spread roughly evenly across all 64 strings", value: "none" },
    { label: "A modest share, maybe 20-30%, since it's such a small slice of the space", value: "modest" },
    { label: "The vast majority, well over 85% of the total probability", value: "vast" },
  ]}
  correctValue="vast"
  explanation="Together k=5 and k=6 carry about 88.6% of the total probability, computed just below, from only 11% of the dimension. That lopsidedness, a tiny subspace holding almost all the weight, is the entire mechanism behind entanglement concentration: project onto that small typical subspace and you keep almost everything that matters while discarding 89% of the dimension."
/>

Their
combined probability mass is

$$
P(k=5)+P(k=6) = 0.354294+0.531441 = 0.885735 \approx 88.57\%
$$

leaving only $\approx 11.43\%$ of the weight on the remaining types
$k=0,\ldots,4$. At $n=6$, the great majority of the probability already
sits on just two of the seven possible types. Read that as a fact about
$n=6$ and not as the start of a trend: the mass inside a *fixed*
one-sigma window does not climb toward $1$ as $n$ grows, it settles at
the Gaussian one-sigma value $2\Phi(1)-1\approx0.683$, which Practice
Question 1 works out. What does behave the way the theorem promises is
the *dimension* fraction, which is the next number. In terms of
raw *dimension* (individual basis strings, not types): those two typical
types together account for $\binom{6}{5}+\binom{6}{6}=6+1=7$ of the full
$2^6=64$-dimensional space: only $10.9\%$ of the dimension, yet
$88.6\%$ of the probability. The asymptotic prediction
$2^{nH(p)}=2^{6\times0.468996}\approx7.03$ is already close
to the exact count of $7$, even at this small, by-hand-checkable $n$.
Entanglement concentration's target rate here is
$m\approx nH(p)=6\times0.468996\approx2.81$ Bell pairs from six copies
(`distillableBellPairsWorked` above), consistent with the typical
subspace's actual dimension of $7$ sitting between $2^2=4$ and $2^3=8$,
i.e. embeddable in $3$ qubits per side with some residual imperfection,
not a clean integer number of pairs at this small $n$. Only as
$n\to\infty$ does $nH(p)$ round cleanly to a whole number of
near-perfect Bell pairs per copy.

<Callout type="note">
This is an illustration of the $n\to\infty$ theorem, not the theorem
itself, and the two halves of it behave differently. The dimension
statement "$|T_\epsilon|\approx 2^{nH(p)}$" does point the right way here:
$7$ against a predicted $7.03$, and a fraction of the full space that
keeps shrinking with $n$. The probability statement does not, because the
set being measured is the one-sigma window rather than $T_\epsilon$: its
mass falls from $94.77\%$ at $n=4$ to $88.57\%$ here and converges to
$\approx0.683$, not to $1$. Only the $\epsilon$-typical set, wide enough
to swallow the $\sqrt n$ fluctuations, captures everything in the limit,
and it is that set the protocol and the $m/n\to H(p)$ rate rest on. See
the cautions below for a third way this small-$n$ example falls short.
</Callout>

## Interactive: the Typical Subspace, Concretely

<BarChart
  bars={distributionBars6}
  maxValue={1}
  ariaLabel="Bar chart of the binomial-weighted Schmidt coefficient probability P(k) for k=0 through 6, for n=6 copies of the partially entangled state with p=0.9, drawn on a full 0-to-1 probability scale. The k=5 and k=6 bars are highlighted as the typical set and fill 35 and 53 percent of the frame respectively, together carrying about 88.6 percent of the total probability, while k=0 through k=4 are barely visible"
/>

The vertical axis is the full probability range $[0,1]$, so bar height
is readable as probability directly, with no normalization to the
tallest bar. The two highlighted bars, $k=5$ and $k=6$, are the typical set
computed above: together they fill $88.6\%$ of the frame's height while
the other five types are barely off the axis. Notice that
$k=6$ (all six copies landing in the $|00\rangle$ branch) is *both*
the single most probable individual basis string (Schmidt coefficient
$0.9^6\approx0.531$, larger than any other single string's) *and* part
of the typical set; at this particular $p$ and $n$ the two notions
happen to coincide, which is the coincidence flagged in Common
Mistakes below as something that stops holding for large $n$. The bars
fall off sharply moving away from $k=6$, the concentration the
typical-subspace theorem predicts, well before $n$ is anywhere near
asymptotically large.

## Typical Sequences Are Not the Likeliest Ones

<Callout type="warning">
**Treating "individually most probable" and "typical" as the same
thing.** At $n=6$, the individual Schmidt coefficient for the
all-$|00\rangle$ string ($k=6$, weight $0.9^6=0.531441$) is exactly
$9\times$ larger than any single $k=5$ string's coefficient
($0.9^5\times0.1=0.059049$, the ratio computed directly as
`flatnessRatio6` above). In this worked example $k=6$ happens to be both
the single most probable individual string *and* inside the typical
set, but that agreement is a small-$n$, large-$p$ coincidence, not a
general fact. The typical-subspace theorem is a statement about
concentration of the *empirical frequency* $k/n$ around $p$, not about
which single string is most likely; for large $n$, the probability of
the single all-$|00\rangle$ string, $p^n$, vanishes, even
though it always remains the single most probable individual string.
Only in the $n\to\infty$ limit do "typical" and "individually most
probable" cleanly separate into visibly different sets.
</Callout>

<Callout type="warning">
**Assuming the typical subspace's Schmidt coefficients are already
"nearly equal" at small $n$.** The theorem's claim that typical
coefficients are all $\approx 2^{-nH(p)}$ is an $n\to\infty$ statement.
Here at $n=6$, the two typical coefficients are $0.9^6\approx0.531$ and
$0.9^5\times0.1\approx0.059$ (per string), a factor of $9$ apart, not
remotely flat. Reporting "the typical subspace is nearly maximally
entangled" from this $n=6$ example alone, without the asymptotic
caveat, overstates what six copies actually demonstrate; the honest
claim is that the *trend* toward flatness (relative to the exponential
scale $2^{-nH(p)}$) is visible, not that flatness has been reached.
</Callout>

## Practice Questions

1. For $p=0.9$ and $n=4$ copies (small enough to enumerate directly),
   find the mean and standard deviation of the type distribution, define
   the typical set the same way as this lesson (types $k$ with
   $|k-np|\leq\sigma$, endpoints included), and compute the total
   probability mass it captures.

   <details className="answer-reveal">
   <summary>Answer</summary>

   Mean $np=3.6$, standard deviation $\sigma=\sqrt{np(1-p)}=\sqrt{0.36}=0.6$,
   typical set $\{3,4\}$, carrying $94.77\%$ of the probability from
   $5$ of the $16$ basis strings.

   The full type distribution
   $P(k)=\binom4k(0.9)^k(0.1)^{4-k}$ is short enough to write out:

   | $k$ | $\binom4k$ | $P(k)$ |
   |---|---|---|
   | 0 | 1 | 0.000100 |
   | 1 | 4 | 0.003600 |
   | 2 | 6 | 0.048600 |
   | 3 | 4 | 0.291600 |
   | 4 | 1 | 0.656100 |

   summing to exactly $1$. The window $|k-3.6|\leq0.6$ is
   $k\in[3.0,4.2]$, so $k=3$ sits exactly on the boundary and $k=4$ is
   inside. Including the boundary,

   $$
   P(3)+P(4)=0.291600+0.656100=0.947700,
   $$

   from $\binom43+\binom44=5$ strings out of $2^4=16$, which is $31.25\%$
   of the dimension. (The original wording of this question said "within
   one standard deviation" without saying whether the boundary counts; at
   $n=4$ that is not a quibble, since $k=3$ lands exactly on it and carries
   $29\%$ of the mass. The statement above fixes the convention.)

   Now compare against $n=6$ from the lesson, because the comparison is
   more instructive than either number alone. Going from $n=4$ to $n=6$,
   the dimension fraction fell usefully, $31.25\%\to10.9\%$, which is the
   trend the theorem predicts. But the probability mass fell too,
   $94.77\%\to88.57\%$, which is the wrong direction if you are expecting
   the typical set to capture everything in the limit.

   It is not the wrong direction, and the reason is worth having, because
   it is a place the small-$n$ illustration and the actual theorem come
   apart. A window of *fixed* width one standard deviation does not
   capture everything asymptotically. By the central limit theorem it
   converges to the Gaussian one-sigma mass, $2\Phi(1)-1\approx0.683$.
   Computing it directly for $p=0.9$: $0.759$ at $n=100$, $0.722$ at
   $n=400$, $0.703$ at $n=1600$, heading for $0.683$.

   The typical set of the asymptotic equipartition property is a different
   and wider set: all $k$ with
   $\big|-\tfrac1n\log_2P-H(p)\big|\leq\epsilon$, which is a window of
   width of order $\epsilon n$ rather than $\sqrt n$. That set has
   probability tending to $1$ *and* dimension $2^{n(H(p)\pm\epsilon)}$,
   because a window growing like $n$ swallows the $\sqrt n$ fluctuations
   while still occupying an exponentially negligible slice of the space.
   The one-sigma window is a legitimate stand-in for building intuition at
   $n=4$ or $6$, where it already shows a small slice of dimension holding
   most of the weight, but it is the $\epsilon$-typical set that the
   protocol and the $m/n\to H(p)$ rate actually rest on.

   </details>

2. A source with $p=0.5$ (a *maximally* entangled state to begin with)
   is fed into the same entanglement-concentration argument. What is
   $H(0.5)$, and what does entanglement concentration have left to do in
   this case? Explain in terms of the theorem's rate $m/n\to H(p)$.

   <details className="answer-reveal">
   <summary>Answer</summary>

   $H(0.5)=1$ bit, the rate is $m/n\to1$, and the protocol has nothing to
   do: it is the identity.

   At $p=0.5$ the source state is
   $|\psi\rangle=\tfrac1{\sqrt2}(|00\rangle+|11\rangle)$, which *is*
   $|\Phi^+\rangle$. So $n$ copies are already $n$ perfect Bell pairs, and
   a protocol delivering $m\approx nH(p)=n$ pairs delivers exactly what it
   was handed. Concentration concentrates nothing because nothing is
   diffuse.

   Every step of the machinery degenerates in the same way, which is the
   check worth running. The type distribution becomes
   $\mathrm{Binomial}(n,\tfrac12)$, every Schmidt coefficient is
   $\lambda_k=2^{-n}$ regardless of $k$, so the joint state is already
   maximally entangled across the *whole* $2^n$-dimensional space. The
   typical subspace has dimension $2^{nH(0.5)}=2^n$: it is the entire
   space, so the projection Alice performs is the identity operator and
   succeeds with probability exactly $1$ even at finite $n$, rather than
   only asymptotically.

   Reading it through the rate makes the boundary clear. $H(p)\leq1$ for
   every $p$, with equality only at $p=\tfrac12$, so $p=\tfrac12$ is where
   the rate is maximal and is the one point where LOCC gets a full Bell
   pair per copy. It cannot do better: extracting more than one Bell pair
   per copy would mean LOCC created entanglement, which the
   monotonicity result cited above forbids.

   The opposite degenerate case is worth naming for contrast. At $p=1$ the
   state is the product $|00\rangle$, $H(1)=0$, and the rate is zero: again
   nothing to do, this time because there is nothing there. The protocol
   is only interesting strictly between, where the entanglement is real but
   spread thinly across many copies, and the typical subspace is a genuine
   proper subspace.

   One more reason the endpoints matter. Pure-state entanglement
   manipulation is *reversible*: concentration turns $n$ copies into
   $nH(p)$ Bell pairs, and dilution turns $m$ Bell pairs back into
   $m/H(p)$ copies, so $S(\rho_A)$ is a genuine exchange rate with no
   spread between buying and selling. At $p=\tfrac12$ that exchange rate is
   $1$ and both directions are the identity map. This reversibility is
   exactly what fails for mixed states, where distillable entanglement and
   entanglement cost are different numbers, and it is why the pure-state
   case is the one that admits a single clean answer.

   </details>

3. Explain, in your own words, why the coherent information
   $I_c(A\rangle B)=S(\rho_B)-S(\rho_{AB})$ is the natural generalization
   of this lesson's rate $H(p)=S(\rho_A)$ once the shared state is mixed
   rather than pure. (Hint: what does $S(\rho_{AB})$ reduce to when
   $\rho_{AB}$ is pure?)

   <details className="answer-reveal">
   <summary>Answer</summary>

   Because it reduces to exactly $S(\rho_A)$ on pure states, and the term
   it adds is precisely the correction for an environment that a pure state
   does not have.

   The reduction is two lines. If $\rho_{AB}$ is pure then
   $S(\rho_{AB})=0$, and the Schmidt decomposition gives
   $S(\rho_B)=S(\rho_A)$. Hence

   $$
   I_c(A\rangle B)=S(\rho_B)-S(\rho_{AB})=S(\rho_A)-0=S(\rho_A)=H(p),
   $$

   this lesson's rate on the nose. Any candidate generalization has to pass
   that test, so passing it is necessary rather than impressive on its own.

   What makes it the *right* generalization is what the subtracted term is
   doing. Purify $\rho_{AB}$ to $|\psi\rangle_{ABE}$ with an inaccessible
   environment $E$. Then $S(\rho_{AB})=S(\rho_E)$, so

   $$
   I_c(A\rangle B)=S(\rho_B)-S(\rho_E):
   $$

   what $B$ holds, minus what the environment holds. Entanglement Alice
   shares with Bob is only useful if the environment is not also holding a
   copy of the relevant correlations, and that competition is exactly the
   feature a pure state lacks, since there $S(\rho_E)=0$ and the correction
   vanishes. Mixedness *is* an environment, and coherent information is
   $S(\rho_A)$ with the environment's share deducted.

   Three consistency checks make the identification stick rather than
   merely plausible. It equals $-S(A|B)$, so it is positive precisely when
   conditional entropy is negative, which the entropy lesson tied to
   genuinely quantum rather than classical correlation. It is monotone
   under local processing on $B$, by the data-processing inequality, which
   any legitimate resource rate must be. And operationally it is the
   hashing protocol's achievable rate and, regularized, the quantum
   capacity itself, so it is the rate at which the general protocols this
   lesson sketched actually run.

   The honest qualification, which the pure-state case makes easy to miss:
   for mixed states, $I_c$ does *not* equal the distillable entanglement in
   general. It is a lower bound achievable by one-way protocols, two-way
   LOCC can beat it, and even the regularized quantity is not known in
   closed form for most channels. The clean single number $S(\rho_A)$ that
   answers everything for pure states has no mixed-state successor, because
   distillable entanglement and entanglement cost genuinely differ there.
   Coherent information is the right generalization of the *rate*, not a
   restoration of the pure-state theory's completeness.

   </details>

<PracticeLinks problems={practiceProblems} />

## Summary

- **Entanglement distillation** converts many copies of a partially
  entangled or noisy shared state into fewer, nearly-perfect Bell pairs,
  using only LOCC.
- For $n$ copies of a pure partially-entangled state
  $|\psi\rangle=\sqrt p|00\rangle+\sqrt{1-p}|11\rangle$, the joint
  Schmidt coefficients are binomially weighted by type $k$; the
  $\epsilon$-**typical subspace** of types near $k\approx pn$ has
  dimension $\approx2^{nH(p)}$ and captures almost all the probability
  weight as $n\to\infty$. The small, by-hand $n=6,p=0.9$ example above
  shows the dimension side of that (two of seven types, $7$ of $64$
  basis strings, against a predicted $7.03$) using a one-sigma window in
  place of $T_\epsilon$; that window's $88.6\%$ of the weight is a
  finite-$n$ figure heading for $2\Phi(1)-1\approx0.683$, not for $1$.
- Projecting onto the typical subspace and relabeling
  (**entanglement concentration**, Bennett–Bernstein–Popescu–Schumacher)
  distills $m\approx nH(p)=nS(\rho_A)$ near-perfect Bell pairs, the
  optimal LOCC rate for pure states.
- For mixed, noisy shared states, more involved LOCC protocols
  (entanglement pumping, hashing) still work, with achievable rate
  bounded by the coherent information $I_c(A\rangle B)$, and for some
  state classes asymptotically equal to it, directly foreshadowing this
  course's capstone on channel capacities.
- Distillation is what makes quantum repeaters, and therefore
  long-distance QKD and teleportation, physically possible over noisy
  channels, and it is the operational justification for treating
  entanglement entropy as *the* entanglement measure in the first place.

## Distillation Under Genuine Mixing

This lesson is what makes
[Entanglement Entropy for Pure Bipartite States](/lessons/quantum-computing/entanglement-and-measurement/entanglement-entropy-for-pure-states)
operational: the entropy that lesson could only *assert* was an
entanglement measure is the distillable rate derived here, and an
asymptotic conversion rate is a fact about protocols rather than a
definition.

<NextDiscovery>
  The [capstone](/lessons/quantum-mastery/quantum-shannon-theory/capstone-what-can-be-sent-through-noise)
  takes the coherent-information bound this lesson stopped at and asks it
  of a channel rather than a state: exactly how many classical bits and
  how many quantum bits survive one use of a given noisy channel. For the
  mixed-state entanglement measures a general distillation protocol is
  judged against, see [Relative Entropy and Mixed-State
  Entanglement](/lessons/quantum-mastery/quantum-information-theory/relative-entropy-and-mixed-state-entanglement).
</NextDiscovery>
